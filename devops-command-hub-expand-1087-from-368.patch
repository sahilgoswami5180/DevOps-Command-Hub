diff --git a/public/data/ansible.commands.json b/public/data/ansible.commands.json
index ed5ce8a..025cfea 100644
--- a/public/data/ansible.commands.json
+++ b/public/data/ansible.commands.json
@@ -250,5 +250,496 @@
       "ansible-galaxy",
       "devops"
     ]
+  },
+  {
+    "id": "ansible-ansible-localhost-m-setup",
+    "command": "ansible localhost -m setup",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible localhost -m setup` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible localhost -m setup",
+    "example": "ansible localhost -m setup",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-command-a-uptime",
+    "command": "ansible all -m command -a \"uptime\"",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m command -a \"uptime\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m command -a \"uptime\"",
+    "example": "ansible all -m command -a \"uptime\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-shell-a-free-h",
+    "command": "ansible all -m shell -a \"free -h\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m shell -a \"free -h\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m shell -a \"free -h\"",
+    "example": "ansible all -m shell -a \"free -h\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-copy-a-src-file-dest-tmp-file",
+    "command": "ansible all -m copy -a \"src=file dest=/tmp/file\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m copy -a \"src=file dest=/tmp/file\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m copy -a \"src=file dest=/tmp/file\"",
+    "example": "ansible all -m copy -a \"src=file dest=/tmp/file\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-file-a-path-tmp-test-state-directory",
+    "command": "ansible all -m file -a \"path=/tmp/test state=directory\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m file -a \"path=/tmp/test state=directory\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m file -a \"path=/tmp/test state=directory\"",
+    "example": "ansible all -m file -a \"path=/tmp/test state=directory\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-service-a-name-nginx-state-restarted",
+    "command": "ansible all -m service -a \"name=nginx state=restarted\"",
+    "title": "Ansible restart/reload karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m service -a \"name=nginx state=restarted\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m service -a \"name=nginx state=restarted\"",
+    "example": "ansible all -m service -a \"name=nginx state=restarted\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-apt-a-name-nginx-state-present-become",
+    "command": "ansible all -m apt -a \"name=nginx state=present\" --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m apt -a \"name=nginx state=present\" --become` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m apt -a \"name=nginx state=present\" --become",
+    "example": "ansible all -m apt -a \"name=nginx state=present\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-yum-a-name-nginx-state-present-become",
+    "command": "ansible all -m yum -a \"name=nginx state=present\" --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m yum -a \"name=nginx state=present\" --become` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m yum -a \"name=nginx state=present\" --become",
+    "example": "ansible all -m yum -a \"name=nginx state=present\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-user-a-name-deploy-state-present-become",
+    "command": "ansible all -m user -a \"name=deploy state=present\" --become",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m user -a \"name=deploy state=present\" --become` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m user -a \"name=deploy state=present\" --become",
+    "example": "ansible all -m user -a \"name=deploy state=present\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-reboot-become",
+    "command": "ansible all -m reboot --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m reboot --become` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible all -m reboot --become",
+    "example": "ansible all -m reboot --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-deploy-yml-i-inventory-ini",
+    "command": "ansible-playbook deploy.yml -i inventory.ini",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-playbook deploy.yml -i inventory.ini` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-playbook deploy.yml -i inventory.ini",
+    "example": "ansible-playbook deploy.yml -i inventory.ini",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-deploy-yml-diff",
+    "command": "ansible-playbook deploy.yml --diff",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-playbook deploy.yml --diff` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-playbook deploy.yml --diff",
+    "example": "ansible-playbook deploy.yml --diff",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-deploy-yml-syntax-check",
+    "command": "ansible-playbook deploy.yml --syntax-check",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-playbook deploy.yml --syntax-check` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-playbook deploy.yml --syntax-check",
+    "example": "ansible-playbook deploy.yml --syntax-check",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-deploy-yml-start-at-task-task-name",
+    "command": "ansible-playbook deploy.yml --start-at-task \"Task name\"",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-playbook deploy.yml --start-at-task \"Task name\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-playbook deploy.yml --start-at-task \"Task name\"",
+    "example": "ansible-playbook deploy.yml --start-at-task \"Task name\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-deploy-yml-extra-vars-env-prod",
+    "command": "ansible-playbook deploy.yml --extra-vars \"env=prod\"",
+    "title": "Ansible apply/deploy/run karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-playbook deploy.yml --extra-vars \"env=prod\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-playbook deploy.yml --extra-vars \"env=prod\"",
+    "example": "ansible-playbook deploy.yml --extra-vars \"env=prod\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-doc-apt",
+    "command": "ansible-doc apt",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-doc apt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-doc apt",
+    "example": "ansible-doc apt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-doc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-doc-l",
+    "command": "ansible-doc -l",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-doc -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-doc -l",
+    "example": "ansible-doc -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-doc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-config-dump",
+    "command": "ansible-config dump",
+    "title": "Ansible backup/export lena",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-config dump` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-config dump",
+    "example": "ansible-config dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-config",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-config-view",
+    "command": "ansible-config view",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-config view` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-config view",
+    "example": "ansible-config view",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-config",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-vault-view-secrets-yml",
+    "command": "ansible-vault view secrets.yml",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-vault view secrets.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-vault view secrets.yml",
+    "example": "ansible-vault view secrets.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-vault",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-vault-rekey-secrets-yml",
+    "command": "ansible-vault rekey secrets.yml",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible-vault rekey secrets.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-vault rekey secrets.yml",
+    "example": "ansible-vault rekey secrets.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-vault",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-galaxy-role-init-role-name",
+    "command": "ansible-galaxy role init role_name",
+    "title": "Ansible resource create karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-galaxy role init role_name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-galaxy role init role_name",
+    "example": "ansible-galaxy role init role_name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-galaxy",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-galaxy-collection-list",
+    "command": "ansible-galaxy collection list",
+    "title": "Ansible details/status dekhna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-galaxy collection list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-galaxy collection list",
+    "example": "ansible-galaxy collection list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-galaxy",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-pull-u-repo-playbook-yml",
+    "command": "ansible-pull -U repo playbook.yml",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-pull -U repo playbook.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ansible-pull -U repo playbook.yml",
+    "example": "ansible-pull -U repo playbook.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-pull",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/apache.commands.json b/public/data/apache.commands.json
index a358076..fe53d15 100644
--- a/public/data/apache.commands.json
+++ b/public/data/apache.commands.json
@@ -250,5 +250,385 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "apache-sudo-systemctl-stop-apache2",
+    "command": "sudo systemctl stop apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo systemctl stop apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl stop apache2",
+    "example": "sudo systemctl stop apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-systemctl-start-apache2",
+    "command": "sudo systemctl start apache2",
+    "title": "Apache apply/deploy/run karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo systemctl start apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl start apache2",
+    "example": "sudo systemctl start apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-systemctl-disable-apache2",
+    "command": "sudo systemctl disable apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo systemctl disable apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl disable apache2",
+    "example": "sudo systemctl disable apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-apache2-v",
+    "command": "apache2 -v",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `apache2 -v` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "apache2 -v",
+    "example": "apache2 -v",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "apache2",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-apache2ctl-m",
+    "command": "apache2ctl -M",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `apache2ctl -M` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "apache2ctl -M",
+    "example": "apache2ctl -M",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "apache2ctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-apache2ctl-t",
+    "command": "apache2ctl -t",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `apache2ctl -t` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "apache2ctl -t",
+    "example": "apache2ctl -t",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "apache2ctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-apache2ctl-s",
+    "command": "apache2ctl -S",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `apache2ctl -S` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "apache2ctl -S",
+    "example": "apache2ctl -S",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "apache2ctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-a2enmod-headers",
+    "command": "sudo a2enmod headers",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo a2enmod headers` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo a2enmod headers",
+    "example": "sudo a2enmod headers",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-a2enmod-proxy",
+    "command": "sudo a2enmod proxy",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo a2enmod proxy` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo a2enmod proxy",
+    "example": "sudo a2enmod proxy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-a2enmod-proxy-http",
+    "command": "sudo a2enmod proxy_http",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo a2enmod proxy_http` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo a2enmod proxy_http",
+    "example": "sudo a2enmod proxy_http",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-a2dismod-module-name",
+    "command": "sudo a2dismod module_name",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo a2dismod module_name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo a2dismod module_name",
+    "example": "sudo a2dismod module_name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-nano-etc-apache2-sites-available-example-conf",
+    "command": "sudo nano /etc/apache2/sites-available/example.conf",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo nano /etc/apache2/sites-available/example.conf` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nano /etc/apache2/sites-available/example.conf",
+    "example": "sudo nano /etc/apache2/sites-available/example.conf",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-tail-n-100-var-log-apache2-access-log",
+    "command": "sudo tail -n 100 /var/log/apache2/access.log",
+    "title": "Apache logs/debugging dekhna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo tail -n 100 /var/log/apache2/access.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo tail -n 100 /var/log/apache2/access.log",
+    "example": "sudo tail -n 100 /var/log/apache2/access.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-tail-n-100-var-log-apache2-error-log",
+    "command": "sudo tail -n 100 /var/log/apache2/error.log",
+    "title": "Apache logs/debugging dekhna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo tail -n 100 /var/log/apache2/error.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo tail -n 100 /var/log/apache2/error.log",
+    "example": "sudo tail -n 100 /var/log/apache2/error.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-grep-r-servername-etc-apache2",
+    "command": "sudo grep -R \"ServerName\" /etc/apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo grep -R \"ServerName\" /etc/apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep -R \"ServerName\" /etc/apache2",
+    "example": "sudo grep -R \"ServerName\" /etc/apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-curl-h-host-example-com-http-127-0-0-1",
+    "command": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `curl -H \"Host: example.com\" http://127.0.0.1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "example": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-apt-install-apache2",
+    "command": "sudo apt install apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo apt install apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt install apache2",
+    "example": "sudo apt install apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-apt-remove-apache2",
+    "command": "sudo apt remove apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo apt remove apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt remove apache2",
+    "example": "sudo apt remove apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-sudo-ss-tulpn-grep-apache",
+    "command": "sudo ss -tulpn | grep apache",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `sudo ss -tulpn | grep apache` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ss -tulpn | grep apache",
+    "example": "sudo ss -tulpn | grep apache",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "apache-ps-aux-grep-apache2",
+    "command": "ps aux | grep apache2",
+    "title": "Apache command use karna",
+    "category": "Apache",
+    "difficulty": "Beginner",
+    "use_case": "Apache me `ps aux | grep apache2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ps aux | grep apache2",
+    "example": "ps aux | grep apache2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "apache",
+      "ps",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/aws.commands.json b/public/data/aws.commands.json
index 42e8930..40ad9f4 100644
--- a/public/data/aws.commands.json
+++ b/public/data/aws.commands.json
@@ -358,5 +358,1097 @@
       "aws",
       "devops"
     ]
+  },
+  {
+    "id": "aws-cli-aws-configure-get-region",
+    "command": "aws configure get region",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws configure get region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws configure get region",
+    "example": "aws configure get region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-configure-set-output-json",
+    "command": "aws configure set output json",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws configure set output json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws configure set output json",
+    "example": "aws configure set output json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3-mb-s3-bucket-name",
+    "command": "aws s3 mb s3://bucket-name",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws s3 mb s3://bucket-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws s3 mb s3://bucket-name",
+    "example": "aws s3 mb s3://bucket-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3-rb-s3-bucket-name",
+    "command": "aws s3 rb s3://bucket-name",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws s3 rb s3://bucket-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws s3 rb s3://bucket-name",
+    "example": "aws s3 rb s3://bucket-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3-rm-s3-bucket-key",
+    "command": "aws s3 rm s3://bucket/key",
+    "title": "AWS CLI delete/destroy action chalana",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws s3 rm s3://bucket/key` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws s3 rm s3://bucket/key",
+    "example": "aws s3 rm s3://bucket/key",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3-presign-s3-bucket-key",
+    "command": "aws s3 presign s3://bucket/key",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws s3 presign s3://bucket/key` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws s3 presign s3://bucket/key",
+    "example": "aws s3 presign s3://bucket/key",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-vpcs",
+    "command": "aws ec2 describe-vpcs",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-vpcs` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-vpcs",
+    "example": "aws ec2 describe-vpcs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-subnets",
+    "command": "aws ec2 describe-subnets",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-subnets` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-subnets",
+    "example": "aws ec2 describe-subnets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-route-tables",
+    "command": "aws ec2 describe-route-tables",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-route-tables` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-route-tables",
+    "example": "aws ec2 describe-route-tables",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-addresses",
+    "command": "aws ec2 describe-addresses",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-addresses` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-addresses",
+    "example": "aws ec2 describe-addresses",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-allocate-address",
+    "command": "aws ec2 allocate-address",
+    "title": "AWS CLI resource create karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 allocate-address` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 allocate-address",
+    "example": "aws ec2 allocate-address",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-associate-address-instance-id-i-xxx-allocation-id-eipalloc-xxx",
+    "command": "aws ec2 associate-address --instance-id i-xxx --allocation-id eipalloc-xxx",
+    "title": "AWS CLI resource create karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 associate-address --instance-id i-xxx --allocation-id eipalloc-xxx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 associate-address --instance-id i-xxx --allocation-id eipalloc-xxx",
+    "example": "aws ec2 associate-address --instance-id i-xxx --allocation-id eipalloc-xxx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-volumes",
+    "command": "aws ec2 describe-volumes",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-volumes` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-volumes",
+    "example": "aws ec2 describe-volumes",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-create-snapshot-volume-id-vol-xxx",
+    "command": "aws ec2 create-snapshot --volume-id vol-xxx",
+    "title": "AWS CLI resource create karna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 create-snapshot --volume-id vol-xxx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 create-snapshot --volume-id vol-xxx",
+    "example": "aws ec2 create-snapshot --volume-id vol-xxx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-snapshots-owner-ids-self",
+    "command": "aws ec2 describe-snapshots --owner-ids self",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Beginner",
+    "use_case": "AWS CLI me `aws ec2 describe-snapshots --owner-ids self` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-snapshots --owner-ids self",
+    "example": "aws ec2 describe-snapshots --owner-ids self",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-key-pairs",
+    "command": "aws ec2 describe-key-pairs",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-key-pairs` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-key-pairs",
+    "example": "aws ec2 describe-key-pairs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-images-owners-self",
+    "command": "aws ec2 describe-images --owners self",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-images --owners self` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ec2 describe-images --owners self",
+    "example": "aws ec2 describe-images --owners self",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-describe-load-balancers",
+    "command": "aws elbv2 describe-load-balancers",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 describe-load-balancers` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws elbv2 describe-load-balancers",
+    "example": "aws elbv2 describe-load-balancers",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-describe-target-groups",
+    "command": "aws elbv2 describe-target-groups",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 describe-target-groups` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws elbv2 describe-target-groups",
+    "example": "aws elbv2 describe-target-groups",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-describe-target-health-target-group-arn-arn",
+    "command": "aws elbv2 describe-target-health --target-group-arn arn",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 describe-target-health --target-group-arn arn` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws elbv2 describe-target-health --target-group-arn arn",
+    "example": "aws elbv2 describe-target-health --target-group-arn arn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-autoscaling-describe-auto-scaling-groups",
+    "command": "aws autoscaling describe-auto-scaling-groups",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws autoscaling describe-auto-scaling-groups` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws autoscaling describe-auto-scaling-groups",
+    "example": "aws autoscaling describe-auto-scaling-groups",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-describe-clusters-clusters-name",
+    "command": "aws ecs describe-clusters --clusters name",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs describe-clusters --clusters name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecs describe-clusters --clusters name",
+    "example": "aws ecs describe-clusters --clusters name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-describe-services-cluster-name-services-svc",
+    "command": "aws ecs describe-services --cluster name --services svc",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs describe-services --cluster name --services svc` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecs describe-services --cluster name --services svc",
+    "example": "aws ecs describe-services --cluster name --services svc",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-list-tasks-cluster-name",
+    "command": "aws ecs list-tasks --cluster name",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs list-tasks --cluster name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecs list-tasks --cluster name",
+    "example": "aws ecs list-tasks --cluster name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-describe-tasks-cluster-name-tasks-taskid",
+    "command": "aws ecs describe-tasks --cluster name --tasks taskid",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs describe-tasks --cluster name --tasks taskid` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecs describe-tasks --cluster name --tasks taskid",
+    "example": "aws ecs describe-tasks --cluster name --tasks taskid",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecr-describe-repositories",
+    "command": "aws ecr describe-repositories",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecr describe-repositories` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecr describe-repositories",
+    "example": "aws ecr describe-repositories",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecr-list-images-repository-name-repo",
+    "command": "aws ecr list-images --repository-name repo",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecr list-images --repository-name repo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecr list-images --repository-name repo",
+    "example": "aws ecr list-images --repository-name repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecr-describe-images-repository-name-repo",
+    "command": "aws ecr describe-images --repository-name repo",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecr describe-images --repository-name repo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ecr describe-images --repository-name repo",
+    "example": "aws ecr describe-images --repository-name repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-logs-tail-log-group-since-1h",
+    "command": "aws logs tail log-group --since 1h",
+    "title": "AWS CLI logs/debugging dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws logs tail log-group --since 1h` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws logs tail log-group --since 1h",
+    "example": "aws logs tail log-group --since 1h",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-logs-filter-log-events-log-group-name-group-filter-pattern-error",
+    "command": "aws logs filter-log-events --log-group-name group --filter-pattern ERROR",
+    "title": "AWS CLI logs/debugging dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws logs filter-log-events --log-group-name group --filter-pattern ERROR` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws logs filter-log-events --log-group-name group --filter-pattern ERROR",
+    "example": "aws logs filter-log-events --log-group-name group --filter-pattern ERROR",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-describe-db-clusters",
+    "command": "aws rds describe-db-clusters",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds describe-db-clusters` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws rds describe-db-clusters",
+    "example": "aws rds describe-db-clusters",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-describe-db-snapshots",
+    "command": "aws rds describe-db-snapshots",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds describe-db-snapshots` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws rds describe-db-snapshots",
+    "example": "aws rds describe-db-snapshots",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-create-db-snapshot-db-instance-identifier-db-db-snapshot-identifier-snap",
+    "command": "aws rds create-db-snapshot --db-instance-identifier db --db-snapshot-identifier snap",
+    "title": "AWS CLI resource create karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds create-db-snapshot --db-instance-identifier db --db-snapshot-identifier snap` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws rds create-db-snapshot --db-instance-identifier db --db-snapshot-identifier snap",
+    "example": "aws rds create-db-snapshot --db-instance-identifier db --db-snapshot-identifier snap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-lambda-list-functions",
+    "command": "aws lambda list-functions",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws lambda list-functions` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws lambda list-functions",
+    "example": "aws lambda list-functions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-lambda-invoke-function-name-fn-output-json",
+    "command": "aws lambda invoke --function-name fn output.json",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws lambda invoke --function-name fn output.json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws lambda invoke --function-name fn output.json",
+    "example": "aws lambda invoke --function-name fn output.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-list-users",
+    "command": "aws iam list-users",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws iam list-users` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws iam list-users",
+    "example": "aws iam list-users",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-list-roles",
+    "command": "aws iam list-roles",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws iam list-roles` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws iam list-roles",
+    "example": "aws iam list-roles",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-list-policies",
+    "command": "aws iam list-policies",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws iam list-policies` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws iam list-policies",
+    "example": "aws iam list-policies",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-secretsmanager-list-secrets",
+    "command": "aws secretsmanager list-secrets",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws secretsmanager list-secrets` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws secretsmanager list-secrets",
+    "example": "aws secretsmanager list-secrets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-secretsmanager-get-secret-value-secret-id-name",
+    "command": "aws secretsmanager get-secret-value --secret-id name",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws secretsmanager get-secret-value --secret-id name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws secretsmanager get-secret-value --secret-id name",
+    "example": "aws secretsmanager get-secret-value --secret-id name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ssm-get-parameter-name-name-with-decryption",
+    "command": "aws ssm get-parameter --name name --with-decryption",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ssm get-parameter --name name --with-decryption` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ssm get-parameter --name name --with-decryption",
+    "example": "aws ssm get-parameter --name name --with-decryption",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudwatch-describe-alarms",
+    "command": "aws cloudwatch describe-alarms",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws cloudwatch describe-alarms` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws cloudwatch describe-alarms",
+    "example": "aws cloudwatch describe-alarms",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudwatch-get-metric-statistics",
+    "command": "aws cloudwatch get-metric-statistics",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws cloudwatch get-metric-statistics` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws cloudwatch get-metric-statistics",
+    "example": "aws cloudwatch get-metric-statistics",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudfront-list-distributions",
+    "command": "aws cloudfront list-distributions",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws cloudfront list-distributions` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws cloudfront list-distributions",
+    "example": "aws cloudfront list-distributions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-route53-list-hosted-zones",
+    "command": "aws route53 list-hosted-zones",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws route53 list-hosted-zones` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws route53 list-hosted-zones",
+    "example": "aws route53 list-hosted-zones",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-route53-list-resource-record-sets-hosted-zone-id-zone",
+    "command": "aws route53 list-resource-record-sets --hosted-zone-id ZONE",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws route53 list-resource-record-sets --hosted-zone-id ZONE` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws route53 list-resource-record-sets --hosted-zone-id ZONE",
+    "example": "aws route53 list-resource-record-sets --hosted-zone-id ZONE",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-acm-list-certificates",
+    "command": "aws acm list-certificates",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws acm list-certificates` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws acm list-certificates",
+    "example": "aws acm list-certificates",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ses-get-send-quota",
+    "command": "aws ses get-send-quota",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ses get-send-quota` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws ses get-send-quota",
+    "example": "aws ses get-send-quota",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-sns-list-topics",
+    "command": "aws sns list-topics",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws sns list-topics` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws sns list-topics",
+    "example": "aws sns list-topics",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-sqs-list-queues",
+    "command": "aws sqs list-queues",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws sqs list-queues` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws sqs list-queues",
+    "example": "aws sqs list-queues",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-list-clusters",
+    "command": "aws eks list-clusters",
+    "title": "AWS CLI details/status dekhna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws eks list-clusters` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws eks list-clusters",
+    "example": "aws eks list-clusters",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-update-kubeconfig-name-cluster-region-region",
+    "command": "aws eks update-kubeconfig --name cluster --region region",
+    "title": "AWS CLI apply/deploy/run karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws eks update-kubeconfig --name cluster --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "aws eks update-kubeconfig --name cluster --region region",
+    "example": "aws eks update-kubeconfig --name cluster --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/azure.commands.json b/public/data/azure.commands.json
index d63b36f..d613f97 100644
--- a/public/data/azure.commands.json
+++ b/public/data/azure.commands.json
@@ -286,5 +286,709 @@
       "az",
       "devops"
     ]
+  },
+  {
+    "id": "azure-cli-az-version",
+    "command": "az version",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az version` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az version",
+    "example": "az version",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-extension-list",
+    "command": "az extension list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az extension list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az extension list",
+    "example": "az extension list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-provider-list-output-table",
+    "command": "az provider list --output table",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az provider list --output table` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az provider list --output table",
+    "example": "az provider list --output table",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-group-delete-n-rg",
+    "command": "az group delete -n rg",
+    "title": "Azure CLI delete/destroy action chalana",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az group delete -n rg` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az group delete -n rg",
+    "example": "az group delete -n rg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-show-g-rg-n-vm",
+    "command": "az vm show -g rg -n vm",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az vm show -g rg -n vm` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az vm show -g rg -n vm",
+    "example": "az vm show -g rg -n vm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-list-ip-addresses",
+    "command": "az vm list-ip-addresses",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az vm list-ip-addresses` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az vm list-ip-addresses",
+    "example": "az vm list-ip-addresses",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-open-port-g-rg-n-vm-port-80",
+    "command": "az vm open-port -g rg -n vm --port 80",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az vm open-port -g rg -n vm --port 80` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az vm open-port -g rg -n vm --port 80",
+    "example": "az vm open-port -g rg -n vm --port 80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-run-command-invoke-g-rg-n-vm-command-id-runshellscript-scripts-uptime",
+    "command": "az vm run-command invoke -g rg -n vm --command-id RunShellScript --scripts \"uptime\"",
+    "title": "Azure CLI apply/deploy/run karna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az vm run-command invoke -g rg -n vm --command-id RunShellScript --scripts \"uptime\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az vm run-command invoke -g rg -n vm --command-id RunShellScript --scripts \"uptime\"",
+    "example": "az vm run-command invoke -g rg -n vm --command-id RunShellScript --scripts \"uptime\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-vnet-list",
+    "command": "az network vnet list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az network vnet list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az network vnet list",
+    "example": "az network vnet list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-nsg-list",
+    "command": "az network nsg list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az network nsg list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az network nsg list",
+    "example": "az network nsg list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-public-ip-list",
+    "command": "az network public-ip list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az network public-ip list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az network public-ip list",
+    "example": "az network public-ip list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-disk-list",
+    "command": "az disk list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az disk list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az disk list",
+    "example": "az disk list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-snapshot-list",
+    "command": "az snapshot list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az snapshot list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az snapshot list",
+    "example": "az snapshot list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-show-g-rg-n-app",
+    "command": "az webapp show -g rg -n app",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az webapp show -g rg -n app` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az webapp show -g rg -n app",
+    "example": "az webapp show -g rg -n app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-restart-g-rg-n-app",
+    "command": "az webapp restart -g rg -n app",
+    "title": "Azure CLI restart/reload karna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az webapp restart -g rg -n app` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az webapp restart -g rg -n app",
+    "example": "az webapp restart -g rg -n app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-config-appsettings-list-g-rg-n-app",
+    "command": "az webapp config appsettings list -g rg -n app",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az webapp config appsettings list -g rg -n app` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az webapp config appsettings list -g rg -n app",
+    "example": "az webapp config appsettings list -g rg -n app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-config-appsettings-set-g-rg-n-app-settings-key-value",
+    "command": "az webapp config appsettings set -g rg -n app --settings KEY=value",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az webapp config appsettings set -g rg -n app --settings KEY=value` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az webapp config appsettings set -g rg -n app --settings KEY=value",
+    "example": "az webapp config appsettings set -g rg -n app --settings KEY=value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-acr-list",
+    "command": "az acr list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az acr list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az acr list",
+    "example": "az acr list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-acr-repository-show-tags-n-registry-repository-repo",
+    "command": "az acr repository show-tags -n registry --repository repo",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az acr repository show-tags -n registry --repository repo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az acr repository show-tags -n registry --repository repo",
+    "example": "az acr repository show-tags -n registry --repository repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-list",
+    "command": "az aks list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az aks list",
+    "example": "az aks list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-show-g-rg-n-cluster",
+    "command": "az aks show -g rg -n cluster",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks show -g rg -n cluster` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az aks show -g rg -n cluster",
+    "example": "az aks show -g rg -n cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-update-g-rg-n-cluster",
+    "command": "az aks update -g rg -n cluster",
+    "title": "Azure CLI apply/deploy/run karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks update -g rg -n cluster` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az aks update -g rg -n cluster",
+    "example": "az aks update -g rg -n cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-monitor-activity-log-list",
+    "command": "az monitor activity-log list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az monitor activity-log list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az monitor activity-log list",
+    "example": "az monitor activity-log list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-monitor-metrics-list-definitions",
+    "command": "az monitor metrics list-definitions",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az monitor metrics list-definitions` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az monitor metrics list-definitions",
+    "example": "az monitor metrics list-definitions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-keyvault-list",
+    "command": "az keyvault list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az keyvault list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az keyvault list",
+    "example": "az keyvault list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-keyvault-secret-show-vault-name-vault-name-secret",
+    "command": "az keyvault secret show --vault-name vault --name secret",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az keyvault secret show --vault-name vault --name secret` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az keyvault secret show --vault-name vault --name secret",
+    "example": "az keyvault secret show --vault-name vault --name secret",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-container-list-account-name-account",
+    "command": "az storage container list --account-name account",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage container list --account-name account` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az storage container list --account-name account",
+    "example": "az storage container list --account-name account",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-blob-list-account-name-account-container-name-container",
+    "command": "az storage blob list --account-name account --container-name container",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage blob list --account-name account --container-name container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az storage blob list --account-name account --container-name container",
+    "example": "az storage blob list --account-name account --container-name container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-role-assignment-list",
+    "command": "az role assignment list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az role assignment list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az role assignment list",
+    "example": "az role assignment list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-ad-signed-in-user-show",
+    "command": "az ad signed-in-user show",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az ad signed-in-user show` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az ad signed-in-user show",
+    "example": "az ad signed-in-user show",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-mysql-flexible-server-list",
+    "command": "az mysql flexible-server list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az mysql flexible-server list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az mysql flexible-server list",
+    "example": "az mysql flexible-server list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-postgres-flexible-server-list",
+    "command": "az postgres flexible-server list",
+    "title": "Azure CLI details/status dekhna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az postgres flexible-server list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "az postgres flexible-server list",
+    "example": "az postgres flexible-server list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/cicd.commands.json b/public/data/cicd.commands.json
index c0303be..1f51b18 100644
--- a/public/data/cicd.commands.json
+++ b/public/data/cicd.commands.json
@@ -286,5 +286,705 @@
       "ssh",
       "devops"
     ]
+  },
+  {
+    "id": "ci-cd-npm-ci-prefer-offline",
+    "command": "npm ci --prefer-offline",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `npm ci --prefer-offline` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "npm ci --prefer-offline",
+    "example": "npm ci --prefer-offline",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-audit",
+    "command": "npm audit",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `npm audit` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "npm audit",
+    "example": "npm audit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-audit-fix",
+    "command": "npm audit fix",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `npm audit fix` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "npm audit fix",
+    "example": "npm audit fix",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-outdated",
+    "command": "npm outdated",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `npm outdated` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "npm outdated",
+    "example": "npm outdated",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-version-patch",
+    "command": "npm version patch",
+    "title": "CI/CD details/status dekhna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `npm version patch` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "npm version patch",
+    "example": "npm version patch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-pnpm-install",
+    "command": "pnpm install",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `pnpm install` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pnpm install",
+    "example": "pnpm install",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "pnpm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-pnpm-build",
+    "command": "pnpm build",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `pnpm build` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pnpm build",
+    "example": "pnpm build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "pnpm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-yarn-install-frozen-lockfile",
+    "command": "yarn install --frozen-lockfile",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `yarn install --frozen-lockfile` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "yarn install --frozen-lockfile",
+    "example": "yarn install --frozen-lockfile",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "yarn",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-yarn-build",
+    "command": "yarn build",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `yarn build` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "yarn build",
+    "example": "yarn build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "yarn",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-composer-validate",
+    "command": "composer validate",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `composer validate` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "composer validate",
+    "example": "composer validate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "composer",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-composer-dump-autoload-o",
+    "command": "composer dump-autoload -o",
+    "title": "CI/CD backup/export lena",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `composer dump-autoload -o` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "composer dump-autoload -o",
+    "example": "composer dump-autoload -o",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "composer",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-test",
+    "command": "php artisan test",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan test` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan test",
+    "example": "php artisan test",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-migrate-status",
+    "command": "php artisan migrate:status",
+    "title": "CI/CD details/status dekhna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan migrate:status` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan migrate:status",
+    "example": "php artisan migrate:status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-optimize",
+    "command": "php artisan optimize",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan optimize` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan optimize",
+    "example": "php artisan optimize",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-optimize-clear",
+    "command": "php artisan optimize:clear",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan optimize:clear` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan optimize:clear",
+    "example": "php artisan optimize:clear",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-down",
+    "command": "php artisan down",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan down` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan down",
+    "example": "php artisan down",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-up",
+    "command": "php artisan up",
+    "title": "CI/CD apply/deploy/run karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan up` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan up",
+    "example": "php artisan up",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-horizon-terminate",
+    "command": "php artisan horizon:terminate",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan horizon:terminate` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan horizon:terminate",
+    "example": "php artisan horizon:terminate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-schedule-run",
+    "command": "php artisan schedule:run",
+    "title": "CI/CD apply/deploy/run karna",
+    "category": "CI/CD",
+    "difficulty": "Beginner",
+    "use_case": "CI/CD me `php artisan schedule:run` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "php artisan schedule:run",
+    "example": "php artisan schedule:run",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-python-m-pytest",
+    "command": "python -m pytest",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `python -m pytest` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "python -m pytest",
+    "example": "python -m pytest",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "python",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-pip-install-r-requirements-txt",
+    "command": "pip install -r requirements.txt",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `pip install -r requirements.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pip install -r requirements.txt",
+    "example": "pip install -r requirements.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "pip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-pip-freeze-requirements-txt",
+    "command": "pip freeze > requirements.txt",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `pip freeze > requirements.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pip freeze > requirements.txt",
+    "example": "pip freeze > requirements.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "pip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-mvn-test",
+    "command": "mvn test",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `mvn test` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mvn test",
+    "example": "mvn test",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "mvn",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-mvn-package",
+    "command": "mvn package",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `mvn package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mvn package",
+    "example": "mvn package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "mvn",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-gradle-build",
+    "command": "gradle build",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `gradle build` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gradle build",
+    "example": "gradle build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "gradle",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-go-test",
+    "command": "go test ./...",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `go test ./...` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "go test ./...",
+    "example": "go test ./...",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "go",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-go-build",
+    "command": "go build ./...",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `go build ./...` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "go build ./...",
+    "example": "go build ./...",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "go",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-docker-compose-f-docker-compose-prod-yml-up-d",
+    "command": "docker compose -f docker-compose.prod.yml up -d",
+    "title": "CI/CD apply/deploy/run karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `docker compose -f docker-compose.prod.yml up -d` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose -f docker-compose.prod.yml up -d",
+    "example": "docker compose -f docker-compose.prod.yml up -d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "ci",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-docker-compose-f-docker-compose-prod-yml-pull",
+    "command": "docker compose -f docker-compose.prod.yml pull",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `docker compose -f docker-compose.prod.yml pull` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose -f docker-compose.prod.yml pull",
+    "example": "docker compose -f docker-compose.prod.yml pull",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "ci",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-rsync-az-delete-dist-user-server-var-www-app",
+    "command": "rsync -az --delete dist/ user@server:/var/www/app",
+    "title": "CI/CD delete/destroy action chalana",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `rsync -az --delete dist/ user@server:/var/www/app` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "rsync -az --delete dist/ user@server:/var/www/app",
+    "example": "rsync -az --delete dist/ user@server:/var/www/app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "rsync",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-ssh-user-server-sudo-systemctl-reload-nginx",
+    "command": "ssh user@server \"sudo systemctl reload nginx\"",
+    "title": "CI/CD restart/reload karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `ssh user@server \"sudo systemctl reload nginx\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ssh user@server \"sudo systemctl reload nginx\"",
+    "example": "ssh user@server \"sudo systemctl reload nginx\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-ssh-user-server-php-artisan-migrate-force",
+    "command": "ssh user@server \"php artisan migrate --force\"",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `ssh user@server \"php artisan migrate --force\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ssh user@server \"php artisan migrate --force\"",
+    "example": "ssh user@server \"php artisan migrate --force\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-git-describe-tags",
+    "command": "git describe --tags",
+    "title": "CI/CD details/status dekhna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `git describe --tags` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git describe --tags",
+    "example": "git describe --tags",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "ci",
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-git-rev-parse-short-head",
+    "command": "git rev-parse --short HEAD",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `git rev-parse --short HEAD` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git rev-parse --short HEAD",
+    "example": "git rev-parse --short HEAD",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "ci",
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-tar-czf-release-tar-gz-dist",
+    "command": "tar -czf release.tar.gz dist",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `tar -czf release.tar.gz dist` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tar -czf release.tar.gz dist",
+    "example": "tar -czf release.tar.gz dist",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "tar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-sha256sum-release-tar-gz",
+    "command": "sha256sum release.tar.gz",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `sha256sum release.tar.gz` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sha256sum release.tar.gz",
+    "example": "sha256sum release.tar.gz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "sha256sum",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/database-backup.commands.json b/public/data/database-backup.commands.json
index d69cfcf..9f26473 100644
--- a/public/data/database-backup.commands.json
+++ b/public/data/database-backup.commands.json
@@ -286,5 +286,594 @@
       "vacuumdb",
       "devops"
     ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-db-backup-sql",
+    "command": "pg_dump db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump db > backup.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_dump db > backup.sql",
+    "example": "pg_dump db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-fc-f-backup-dump-db",
+    "command": "pg_dump -Fc -f backup.dump db",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump -Fc -f backup.dump db` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_dump -Fc -f backup.dump db",
+    "example": "pg_dump -Fc -f backup.dump db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-fd-j-4-f-backup-dir-db",
+    "command": "pg_dump -Fd -j 4 -f backup_dir db",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump -Fd -j 4 -f backup_dir db` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_dump -Fd -j 4 -f backup_dir db",
+    "example": "pg_dump -Fd -j 4 -f backup_dir db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-l-backup-dump",
+    "command": "pg_restore -l backup.dump",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore -l backup.dump` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_restore -l backup.dump",
+    "example": "pg_restore -l backup.dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-c-d-db-backup-dump",
+    "command": "pg_restore -c -d db backup.dump",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore -c -d db backup.dump` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_restore -c -d db backup.dump",
+    "example": "pg_restore -c -d db backup.dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-j-4-d-db-backup-dir",
+    "command": "pg_restore -j 4 -d db backup_dir",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore -j 4 -d db backup_dir` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_restore -j 4 -d db backup_dir",
+    "example": "pg_restore -j 4 -d db backup_dir",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dumpall-all-sql",
+    "command": "pg_dumpall > all.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dumpall > all.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_dumpall > all.sql",
+    "example": "pg_dumpall > all.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dumpall",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dumpall-roles-only-roles-sql",
+    "command": "pg_dumpall --roles-only > roles.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dumpall --roles-only > roles.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pg_dumpall --roles-only > roles.sql",
+    "example": "pg_dumpall --roles-only > roles.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dumpall",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-pg-database-size-db",
+    "command": "psql -c \"SELECT pg_database_size('db');\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT pg_database_size('db');\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"SELECT pg_database_size('db');\"",
+    "example": "psql -c \"SELECT pg_database_size('db');\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-from-pg-stat-activity",
+    "command": "psql -c \"SELECT * FROM pg_stat_activity;\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT * FROM pg_stat_activity;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"SELECT * FROM pg_stat_activity;\"",
+    "example": "psql -c \"SELECT * FROM pg_stat_activity;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-datname-from-pg-database",
+    "command": "psql -c \"SELECT datname FROM pg_database;\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT datname FROM pg_database;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"SELECT datname FROM pg_database;\"",
+    "example": "psql -c \"SELECT datname FROM pg_database;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-create-database-db",
+    "command": "psql -c \"CREATE DATABASE db;\"",
+    "title": "PostgreSQL / MySQL Backup resource create karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"CREATE DATABASE db;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"CREATE DATABASE db;\"",
+    "example": "psql -c \"CREATE DATABASE db;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-drop-database-db",
+    "command": "psql -c \"DROP DATABASE db;\"",
+    "title": "PostgreSQL / MySQL Backup delete/destroy action chalana",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"DROP DATABASE db;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"DROP DATABASE db;\"",
+    "example": "psql -c \"DROP DATABASE db;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-du",
+    "command": "psql -c \"\\du\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"\\du\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"\\du\"",
+    "example": "psql -c \"\\du\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-conninfo",
+    "command": "psql -c \"\\conninfo\"",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"\\conninfo\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "psql -c \"\\conninfo\"",
+    "example": "psql -c \"\\conninfo\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-reindexdb-db",
+    "command": "reindexdb db",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `reindexdb db` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "reindexdb db",
+    "example": "reindexdb db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "reindexdb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-vacuumdb-full-db",
+    "command": "vacuumdb --full db",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `vacuumdb --full db` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "vacuumdb --full db",
+    "example": "vacuumdb --full db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "vacuumdb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqlshow-u-user-p",
+    "command": "mysqlshow -u user -p",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqlshow -u user -p` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqlshow -u user -p",
+    "example": "mysqlshow -u user -p",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqlshow",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-show-databases",
+    "command": "mysql -e \"SHOW DATABASES;\"",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SHOW DATABASES;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysql -e \"SHOW DATABASES;\"",
+    "example": "mysql -e \"SHOW DATABASES;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-show-processlist",
+    "command": "mysql -e \"SHOW PROCESSLIST;\"",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SHOW PROCESSLIST;\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysql -e \"SHOW PROCESSLIST;\"",
+    "example": "mysql -e \"SHOW PROCESSLIST;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-show-variables-like-version",
+    "command": "mysql -e \"SHOW VARIABLES LIKE 'version';\"",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SHOW VARIABLES LIKE 'version';\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysql -e \"SHOW VARIABLES LIKE 'version';\"",
+    "example": "mysql -e \"SHOW VARIABLES LIKE 'version';\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-single-transaction-u-user-p-db-backup-sql",
+    "command": "mysqldump --single-transaction -u user -p db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --single-transaction -u user -p db > backup.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqldump --single-transaction -u user -p db > backup.sql",
+    "example": "mysqldump --single-transaction -u user -p db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-all-databases-u-root-p-all-sql",
+    "command": "mysqldump --all-databases -u root -p > all.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --all-databases -u root -p > all.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqldump --all-databases -u root -p > all.sql",
+    "example": "mysqldump --all-databases -u root -p > all.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-routines-triggers-u-user-p-db-backup-sql",
+    "command": "mysqldump --routines --triggers -u user -p db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --routines --triggers -u user -p db > backup.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqldump --routines --triggers -u user -p db > backup.sql",
+    "example": "mysqldump --routines --triggers -u user -p db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqlcheck-u-user-p-all-databases",
+    "command": "mysqlcheck -u user -p --all-databases",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqlcheck -u user -p --all-databases` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqlcheck -u user -p --all-databases",
+    "example": "mysqlcheck -u user -p --all-databases",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqlcheck",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqladmin-u-root-p-processlist",
+    "command": "mysqladmin -u root -p processlist",
+    "title": "PostgreSQL / MySQL Backup details/status dekhna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqladmin -u root -p processlist` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqladmin -u root -p processlist",
+    "example": "mysqladmin -u root -p processlist",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqladmin",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqladmin-u-root-p-variables",
+    "command": "mysqladmin -u root -p variables",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqladmin -u root -p variables` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mysqladmin -u root -p variables",
+    "example": "mysqladmin -u root -p variables",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqladmin",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mariadb-dump-u-user-p-db-backup-sql",
+    "command": "mariadb-dump -u user -p db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mariadb-dump -u user -p db > backup.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mariadb-dump -u user -p db > backup.sql",
+    "example": "mariadb-dump -u user -p db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mariadb-dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-gzip-backup-sql",
+    "command": "gzip backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `gzip backup.sql` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gzip backup.sql",
+    "example": "gzip backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "gzip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-gunzip-backup-sql-gz",
+    "command": "gunzip backup.sql.gz",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `gunzip backup.sql.gz` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gunzip backup.sql.gz",
+    "example": "gunzip backup.sql.gz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "gunzip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-zcat-backup-sql-gz-mysql-u-user-p-db",
+    "command": "zcat backup.sql.gz | mysql -u user -p db",
+    "title": "PostgreSQL / MySQL Backup backup/export lena",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `zcat backup.sql.gz | mysql -u user -p db` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "zcat backup.sql.gz | mysql -u user -p db",
+    "example": "zcat backup.sql.gz | mysql -u user -p db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "zcat",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/docker.commands.json b/public/data/docker.commands.json
index 6bd806b..b609704 100644
--- a/public/data/docker.commands.json
+++ b/public/data/docker.commands.json
@@ -427,5 +427,1127 @@
       "cleanup",
       "danger"
     ]
+  },
+  {
+    "id": "docker-docker-login",
+    "command": "docker login",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker login` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker login",
+    "example": "docker login",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-logout",
+    "command": "docker logout",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker logout` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker logout",
+    "example": "docker logout",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-tag-image-repo-image-tag",
+    "command": "docker tag image repo/image:tag",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker tag image repo/image:tag` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker tag image repo/image:tag",
+    "example": "docker tag image repo/image:tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-push-repo-image-tag",
+    "command": "docker push repo/image:tag",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker push repo/image:tag` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker push repo/image:tag",
+    "example": "docker push repo/image:tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-history-image",
+    "command": "docker history image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker history image` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker history image",
+    "example": "docker history image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-save-image-image-tar",
+    "command": "docker save image > image.tar",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker save image > image.tar` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker save image > image.tar",
+    "example": "docker save image > image.tar",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-load-image-tar",
+    "command": "docker load < image.tar",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker load < image.tar` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker load < image.tar",
+    "example": "docker load < image.tar",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-export-container-container-tar",
+    "command": "docker export container > container.tar",
+    "title": "Docker backup/export lena",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker export container > container.tar` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker export container > container.tar",
+    "example": "docker export container > container.tar",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-import-container-tar-image-tag",
+    "command": "docker import container.tar image:tag",
+    "title": "Docker restore/import karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker import container.tar image:tag` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker import container.tar image:tag",
+    "example": "docker import container.tar image:tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-cp-container-path-file-file",
+    "command": "docker cp container:/path/file ./file",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker cp container:/path/file ./file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker cp container:/path/file ./file",
+    "example": "docker cp container:/path/file ./file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-cp-file-container-path-file",
+    "command": "docker cp ./file container:/path/file",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker cp ./file container:/path/file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker cp ./file container:/path/file",
+    "example": "docker cp ./file container:/path/file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-stats",
+    "command": "docker stats",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker stats` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker stats",
+    "example": "docker stats",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-top-container",
+    "command": "docker top container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker top container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker top container",
+    "example": "docker top container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-port-container",
+    "command": "docker port container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Beginner",
+    "use_case": "Docker me `docker port container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker port container",
+    "example": "docker port container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-events",
+    "command": "docker events",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker events` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker events",
+    "example": "docker events",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-wait-container",
+    "command": "docker wait container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker wait container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker wait container",
+    "example": "docker wait container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-rename-old-new",
+    "command": "docker rename old new",
+    "title": "Docker resource create karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker rename old new` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker rename old new",
+    "example": "docker rename old new",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-update-restart-unless-stopped-container",
+    "command": "docker update --restart unless-stopped container",
+    "title": "Docker restart/reload karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker update --restart unless-stopped container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker update --restart unless-stopped container",
+    "example": "docker update --restart unless-stopped container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-commit-container-image-tag",
+    "command": "docker commit container image:tag",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker commit container image:tag` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker commit container image:tag",
+    "example": "docker commit container image:tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-create-name-app-image",
+    "command": "docker create --name app image",
+    "title": "Docker resource create karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker create --name app image` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker create --name app image",
+    "example": "docker create --name app image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-start-container",
+    "command": "docker start container",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker start container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker start container",
+    "example": "docker start container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-pause-container",
+    "command": "docker pause container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker pause container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker pause container",
+    "example": "docker pause container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-unpause-container",
+    "command": "docker unpause container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker unpause container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker unpause container",
+    "example": "docker unpause container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-container-prune",
+    "command": "docker container prune",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker container prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker container prune",
+    "example": "docker container prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-image-prune",
+    "command": "docker image prune",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker image prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker image prune",
+    "example": "docker image prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-image-prune-a",
+    "command": "docker image prune -a",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker image prune -a` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker image prune -a",
+    "example": "docker image prune -a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-volume-prune",
+    "command": "docker volume prune",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker volume prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker volume prune",
+    "example": "docker volume prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-prune",
+    "command": "docker network prune",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker network prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker network prune",
+    "example": "docker network prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-builder-prune",
+    "command": "docker builder prune",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker builder prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker builder prune",
+    "example": "docker builder prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-system-df",
+    "command": "docker system df",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker system df` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker system df",
+    "example": "docker system df",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-ps",
+    "command": "docker compose ps",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose ps` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose ps",
+    "example": "docker compose ps",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-logs-f",
+    "command": "docker compose logs -f",
+    "title": "Docker logs/debugging dekhna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose logs -f` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose logs -f",
+    "example": "docker compose logs -f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-logs-service",
+    "command": "docker compose logs service",
+    "title": "Docker logs/debugging dekhna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose logs service` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose logs service",
+    "example": "docker compose logs service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-exec-app-sh",
+    "command": "docker compose exec app sh",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose exec app sh` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose exec app sh",
+    "example": "docker compose exec app sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-run-rm-app-command",
+    "command": "docker compose run --rm app command",
+    "title": "Docker apply/deploy/run karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose run --rm app command` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose run --rm app command",
+    "example": "docker compose run --rm app command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-pull",
+    "command": "docker compose pull",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose pull` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose pull",
+    "example": "docker compose pull",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-build",
+    "command": "docker compose build",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose build` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose build",
+    "example": "docker compose build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-restart-service",
+    "command": "docker compose restart service",
+    "title": "Docker restart/reload karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose restart service` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose restart service",
+    "example": "docker compose restart service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-stop",
+    "command": "docker compose stop",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose stop` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose stop",
+    "example": "docker compose stop",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-rm",
+    "command": "docker compose rm",
+    "title": "Docker delete/destroy action chalana",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose rm` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose rm",
+    "example": "docker compose rm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-config",
+    "command": "docker compose config",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose config` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose config",
+    "example": "docker compose config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-down-v",
+    "command": "docker compose down -v",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose down -v` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose down -v",
+    "example": "docker compose down -v",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-create-app-net",
+    "command": "docker network create app-net",
+    "title": "Docker resource create karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker network create app-net` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker network create app-net",
+    "example": "docker network create app-net",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-connect-app-net-container",
+    "command": "docker network connect app-net container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker network connect app-net container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker network connect app-net container",
+    "example": "docker network connect app-net container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-disconnect-app-net-container",
+    "command": "docker network disconnect app-net container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker network disconnect app-net container` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker network disconnect app-net container",
+    "example": "docker network disconnect app-net container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-volume-create-app-data",
+    "command": "docker volume create app-data",
+    "title": "Docker resource create karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker volume create app-data` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker volume create app-data",
+    "example": "docker volume create app-data",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-volume-rm-app-data",
+    "command": "docker volume rm app-data",
+    "title": "Docker delete/destroy action chalana",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker volume rm app-data` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker volume rm app-data",
+    "example": "docker volume rm app-data",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-context-ls",
+    "command": "docker context ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker context ls` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker context ls",
+    "example": "docker context ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-context-use-default",
+    "command": "docker context use default",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker context use default` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker context use default",
+    "example": "docker context use default",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-buildx-ls",
+    "command": "docker buildx ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker buildx ls` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker buildx ls",
+    "example": "docker buildx ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-buildx-build-platform-linux-amd64-t-app",
+    "command": "docker buildx build --platform linux/amd64 -t app .",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker buildx build --platform linux/amd64 -t app .` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker buildx build --platform linux/amd64 -t app .",
+    "example": "docker buildx build --platform linux/amd64 -t app .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/firewall.commands.json b/public/data/firewall.commands.json
index 69ac1a1..fa5feb6 100644
--- a/public/data/firewall.commands.json
+++ b/public/data/firewall.commands.json
@@ -268,5 +268,404 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "firewall-sudo-ufw-default-deny-incoming",
+    "command": "sudo ufw default deny incoming",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw default deny incoming` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw default deny incoming",
+    "example": "sudo ufw default deny incoming",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-default-allow-outgoing",
+    "command": "sudo ufw default allow outgoing",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw default allow outgoing` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw default allow outgoing",
+    "example": "sudo ufw default allow outgoing",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-allow-from-1-2-3-4-to-any-port-22",
+    "command": "sudo ufw allow from 1.2.3.4 to any port 22",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw allow from 1.2.3.4 to any port 22` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw allow from 1.2.3.4 to any port 22",
+    "example": "sudo ufw allow from 1.2.3.4 to any port 22",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-allow-5432-tcp",
+    "command": "sudo ufw allow 5432/tcp",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw allow 5432/tcp` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw allow 5432/tcp",
+    "example": "sudo ufw allow 5432/tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-deny-from-1-2-3-4",
+    "command": "sudo ufw deny from 1.2.3.4",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw deny from 1.2.3.4` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw deny from 1.2.3.4",
+    "example": "sudo ufw deny from 1.2.3.4",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-delete-allow-80-tcp",
+    "command": "sudo ufw delete allow 80/tcp",
+    "title": "Firewall delete/destroy action chalana",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw delete allow 80/tcp` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw delete allow 80/tcp",
+    "example": "sudo ufw delete allow 80/tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-app-list",
+    "command": "sudo ufw app list",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw app list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw app list",
+    "example": "sudo ufw app list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-app-info-openssh",
+    "command": "sudo ufw app info OpenSSH",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw app info OpenSSH` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw app info OpenSSH",
+    "example": "sudo ufw app info OpenSSH",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-p-tcp-dport-80-j-accept",
+    "command": "sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT",
+    "example": "sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-p-tcp-dport-22-j-accept",
+    "command": "sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT",
+    "example": "sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-s-1-2-3-4-j-drop",
+    "command": "sudo iptables -A INPUT -s 1.2.3.4 -j DROP",
+    "title": "Firewall delete/destroy action chalana",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -s 1.2.3.4 -j DROP` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables -A INPUT -s 1.2.3.4 -j DROP",
+    "example": "sudo iptables -A INPUT -s 1.2.3.4 -j DROP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-d-input-1",
+    "command": "sudo iptables -D INPUT 1",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -D INPUT 1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables -D INPUT 1",
+    "example": "sudo iptables -D INPUT 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-save",
+    "command": "sudo iptables-save",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables-save` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables-save",
+    "example": "sudo iptables-save",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-restore-rules-v4",
+    "command": "sudo iptables-restore < rules.v4",
+    "title": "Firewall restore/import karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables-restore < rules.v4` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables-restore < rules.v4",
+    "example": "sudo iptables-restore < rules.v4",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-list-tables",
+    "command": "sudo nft list tables",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo nft list tables` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nft list tables",
+    "example": "sudo nft list tables",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-list-ruleset",
+    "command": "sudo nft list ruleset",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo nft list ruleset` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nft list ruleset",
+    "example": "sudo nft list ruleset",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-get-active-zones",
+    "command": "sudo firewall-cmd --get-active-zones",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo firewall-cmd --get-active-zones` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo firewall-cmd --get-active-zones",
+    "example": "sudo firewall-cmd --get-active-zones",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-list-services",
+    "command": "sudo firewall-cmd --list-services",
+    "title": "Firewall details/status dekhna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo firewall-cmd --list-services` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo firewall-cmd --list-services",
+    "example": "sudo firewall-cmd --list-services",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-add-port-8080-tcp-permanent",
+    "command": "sudo firewall-cmd --add-port=8080/tcp --permanent",
+    "title": "Firewall resource create karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo firewall-cmd --add-port=8080/tcp --permanent` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo firewall-cmd --add-port=8080/tcp --permanent",
+    "example": "sudo firewall-cmd --add-port=8080/tcp --permanent",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-remove-port-8080-tcp-permanent",
+    "command": "sudo firewall-cmd --remove-port=8080/tcp --permanent",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo firewall-cmd --remove-port=8080/tcp --permanent` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo firewall-cmd --remove-port=8080/tcp --permanent",
+    "example": "sudo firewall-cmd --remove-port=8080/tcp --permanent",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-zone-public-add-source-1-2-3-4-permanent",
+    "command": "sudo firewall-cmd --zone=public --add-source=1.2.3.4 --permanent",
+    "title": "Firewall resource create karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --zone=public --add-source=1.2.3.4 --permanent` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo firewall-cmd --zone=public --add-source=1.2.3.4 --permanent",
+    "example": "sudo firewall-cmd --zone=public --add-source=1.2.3.4 --permanent",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/gcp.commands.json b/public/data/gcp.commands.json
index 9d08327..aa038a5 100644
--- a/public/data/gcp.commands.json
+++ b/public/data/gcp.commands.json
@@ -286,5 +286,731 @@
       "gcloud",
       "devops"
     ]
+  },
+  {
+    "id": "gcp-cli-gcloud-config-set-compute-region-asia-south1",
+    "command": "gcloud config set compute/region asia-south1",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud config set compute/region asia-south1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud config set compute/region asia-south1",
+    "example": "gcloud config set compute/region asia-south1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-config-set-compute-zone-asia-south1-a",
+    "command": "gcloud config set compute/zone asia-south1-a",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud config set compute/zone asia-south1-a` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud config set compute/zone asia-south1-a",
+    "example": "gcloud config set compute/zone asia-south1-a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-services-list",
+    "command": "gcloud services list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud services list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud services list",
+    "example": "gcloud services list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-services-enable-run-googleapis-com",
+    "command": "gcloud services enable run.googleapis.com",
+    "title": "GCP CLI apply/deploy/run karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud services enable run.googleapis.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud services enable run.googleapis.com",
+    "example": "gcloud services enable run.googleapis.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-describe-instance-zone-zone",
+    "command": "gcloud compute instances describe instance --zone zone",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute instances describe instance --zone zone` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute instances describe instance --zone zone",
+    "example": "gcloud compute instances describe instance --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-start-instance-zone-zone",
+    "command": "gcloud compute instances start instance --zone zone",
+    "title": "GCP CLI apply/deploy/run karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute instances start instance --zone zone` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute instances start instance --zone zone",
+    "example": "gcloud compute instances start instance --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-stop-instance-zone-zone",
+    "command": "gcloud compute instances stop instance --zone zone",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute instances stop instance --zone zone` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute instances stop instance --zone zone",
+    "example": "gcloud compute instances stop instance --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-disks-list",
+    "command": "gcloud compute disks list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute disks list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute disks list",
+    "example": "gcloud compute disks list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-snapshots-list",
+    "command": "gcloud compute snapshots list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute snapshots list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute snapshots list",
+    "example": "gcloud compute snapshots list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-firewall-rules-list",
+    "command": "gcloud compute firewall-rules list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute firewall-rules list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute firewall-rules list",
+    "example": "gcloud compute firewall-rules list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-firewall-rules-create-allow-http-allow-tcp-80",
+    "command": "gcloud compute firewall-rules create allow-http --allow tcp:80",
+    "title": "GCP CLI resource create karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud compute firewall-rules create allow-http --allow tcp:80` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud compute firewall-rules create allow-http --allow tcp:80",
+    "example": "gcloud compute firewall-rules create allow-http --allow tcp:80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-container-clusters-list",
+    "command": "gcloud container clusters list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud container clusters list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud container clusters list",
+    "example": "gcloud container clusters list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-container-clusters-get-credentials-cluster-zone-zone",
+    "command": "gcloud container clusters get-credentials cluster --zone zone",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud container clusters get-credentials cluster --zone zone` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud container clusters get-credentials cluster --zone zone",
+    "example": "gcloud container clusters get-credentials cluster --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-artifacts-docker-images-list-repo",
+    "command": "gcloud artifacts docker images list repo",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud artifacts docker images list repo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud artifacts docker images list repo",
+    "example": "gcloud artifacts docker images list repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-services-describe-service-region-region",
+    "command": "gcloud run services describe service --region region",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud run services describe service --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud run services describe service --region region",
+    "example": "gcloud run services describe service --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-services-logs-read-service-region-region",
+    "command": "gcloud run services logs read service --region region",
+    "title": "GCP CLI logs/debugging dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud run services logs read service --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud run services logs read service --region region",
+    "example": "gcloud run services logs read service --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-services-update-service-set-env-vars-key-value-region-region",
+    "command": "gcloud run services update service --set-env-vars KEY=value --region region",
+    "title": "GCP CLI apply/deploy/run karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud run services update service --set-env-vars KEY=value --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud run services update service --set-env-vars KEY=value --region region",
+    "example": "gcloud run services update service --set-env-vars KEY=value --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-jobs-describe-job-region-region",
+    "command": "gcloud run jobs describe job --region region",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud run jobs describe job --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud run jobs describe job --region region",
+    "example": "gcloud run jobs describe job --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-jobs-update-job-image-image-region-region",
+    "command": "gcloud run jobs update job --image image --region region",
+    "title": "GCP CLI apply/deploy/run karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud run jobs update job --image image --region region` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud run jobs update job --image image --region region",
+    "example": "gcloud run jobs update job --image image --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-builds-list",
+    "command": "gcloud builds list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud builds list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud builds list",
+    "example": "gcloud builds list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-builds-log-build-id",
+    "command": "gcloud builds log BUILD_ID",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud builds log BUILD_ID` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud builds log BUILD_ID",
+    "example": "gcloud builds log BUILD_ID",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-instances-list",
+    "command": "gcloud sql instances list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql instances list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud sql instances list",
+    "example": "gcloud sql instances list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-databases-list-instance-instance",
+    "command": "gcloud sql databases list --instance instance",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql databases list --instance instance` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud sql databases list --instance instance",
+    "example": "gcloud sql databases list --instance instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-users-list-instance-instance",
+    "command": "gcloud sql users list --instance instance",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql users list --instance instance` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud sql users list --instance instance",
+    "example": "gcloud sql users list --instance instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-buckets-list",
+    "command": "gcloud storage buckets list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage buckets list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud storage buckets list",
+    "example": "gcloud storage buckets list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-cp-file-gs-bucket-path",
+    "command": "gcloud storage cp file gs://bucket/path",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage cp file gs://bucket/path` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud storage cp file gs://bucket/path",
+    "example": "gcloud storage cp file gs://bucket/path",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-rsync-dist-gs-bucket",
+    "command": "gcloud storage rsync ./dist gs://bucket",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage rsync ./dist gs://bucket` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud storage rsync ./dist gs://bucket",
+    "example": "gcloud storage rsync ./dist gs://bucket",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-iam-service-accounts-list",
+    "command": "gcloud iam service-accounts list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud iam service-accounts list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud iam service-accounts list",
+    "example": "gcloud iam service-accounts list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-iam-service-accounts-keys-create-key-json-iam-account-sa",
+    "command": "gcloud iam service-accounts keys create key.json --iam-account sa",
+    "title": "GCP CLI resource create karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud iam service-accounts keys create key.json --iam-account sa` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud iam service-accounts keys create key.json --iam-account sa",
+    "example": "gcloud iam service-accounts keys create key.json --iam-account sa",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-secrets-create-name-replication-policy-automatic",
+    "command": "gcloud secrets create name --replication-policy automatic",
+    "title": "GCP CLI resource create karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud secrets create name --replication-policy automatic` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud secrets create name --replication-policy automatic",
+    "example": "gcloud secrets create name --replication-policy automatic",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-secrets-versions-add-name-data-file-file",
+    "command": "gcloud secrets versions add name --data-file file",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud secrets versions add name --data-file file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud secrets versions add name --data-file file",
+    "example": "gcloud secrets versions add name --data-file file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-logging-read-severity-error-limit-50",
+    "command": "gcloud logging read \"severity>=ERROR\" --limit 50",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud logging read \"severity>=ERROR\" --limit 50` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud logging read \"severity>=ERROR\" --limit 50",
+    "example": "gcloud logging read \"severity>=ERROR\" --limit 50",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-monitoring-policies-list",
+    "command": "gcloud monitoring policies list",
+    "title": "GCP CLI details/status dekhna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud monitoring policies list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gcloud monitoring policies list",
+    "example": "gcloud monitoring policies list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/git.commands.json b/public/data/git.commands.json
index 826f08b..27d0495 100644
--- a/public/data/git.commands.json
+++ b/public/data/git.commands.json
@@ -380,5 +380,997 @@
       "pr",
       "status"
     ]
+  },
+  {
+    "id": "git-github-git-init",
+    "command": "git init",
+    "title": "Git / GitHub resource create karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git init` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git init",
+    "example": "git init",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-clone-repo-url",
+    "command": "git clone <repo-url>",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git clone <repo-url>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git clone <repo-url>",
+    "example": "git clone <repo-url>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-clone-depth-1-repo-url",
+    "command": "git clone --depth 1 <repo-url>",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git clone --depth 1 <repo-url>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git clone --depth 1 <repo-url>",
+    "example": "git clone --depth 1 <repo-url>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-global-user-name-name",
+    "command": "git config --global user.name \"Name\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git config --global user.name \"Name\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git config --global user.name \"Name\"",
+    "example": "git config --global user.name \"Name\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-global-user-email-email-example-com",
+    "command": "git config --global user.email \"email@example.com\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git config --global user.email \"email@example.com\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git config --global user.email \"email@example.com\"",
+    "example": "git config --global user.email \"email@example.com\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-list",
+    "command": "git config --list",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git config --list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git config --list",
+    "example": "git config --list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-restore-file",
+    "command": "git restore file",
+    "title": "Git / GitHub restore/import karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git restore file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git restore file",
+    "example": "git restore file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-restore-staged-file",
+    "command": "git restore --staged file",
+    "title": "Git / GitHub restore/import karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git restore --staged file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git restore --staged file",
+    "example": "git restore --staged file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-reset-head-file",
+    "command": "git reset HEAD file",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git reset HEAD file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git reset HEAD file",
+    "example": "git reset HEAD file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-reset-hard-head",
+    "command": "git reset --hard HEAD",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git reset --hard HEAD` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git reset --hard HEAD",
+    "example": "git reset --hard HEAD",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-revert-commit",
+    "command": "git revert <commit>",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git revert <commit>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git revert <commit>",
+    "example": "git revert <commit>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-cherry-pick-commit",
+    "command": "git cherry-pick <commit>",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git cherry-pick <commit>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git cherry-pick <commit>",
+    "example": "git cherry-pick <commit>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-merge-branch",
+    "command": "git merge branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git merge branch` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git merge branch",
+    "example": "git merge branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-merge-abort",
+    "command": "git merge --abort",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git merge --abort` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git merge --abort",
+    "example": "git merge --abort",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-rebase-main",
+    "command": "git rebase main",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git rebase main` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git rebase main",
+    "example": "git rebase main",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-rebase-abort",
+    "command": "git rebase --abort",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Beginner",
+    "use_case": "Git / GitHub me `git rebase --abort` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git rebase --abort",
+    "example": "git rebase --abort",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-rebase-continue",
+    "command": "git rebase --continue",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git rebase --continue` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git rebase --continue",
+    "example": "git rebase --continue",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-fetch-prune",
+    "command": "git fetch --prune",
+    "title": "Git / GitHub apply/deploy/run karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git fetch --prune` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git fetch --prune",
+    "example": "git fetch --prune",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-pull-rebase-origin-main",
+    "command": "git pull --rebase origin main",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git pull --rebase origin main` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git pull --rebase origin main",
+    "example": "git pull --rebase origin main",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-push-u-origin-branch",
+    "command": "git push -u origin branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git push -u origin branch` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git push -u origin branch",
+    "example": "git push -u origin branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-push-force-with-lease",
+    "command": "git push --force-with-lease",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git push --force-with-lease` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git push --force-with-lease",
+    "example": "git push --force-with-lease",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-tag",
+    "command": "git tag",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git tag` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git tag",
+    "example": "git tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-tag-v1-0-0",
+    "command": "git tag v1.0.0",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git tag v1.0.0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git tag v1.0.0",
+    "example": "git tag v1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-tag-d-v1-0-0",
+    "command": "git tag -d v1.0.0",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git tag -d v1.0.0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git tag -d v1.0.0",
+    "example": "git tag -d v1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-push-origin-delete-branch",
+    "command": "git push origin --delete branch",
+    "title": "Git / GitHub delete/destroy action chalana",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git push origin --delete branch` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git push origin --delete branch",
+    "example": "git push origin --delete branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-clean-fd",
+    "command": "git clean -fd",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git clean -fd` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git clean -fd",
+    "example": "git clean -fd",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-clean-fdn",
+    "command": "git clean -fdn",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git clean -fdn` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git clean -fdn",
+    "example": "git clean -fdn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-blame-file",
+    "command": "git blame file",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git blame file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git blame file",
+    "example": "git blame file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-show-commit",
+    "command": "git show <commit>",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git show <commit>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git show <commit>",
+    "example": "git show <commit>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-reflog",
+    "command": "git reflog",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git reflog` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git reflog",
+    "example": "git reflog",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-bisect-start",
+    "command": "git bisect start",
+    "title": "Git / GitHub apply/deploy/run karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git bisect start` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git bisect start",
+    "example": "git bisect start",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-shortlog-sn",
+    "command": "git shortlog -sn",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git shortlog -sn` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git shortlog -sn",
+    "example": "git shortlog -sn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-archive-format-zip-head-o-source-zip",
+    "command": "git archive --format=zip HEAD -o source.zip",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git archive --format=zip HEAD -o source.zip` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "git archive --format=zip HEAD -o source.zip",
+    "example": "git archive --format=zip HEAD -o source.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-clone-owner-repo",
+    "command": "gh repo clone owner/repo",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh repo clone owner/repo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh repo clone owner/repo",
+    "example": "gh repo clone owner/repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-view",
+    "command": "gh repo view",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh repo view` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh repo view",
+    "example": "gh repo view",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-issue-list",
+    "command": "gh issue list",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh issue list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh issue list",
+    "example": "gh issue list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-issue-view-1",
+    "command": "gh issue view 1",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh issue view 1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh issue view 1",
+    "example": "gh issue view 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-list",
+    "command": "gh pr list",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh pr list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh pr list",
+    "example": "gh pr list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-view-1",
+    "command": "gh pr view 1",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh pr view 1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh pr view 1",
+    "example": "gh pr view 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-checkout-1",
+    "command": "gh pr checkout 1",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh pr checkout 1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh pr checkout 1",
+    "example": "gh pr checkout 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-merge-1-squash",
+    "command": "gh pr merge 1 --squash",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh pr merge 1 --squash` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh pr merge 1 --squash",
+    "example": "gh pr merge 1 --squash",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-release-list",
+    "command": "gh release list",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh release list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh release list",
+    "example": "gh release list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-release-upload-v1-0-0-file-zip",
+    "command": "gh release upload v1.0.0 file.zip",
+    "title": "Git / GitHub apply/deploy/run karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh release upload v1.0.0 file.zip` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh release upload v1.0.0 file.zip",
+    "example": "gh release upload v1.0.0 file.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-secret-set-secret-name",
+    "command": "gh secret set SECRET_NAME",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh secret set SECRET_NAME` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh secret set SECRET_NAME",
+    "example": "gh secret set SECRET_NAME",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-secret-list",
+    "command": "gh secret list",
+    "title": "Git / GitHub details/status dekhna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh secret list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh secret list",
+    "example": "gh secret list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-workflow-enable-workflow-yml",
+    "command": "gh workflow enable workflow.yml",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh workflow enable workflow.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh workflow enable workflow.yml",
+    "example": "gh workflow enable workflow.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-workflow-disable-workflow-yml",
+    "command": "gh workflow disable workflow.yml",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `gh workflow disable workflow.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh workflow disable workflow.yml",
+    "example": "gh workflow disable workflow.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/github-actions.commands.json b/public/data/github-actions.commands.json
index b804ce0..7d473a8 100644
--- a/public/data/github-actions.commands.json
+++ b/public/data/github-actions.commands.json
@@ -250,5 +250,404 @@
       "gh",
       "devops"
     ]
+  },
+  {
+    "id": "github-actions-gh-run-list-limit-20",
+    "command": "gh run list --limit 20",
+    "title": "GitHub Actions details/status dekhna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh run list --limit 20` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh run list --limit 20",
+    "example": "gh run list --limit 20",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-run-view-log",
+    "command": "gh run view --log",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh run view --log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh run view --log",
+    "example": "gh run view --log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-run-view-log-failed",
+    "command": "gh run view --log-failed",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh run view --log-failed` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh run view --log-failed",
+    "example": "gh run view --log-failed",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-run-download-run-id",
+    "command": "gh run download <run-id>",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh run download <run-id>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh run download <run-id>",
+    "example": "gh run download <run-id>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-run-cancel-run-id",
+    "command": "gh run cancel <run-id>",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh run cancel <run-id>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh run cancel <run-id>",
+    "example": "gh run cancel <run-id>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-workflow-view-workflow-yml",
+    "command": "gh workflow view workflow.yml",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh workflow view workflow.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh workflow view workflow.yml",
+    "example": "gh workflow view workflow.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-workflow-run-workflow-yml-f-key-value",
+    "command": "gh workflow run workflow.yml -f key=value",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh workflow run workflow.yml -f key=value` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh workflow run workflow.yml -f key=value",
+    "example": "gh workflow run workflow.yml -f key=value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-cache-list",
+    "command": "gh cache list",
+    "title": "GitHub Actions details/status dekhna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh cache list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh cache list",
+    "example": "gh cache list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-cache-delete-cache-id",
+    "command": "gh cache delete <cache-id>",
+    "title": "GitHub Actions delete/destroy action chalana",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh cache delete <cache-id>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh cache delete <cache-id>",
+    "example": "gh cache delete <cache-id>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-api-repos-owner-repo-actions-secrets",
+    "command": "gh api repos/:owner/:repo/actions/secrets",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh api repos/:owner/:repo/actions/secrets` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh api repos/:owner/:repo/actions/secrets",
+    "example": "gh api repos/:owner/:repo/actions/secrets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-api-repos-owner-repo-actions-variables",
+    "command": "gh api repos/:owner/:repo/actions/variables",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh api repos/:owner/:repo/actions/variables` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh api repos/:owner/:repo/actions/variables",
+    "example": "gh api repos/:owner/:repo/actions/variables",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-secret-set-secret-body-value",
+    "command": "gh secret set SECRET --body value",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh secret set SECRET --body value` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh secret set SECRET --body value",
+    "example": "gh secret set SECRET --body value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-secret-delete-secret",
+    "command": "gh secret delete SECRET",
+    "title": "GitHub Actions delete/destroy action chalana",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh secret delete SECRET` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh secret delete SECRET",
+    "example": "gh secret delete SECRET",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-variable-set-env-body-production",
+    "command": "gh variable set ENV --body production",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh variable set ENV --body production` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh variable set ENV --body production",
+    "example": "gh variable set ENV --body production",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-variable-delete-env",
+    "command": "gh variable delete ENV",
+    "title": "GitHub Actions delete/destroy action chalana",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh variable delete ENV` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh variable delete ENV",
+    "example": "gh variable delete ENV",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-release-view-v1-0-0",
+    "command": "gh release view v1.0.0",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh release view v1.0.0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh release view v1.0.0",
+    "example": "gh release view v1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-release-download-v1-0-0",
+    "command": "gh release download v1.0.0",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh release download v1.0.0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh release download v1.0.0",
+    "example": "gh release download v1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-release-delete-v1-0-0",
+    "command": "gh release delete v1.0.0",
+    "title": "GitHub Actions delete/destroy action chalana",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh release delete v1.0.0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh release delete v1.0.0",
+    "example": "gh release delete v1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-api-repos-owner-repo-actions-runners",
+    "command": "gh api repos/:owner/:repo/actions/runners",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh api repos/:owner/:repo/actions/runners` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh api repos/:owner/:repo/actions/runners",
+    "example": "gh api repos/:owner/:repo/actions/runners",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-api-repos-owner-repo-actions-workflows",
+    "command": "gh api repos/:owner/:repo/actions/workflows",
+    "title": "GitHub Actions command use karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh api repos/:owner/:repo/actions/workflows` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh api repos/:owner/:repo/actions/workflows",
+    "example": "gh api repos/:owner/:repo/actions/workflows",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "github-actions-gh-api-repos-owner-repo-actions-runs-paginate",
+    "command": "gh api repos/:owner/:repo/actions/runs --paginate",
+    "title": "GitHub Actions apply/deploy/run karna",
+    "category": "GitHub Actions",
+    "difficulty": "Beginner",
+    "use_case": "GitHub Actions me `gh api repos/:owner/:repo/actions/runs --paginate` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gh api repos/:owner/:repo/actions/runs --paginate",
+    "example": "gh api repos/:owner/:repo/actions/runs --paginate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "github",
+      "gh",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/helm.commands.json b/public/data/helm.commands.json
index 87e9eb8..77eed24 100644
--- a/public/data/helm.commands.json
+++ b/public/data/helm.commands.json
@@ -268,5 +268,467 @@
       "helm",
       "devops"
     ]
+  },
+  {
+    "id": "helm-helm-create-mychart",
+    "command": "helm create mychart",
+    "title": "Helm resource create karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm create mychart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm create mychart",
+    "example": "helm create mychart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-dependency-update",
+    "command": "helm dependency update",
+    "title": "Helm apply/deploy/run karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm dependency update` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm dependency update",
+    "example": "helm dependency update",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-dependency-build",
+    "command": "helm dependency build",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm dependency build` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm dependency build",
+    "example": "helm dependency build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-package-mychart",
+    "command": "helm package mychart",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm package mychart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm package mychart",
+    "example": "helm package mychart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-repo-remove-bitnami",
+    "command": "helm repo remove bitnami",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm repo remove bitnami` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm repo remove bitnami",
+    "example": "helm repo remove bitnami",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-search-hub-nginx",
+    "command": "helm search hub nginx",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm search hub nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm search hub nginx",
+    "example": "helm search hub nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-install-release-chart-values-values-yaml",
+    "command": "helm install release chart --values values.yaml",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm install release chart --values values.yaml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm install release chart --values values.yaml",
+    "example": "helm install release chart --values values.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-upgrade-release-chart-values-values-yaml",
+    "command": "helm upgrade release chart --values values.yaml",
+    "title": "Helm apply/deploy/run karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm upgrade release chart --values values.yaml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm upgrade release chart --values values.yaml",
+    "example": "helm upgrade release chart --values values.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-upgrade-release-chart-set-image-tag-v1",
+    "command": "helm upgrade release chart --set image.tag=v1",
+    "title": "Helm apply/deploy/run karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm upgrade release chart --set image.tag=v1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm upgrade release chart --set image.tag=v1",
+    "example": "helm upgrade release chart --set image.tag=v1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-rollback-release-2",
+    "command": "helm rollback release 2",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm rollback release 2` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm rollback release 2",
+    "example": "helm rollback release 2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-history-release-n-ns",
+    "command": "helm history release -n ns",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm history release -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm history release -n ns",
+    "example": "helm history release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-get-manifest-release-n-ns",
+    "command": "helm get manifest release -n ns",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm get manifest release -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm get manifest release -n ns",
+    "example": "helm get manifest release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-get-all-release-n-ns",
+    "command": "helm get all release -n ns",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm get all release -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm get all release -n ns",
+    "example": "helm get all release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-get-hooks-release-n-ns",
+    "command": "helm get hooks release -n ns",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm get hooks release -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm get hooks release -n ns",
+    "example": "helm get hooks release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-show-values-chart",
+    "command": "helm show values chart",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm show values chart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm show values chart",
+    "example": "helm show values chart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-show-chart-chart",
+    "command": "helm show chart chart",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm show chart chart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm show chart chart",
+    "example": "helm show chart chart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-show-readme-chart",
+    "command": "helm show readme chart",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm show readme chart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm show readme chart",
+    "example": "helm show readme chart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-plugin-list",
+    "command": "helm plugin list",
+    "title": "Helm details/status dekhna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm plugin list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm plugin list",
+    "example": "helm plugin list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-env",
+    "command": "helm env",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm env` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm env",
+    "example": "helm env",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-pull-chart-untar",
+    "command": "helm pull chart --untar",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm pull chart --untar` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm pull chart --untar",
+    "example": "helm pull chart --untar",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-test-release-n-ns",
+    "command": "helm test release -n ns",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm test release -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "helm test release -n ns",
+    "example": "helm test release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/jenkins.commands.json b/public/data/jenkins.commands.json
index e486eba..c67d1d9 100644
--- a/public/data/jenkins.commands.json
+++ b/public/data/jenkins.commands.json
@@ -178,5 +178,347 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "jenkins-sudo-systemctl-stop-jenkins",
+    "command": "sudo systemctl stop jenkins",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo systemctl stop jenkins` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl stop jenkins",
+    "example": "sudo systemctl stop jenkins",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-systemctl-start-jenkins",
+    "command": "sudo systemctl start jenkins",
+    "title": "Jenkins apply/deploy/run karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo systemctl start jenkins` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl start jenkins",
+    "example": "sudo systemctl start jenkins",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-tail-f-var-log-jenkins-jenkins-log",
+    "command": "sudo tail -f /var/log/jenkins/jenkins.log",
+    "title": "Jenkins logs/debugging dekhna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo tail -f /var/log/jenkins/jenkins.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo tail -f /var/log/jenkins/jenkins.log",
+    "example": "sudo tail -f /var/log/jenkins/jenkins.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-ls-lah-var-lib-jenkins-jobs",
+    "command": "sudo ls -lah /var/lib/jenkins/jobs",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo ls -lah /var/lib/jenkins/jobs` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ls -lah /var/lib/jenkins/jobs",
+    "example": "sudo ls -lah /var/lib/jenkins/jobs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-ls-lah-var-lib-jenkins-workspace",
+    "command": "sudo ls -lah /var/lib/jenkins/workspace",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo ls -lah /var/lib/jenkins/workspace` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ls -lah /var/lib/jenkins/workspace",
+    "example": "sudo ls -lah /var/lib/jenkins/workspace",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-du-sh-var-lib-jenkins",
+    "command": "sudo du -sh /var/lib/jenkins",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo du -sh /var/lib/jenkins` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo du -sh /var/lib/jenkins",
+    "example": "sudo du -sh /var/lib/jenkins",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-find-var-lib-jenkins-workspace-maxdepth-2-type-d",
+    "command": "sudo find /var/lib/jenkins/workspace -maxdepth 2 -type d",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo find /var/lib/jenkins/workspace -maxdepth 2 -type d` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo find /var/lib/jenkins/workspace -maxdepth 2 -type d",
+    "example": "sudo find /var/lib/jenkins/workspace -maxdepth 2 -type d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-help",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ help",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ help` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ help",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ help",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-get-job-job-name",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ get-job job-name",
+    "title": "Jenkins details/status dekhna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ get-job job-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ get-job job-name",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ get-job job-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-create-job-job-name-config-xml",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ create-job job-name < config.xml",
+    "title": "Jenkins resource create karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ create-job job-name < config.xml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ create-job job-name < config.xml",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ create-job job-name < config.xml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-delete-job-job-name",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ delete-job job-name",
+    "title": "Jenkins delete/destroy action chalana",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ delete-job job-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ delete-job job-name",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ delete-job job-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-enable-job-job-name",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ enable-job job-name",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ enable-job job-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ enable-job job-name",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ enable-job job-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-disable-job-job-name",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ disable-job job-name",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ disable-job job-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ disable-job job-name",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ disable-job job-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-safe-restart",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ safe-restart",
+    "title": "Jenkins restart/reload karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ safe-restart` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ safe-restart",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ safe-restart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-quiet-down",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ quiet-down",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ quiet-down` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ quiet-down",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ quiet-down",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-java-jar-jenkins-cli-jar-s-http-localhost-8080-cancel-quiet-down",
+    "command": "java -jar jenkins-cli.jar -s http://localhost:8080/ cancel-quiet-down",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `java -jar jenkins-cli.jar -s http://localhost:8080/ cancel-quiet-down` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "java -jar jenkins-cli.jar -s http://localhost:8080/ cancel-quiet-down",
+    "example": "java -jar jenkins-cli.jar -s http://localhost:8080/ cancel-quiet-down",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "java",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-chown-r-jenkins-jenkins-var-lib-jenkins-workspace",
+    "command": "sudo chown -R jenkins:jenkins /var/lib/jenkins/workspace",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo chown -R jenkins:jenkins /var/lib/jenkins/workspace` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chown -R jenkins:jenkins /var/lib/jenkins/workspace",
+    "example": "sudo chown -R jenkins:jenkins /var/lib/jenkins/workspace",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "jenkins-sudo-find-var-lib-jenkins-name-log-type-f",
+    "command": "sudo find /var/lib/jenkins -name \"*.log\" -type f",
+    "title": "Jenkins command use karna",
+    "category": "Jenkins",
+    "difficulty": "Beginner",
+    "use_case": "Jenkins me `sudo find /var/lib/jenkins -name \"*.log\" -type f` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo find /var/lib/jenkins -name \"*.log\" -type f",
+    "example": "sudo find /var/lib/jenkins -name \"*.log\" -type f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "jenkins",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/kubernetes.commands.json b/public/data/kubernetes.commands.json
index c75eb18..83e5156 100644
--- a/public/data/kubernetes.commands.json
+++ b/public/data/kubernetes.commands.json
@@ -394,5 +394,1155 @@
       "kubectl",
       "devops"
     ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-namespaces",
+    "command": "kubectl get namespaces",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get namespaces` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get namespaces",
+    "example": "kubectl get namespaces",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-namespace-dev",
+    "command": "kubectl create namespace dev",
+    "title": "Kubernetes resource create karna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl create namespace dev` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl create namespace dev",
+    "example": "kubectl create namespace dev",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-delete-namespace-dev",
+    "command": "kubectl delete namespace dev",
+    "title": "Kubernetes delete/destroy action chalana",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl delete namespace dev` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl delete namespace dev",
+    "example": "kubectl delete namespace dev",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-all-n-ns",
+    "command": "kubectl get all -n <ns>",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get all -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get all -n <ns>",
+    "example": "kubectl get all -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-ingress-a",
+    "command": "kubectl get ingress -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get ingress -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get ingress -A",
+    "example": "kubectl get ingress -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-configmap-a",
+    "command": "kubectl get configmap -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get configmap -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get configmap -A",
+    "example": "kubectl get configmap -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-secret-a",
+    "command": "kubectl get secret -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get secret -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get secret -A",
+    "example": "kubectl get secret -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pv",
+    "command": "kubectl get pv",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get pv` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get pv",
+    "example": "kubectl get pv",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pvc-a",
+    "command": "kubectl get pvc -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get pvc -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get pvc -A",
+    "example": "kubectl get pvc -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-jobs-a",
+    "command": "kubectl get jobs -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get jobs -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get jobs -A",
+    "example": "kubectl get jobs -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-cronjobs-a",
+    "command": "kubectl get cronjobs -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get cronjobs -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get cronjobs -A",
+    "example": "kubectl get cronjobs -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-hpa-a",
+    "command": "kubectl get hpa -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get hpa -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get hpa -A",
+    "example": "kubectl get hpa -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-endpoints-a",
+    "command": "kubectl get endpoints -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Beginner",
+    "use_case": "Kubernetes me `kubectl get endpoints -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get endpoints -A",
+    "example": "kubectl get endpoints -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-serviceaccounts-a",
+    "command": "kubectl get serviceaccounts -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get serviceaccounts -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get serviceaccounts -A",
+    "example": "kubectl get serviceaccounts -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-roles-a",
+    "command": "kubectl get roles -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get roles -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get roles -A",
+    "example": "kubectl get roles -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-rolebindings-a",
+    "command": "kubectl get rolebindings -A",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get rolebindings -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get rolebindings -A",
+    "example": "kubectl get rolebindings -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-clusterroles",
+    "command": "kubectl get clusterroles",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get clusterroles` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get clusterroles",
+    "example": "kubectl get clusterroles",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-clusterrolebindings",
+    "command": "kubectl get clusterrolebindings",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get clusterrolebindings` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get clusterrolebindings",
+    "example": "kubectl get clusterrolebindings",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-node-node",
+    "command": "kubectl describe node <node>",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe node <node>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl describe node <node>",
+    "example": "kubectl describe node <node>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-svc-service-n-ns",
+    "command": "kubectl describe svc <service> -n <ns>",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe svc <service> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl describe svc <service> -n <ns>",
+    "example": "kubectl describe svc <service> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-deploy-name-n-ns",
+    "command": "kubectl describe deploy <name> -n <ns>",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe deploy <name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl describe deploy <name> -n <ns>",
+    "example": "kubectl describe deploy <name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-ingress-name-n-ns",
+    "command": "kubectl describe ingress <name> -n <ns>",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe ingress <name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl describe ingress <name> -n <ns>",
+    "example": "kubectl describe ingress <name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-deployment-name-n-ns",
+    "command": "kubectl logs deployment/<name> -n <ns>",
+    "title": "Kubernetes logs/debugging dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs deployment/<name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl logs deployment/<name> -n <ns>",
+    "example": "kubectl logs deployment/<name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-previous-pod-n-ns",
+    "command": "kubectl logs --previous <pod> -n <ns>",
+    "title": "Kubernetes logs/debugging dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs --previous <pod> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl logs --previous <pod> -n <ns>",
+    "example": "kubectl logs --previous <pod> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-l-app-myapp-n-ns",
+    "command": "kubectl logs -l app=myapp -n <ns>",
+    "title": "Kubernetes logs/debugging dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs -l app=myapp -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl logs -l app=myapp -n <ns>",
+    "example": "kubectl logs -l app=myapp -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-exec-it-pod-n-ns-bash",
+    "command": "kubectl exec -it <pod> -n <ns> -- bash",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl exec -it <pod> -n <ns> -- bash` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl exec -it <pod> -n <ns> -- bash",
+    "example": "kubectl exec -it <pod> -n <ns> -- bash",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-cp-ns-pod-path-file-file",
+    "command": "kubectl cp <ns>/<pod>:/path/file ./file",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl cp <ns>/<pod>:/path/file ./file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl cp <ns>/<pod>:/path/file ./file",
+    "example": "kubectl cp <ns>/<pod>:/path/file ./file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-edit-deploy-name-n-ns",
+    "command": "kubectl edit deploy <name> -n <ns>",
+    "title": "Kubernetes apply/deploy/run karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl edit deploy <name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl edit deploy <name> -n <ns>",
+    "example": "kubectl edit deploy <name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-set-image-deploy-name-app-image-tag-n-ns",
+    "command": "kubectl set image deploy/<name> app=image:tag -n <ns>",
+    "title": "Kubernetes apply/deploy/run karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl set image deploy/<name> app=image:tag -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl set image deploy/<name> app=image:tag -n <ns>",
+    "example": "kubectl set image deploy/<name> app=image:tag -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-rollout-history-deploy-name-n-ns",
+    "command": "kubectl rollout history deploy/<name> -n <ns>",
+    "title": "Kubernetes restart/reload karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl rollout history deploy/<name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl rollout history deploy/<name> -n <ns>",
+    "example": "kubectl rollout history deploy/<name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-rollout-undo-deploy-name-n-ns",
+    "command": "kubectl rollout undo deploy/<name> -n <ns>",
+    "title": "Kubernetes restart/reload karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl rollout undo deploy/<name> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl rollout undo deploy/<name> -n <ns>",
+    "example": "kubectl rollout undo deploy/<name> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-autoscale-deploy-name-cpu-percent-70-min-2-max-10",
+    "command": "kubectl autoscale deploy <name> --cpu-percent=70 --min=2 --max=10",
+    "title": "Kubernetes apply/deploy/run karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl autoscale deploy <name> --cpu-percent=70 --min=2 --max=10` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl autoscale deploy <name> --cpu-percent=70 --min=2 --max=10",
+    "example": "kubectl autoscale deploy <name> --cpu-percent=70 --min=2 --max=10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-drain-node-ignore-daemonsets",
+    "command": "kubectl drain <node> --ignore-daemonsets",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl drain <node> --ignore-daemonsets` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl drain <node> --ignore-daemonsets",
+    "example": "kubectl drain <node> --ignore-daemonsets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-uncordon-node",
+    "command": "kubectl uncordon <node>",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl uncordon <node>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl uncordon <node>",
+    "example": "kubectl uncordon <node>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-cordon-node",
+    "command": "kubectl cordon <node>",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl cordon <node>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl cordon <node>",
+    "example": "kubectl cordon <node>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-label-nodes-node-key-value",
+    "command": "kubectl label nodes <node> key=value",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl label nodes <node> key=value` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl label nodes <node> key=value",
+    "example": "kubectl label nodes <node> key=value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-taint-nodes-node-key-value-noschedule",
+    "command": "kubectl taint nodes <node> key=value:NoSchedule",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl taint nodes <node> key=value:NoSchedule` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl taint nodes <node> key=value:NoSchedule",
+    "example": "kubectl taint nodes <node> key=value:NoSchedule",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-configmap-app-config-from-file-config-yml",
+    "command": "kubectl create configmap app-config --from-file=config.yml",
+    "title": "Kubernetes resource create karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl create configmap app-config --from-file=config.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl create configmap app-config --from-file=config.yml",
+    "example": "kubectl create configmap app-config --from-file=config.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-secret-tls-tls-secret-cert-cert-pem-key-key-pem",
+    "command": "kubectl create secret tls tls-secret --cert=cert.pem --key=key.pem",
+    "title": "Kubernetes resource create karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl create secret tls tls-secret --cert=cert.pem --key=key.pem` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl create secret tls tls-secret --cert=cert.pem --key=key.pem",
+    "example": "kubectl create secret tls tls-secret --cert=cert.pem --key=key.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-explain-pod",
+    "command": "kubectl explain pod",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl explain pod` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl explain pod",
+    "example": "kubectl explain pod",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-api-resources",
+    "command": "kubectl api-resources",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl api-resources` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl api-resources",
+    "example": "kubectl api-resources",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-api-versions",
+    "command": "kubectl api-versions",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl api-versions` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl api-versions",
+    "example": "kubectl api-versions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-auth-can-i-get-pods",
+    "command": "kubectl auth can-i get pods",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl auth can-i get pods` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl auth can-i get pods",
+    "example": "kubectl auth can-i get pods",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-auth-can-i-list",
+    "command": "kubectl auth can-i --list",
+    "title": "Kubernetes details/status dekhna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl auth can-i --list` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl auth can-i --list",
+    "example": "kubectl auth can-i --list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-diff-f-file-yaml",
+    "command": "kubectl diff -f file.yaml",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl diff -f file.yaml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl diff -f file.yaml",
+    "example": "kubectl diff -f file.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-kustomize",
+    "command": "kubectl kustomize .",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl kustomize .` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl kustomize .",
+    "example": "kubectl kustomize .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-apply-k",
+    "command": "kubectl apply -k .",
+    "title": "Kubernetes apply/deploy/run karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl apply -k .` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl apply -k .",
+    "example": "kubectl apply -k .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-delete-pod-pod-n-ns",
+    "command": "kubectl delete pod <pod> -n <ns>",
+    "title": "Kubernetes delete/destroy action chalana",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl delete pod <pod> -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl delete pod <pod> -n <ns>",
+    "example": "kubectl delete pod <pod> -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-proxy",
+    "command": "kubectl proxy",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl proxy` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl proxy",
+    "example": "kubectl proxy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-port-forward-pod-pod-8080-80-n-ns",
+    "command": "kubectl port-forward pod/<pod> 8080:80 -n <ns>",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl port-forward pod/<pod> 8080:80 -n <ns>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl port-forward pod/<pod> 8080:80 -n <ns>",
+    "example": "kubectl port-forward pod/<pod> 8080:80 -n <ns>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/linux.commands.json b/public/data/linux.commands.json
index c99d3a9..4554a81 100644
--- a/public/data/linux.commands.json
+++ b/public/data/linux.commands.json
@@ -548,5 +548,1487 @@
       "service",
       "restart"
     ]
+  },
+  {
+    "id": "linux-whoami",
+    "command": "whoami",
+    "title": "Linux details/status dekhna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `whoami` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "whoami",
+    "example": "whoami",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "whoami",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-id",
+    "command": "id",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `id` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "id",
+    "example": "id",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "id",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-hostname",
+    "command": "hostname",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `hostname` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "hostname",
+    "example": "hostname",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "hostname",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-hostnamectl",
+    "command": "hostnamectl",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `hostnamectl` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "hostnamectl",
+    "example": "hostnamectl",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "hostnamectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-uname-a",
+    "command": "uname -a",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `uname -a` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "uname -a",
+    "example": "uname -a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "uname",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-cat-etc-os-release",
+    "command": "cat /etc/os-release",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `cat /etc/os-release` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cat /etc/os-release",
+    "example": "cat /etc/os-release",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-date",
+    "command": "date",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `date` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "date",
+    "example": "date",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "date",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-timedatectl",
+    "command": "timedatectl",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `timedatectl` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "timedatectl",
+    "example": "timedatectl",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "timedatectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-cal",
+    "command": "cal",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Beginner",
+    "use_case": "Linux me `cal` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cal",
+    "example": "cal",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cal",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-history",
+    "command": "history",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `history` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "history",
+    "example": "history",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "history",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-clear",
+    "command": "clear",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `clear` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "clear",
+    "example": "clear",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "clear",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-alias",
+    "command": "alias",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `alias` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "alias",
+    "example": "alias",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "alias",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-unalias-name",
+    "command": "unalias name",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `unalias name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "unalias name",
+    "example": "unalias name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "unalias",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-env",
+    "command": "env",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `env` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "env",
+    "example": "env",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "env",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-printenv",
+    "command": "printenv",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `printenv` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "printenv",
+    "example": "printenv",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "printenv",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-echo-path",
+    "command": "echo $PATH",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `echo $PATH` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "echo $PATH",
+    "example": "echo $PATH",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "echo",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-which-nginx",
+    "command": "which nginx",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `which nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "which nginx",
+    "example": "which nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "which",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-whereis-nginx",
+    "command": "whereis nginx",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `whereis nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "whereis nginx",
+    "example": "whereis nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "whereis",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-type-cd",
+    "command": "type cd",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `type cd` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "type cd",
+    "example": "type cd",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "type",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-file-filename",
+    "command": "file filename",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `file filename` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "file filename",
+    "example": "file filename",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "file",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-stat-filename",
+    "command": "stat filename",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `stat filename` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "stat filename",
+    "example": "stat filename",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "stat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-wc-l-file-txt",
+    "command": "wc -l file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `wc -l file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "wc -l file.txt",
+    "example": "wc -l file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "wc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sort-file-txt",
+    "command": "sort file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `sort file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sort file.txt",
+    "example": "sort file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "sort",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-uniq-file-txt",
+    "command": "uniq file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `uniq file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "uniq file.txt",
+    "example": "uniq file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "uniq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-cut-d-f1-etc-passwd",
+    "command": "cut -d: -f1 /etc/passwd",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `cut -d: -f1 /etc/passwd` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cut -d: -f1 /etc/passwd",
+    "example": "cut -d: -f1 /etc/passwd",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cut",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-awk-print-1-file-txt",
+    "command": "awk '{print $1}' file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `awk '{print $1}' file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "awk '{print $1}' file.txt",
+    "example": "awk '{print $1}' file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sed-n-1-20p-file-txt",
+    "command": "sed -n \"1,20p\" file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `sed -n \"1,20p\" file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sed -n \"1,20p\" file.txt",
+    "example": "sed -n \"1,20p\" file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "sed",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tar-czf-backup-tar-gz-folder",
+    "command": "tar -czf backup.tar.gz folder",
+    "title": "Linux backup/export lena",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `tar -czf backup.tar.gz folder` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tar -czf backup.tar.gz folder",
+    "example": "tar -czf backup.tar.gz folder",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tar-xzf-backup-tar-gz",
+    "command": "tar -xzf backup.tar.gz",
+    "title": "Linux backup/export lena",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `tar -xzf backup.tar.gz` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tar -xzf backup.tar.gz",
+    "example": "tar -xzf backup.tar.gz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-zip-r-backup-zip-folder",
+    "command": "zip -r backup.zip folder",
+    "title": "Linux backup/export lena",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `zip -r backup.zip folder` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "zip -r backup.zip folder",
+    "example": "zip -r backup.zip folder",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "zip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-unzip-backup-zip",
+    "command": "unzip backup.zip",
+    "title": "Linux backup/export lena",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `unzip backup.zip` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "unzip backup.zip",
+    "example": "unzip backup.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "unzip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-gzip-file-log",
+    "command": "gzip file.log",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `gzip file.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gzip file.log",
+    "example": "gzip file.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "gzip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-gunzip-file-log-gz",
+    "command": "gunzip file.log.gz",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `gunzip file.log.gz` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "gunzip file.log.gz",
+    "example": "gunzip file.log.gz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "gunzip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-rsync-av-source-destination",
+    "command": "rsync -av source/ destination/",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `rsync -av source/ destination/` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "rsync -av source/ destination/",
+    "example": "rsync -av source/ destination/",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "rsync",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-rsync-avz-folder-user-host-path",
+    "command": "rsync -avz folder/ user@host:/path/",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `rsync -avz folder/ user@host:/path/` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "rsync -avz folder/ user@host:/path/",
+    "example": "rsync -avz folder/ user@host:/path/",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "rsync",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-scp-file-user-host-path",
+    "command": "scp file user@host:/path/",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `scp file user@host:/path/` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "scp file user@host:/path/",
+    "example": "scp file user@host:/path/",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "scp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-user-server",
+    "command": "ssh user@server",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ssh user@server` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ssh user@server",
+    "example": "ssh user@server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-keygen-t-ed25519",
+    "command": "ssh-keygen -t ed25519",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ssh-keygen -t ed25519` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ssh-keygen -t ed25519",
+    "example": "ssh-keygen -t ed25519",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh-keygen",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-copy-id-user-server",
+    "command": "ssh-copy-id user@server",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ssh-copy-id user@server` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ssh-copy-id user@server",
+    "example": "ssh-copy-id user@server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh-copy-id",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-crontab-l",
+    "command": "crontab -l",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `crontab -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "crontab -l",
+    "example": "crontab -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "crontab",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-crontab-e",
+    "command": "crontab -e",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `crontab -e` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "crontab -e",
+    "example": "crontab -e",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "crontab",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-crontab-l",
+    "command": "sudo crontab -l",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `sudo crontab -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo crontab -l",
+    "example": "sudo crontab -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-at-now-5-minutes",
+    "command": "at now + 5 minutes",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `at now + 5 minutes` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "at now + 5 minutes",
+    "example": "at now + 5 minutes",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "at",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sleep-10",
+    "command": "sleep 10",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `sleep 10` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sleep 10",
+    "example": "sleep 10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "sleep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-watch-n-2-df-h",
+    "command": "watch -n 2 df -h",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `watch -n 2 df -h` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "watch -n 2 df -h",
+    "example": "watch -n 2 df -h",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "watch",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nohup-command",
+    "command": "nohup command &",
+    "title": "Linux apply/deploy/run karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `nohup command &` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "nohup command &",
+    "example": "nohup command &",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nohup",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-jobs",
+    "command": "jobs",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `jobs` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "jobs",
+    "example": "jobs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "jobs",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-fg",
+    "command": "fg",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `fg` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "fg",
+    "example": "fg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "fg",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-bg",
+    "command": "bg",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `bg` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "bg",
+    "example": "bg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "bg",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-killall-nginx",
+    "command": "killall nginx",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `killall nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "killall nginx",
+    "example": "killall nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "killall",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-pkill-f-worker",
+    "command": "pkill -f worker",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `pkill -f worker` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pkill -f worker",
+    "example": "pkill -f worker",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "pkill",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nice-n-10-command",
+    "command": "nice -n 10 command",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `nice -n 10 command` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "nice -n 10 command",
+    "example": "nice -n 10 command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nice",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-renice-5-p-1234",
+    "command": "renice 5 -p 1234",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `renice 5 -p 1234` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "renice 5 -p 1234",
+    "example": "renice 5 -p 1234",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "renice",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-mount",
+    "command": "mount",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `mount` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mount",
+    "example": "mount",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "mount",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-umount-mnt-data",
+    "command": "umount /mnt/data",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `umount /mnt/data` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "umount /mnt/data",
+    "example": "umount /mnt/data",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "umount",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lsblk",
+    "command": "lsblk",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lsblk` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "lsblk",
+    "example": "lsblk",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lsblk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-blkid",
+    "command": "blkid",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `blkid` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "blkid",
+    "example": "blkid",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "blkid",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-fdisk-l",
+    "command": "fdisk -l",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `fdisk -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "fdisk -l",
+    "example": "fdisk -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "fdisk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-findmnt",
+    "command": "findmnt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `findmnt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "findmnt",
+    "example": "findmnt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "findmnt",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ln-s-source-link",
+    "command": "ln -s source link",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ln -s source link` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ln -s source link",
+    "example": "ln -s source link",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ln",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-readlink-f-link",
+    "command": "readlink -f link",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `readlink -f link` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "readlink -f link",
+    "example": "readlink -f link",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "readlink",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-basename-path-file-txt",
+    "command": "basename /path/file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `basename /path/file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "basename /path/file.txt",
+    "example": "basename /path/file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "basename",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dirname-path-file-txt",
+    "command": "dirname /path/file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dirname /path/file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dirname /path/file.txt",
+    "example": "dirname /path/file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dirname",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-xargs",
+    "command": "xargs",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `xargs` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "xargs",
+    "example": "xargs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "xargs",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tee-file-txt",
+    "command": "tee file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `tee file.txt` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tee file.txt",
+    "example": "tee file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tee",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-update",
+    "command": "sudo apt update",
+    "title": "Linux apply/deploy/run karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt update` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt update",
+    "example": "sudo apt update",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-install-package",
+    "command": "sudo apt install package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt install package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt install package",
+    "example": "sudo apt install package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-remove-package",
+    "command": "sudo apt remove package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt remove package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt remove package",
+    "example": "sudo apt remove package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-autoremove",
+    "command": "sudo apt autoremove",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt autoremove` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt autoremove",
+    "example": "sudo apt autoremove",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-cache-policy-package",
+    "command": "sudo apt-cache policy package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt-cache policy package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt-cache policy package",
+    "example": "sudo apt-cache policy package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dpkg-l",
+    "command": "dpkg -l",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dpkg -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dpkg -l",
+    "example": "dpkg -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dpkg",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dpkg-i-package-deb",
+    "command": "dpkg -i package.deb",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dpkg -i package.deb` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dpkg -i package.deb",
+    "example": "dpkg -i package.deb",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dpkg",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-yum-update",
+    "command": "sudo yum update",
+    "title": "Linux apply/deploy/run karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo yum update` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo yum update",
+    "example": "sudo yum update",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-yum-install-package",
+    "command": "sudo yum install package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo yum install package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo yum install package",
+    "example": "sudo yum install package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-dnf-install-package",
+    "command": "sudo dnf install package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo dnf install package` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo dnf install package",
+    "example": "sudo dnf install package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-service-nginx-status",
+    "command": "sudo service nginx status",
+    "title": "Linux details/status dekhna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo service nginx status` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo service nginx status",
+    "example": "sudo service nginx status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-reboot",
+    "command": "sudo reboot",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo reboot` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo reboot",
+    "example": "sudo reboot",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-shutdown-h-now",
+    "command": "sudo shutdown -h now",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo shutdown -h now` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo shutdown -h now",
+    "example": "sudo shutdown -h now",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/logs-debugging.commands.json b/public/data/logs-debugging.commands.json
index 31aefbb..47497d3 100644
--- a/public/data/logs-debugging.commands.json
+++ b/public/data/logs-debugging.commands.json
@@ -322,5 +322,602 @@
       "nslookup",
       "devops"
     ]
+  },
+  {
+    "id": "logs-debugging-journalctl-u-nginx-since-1-hour-ago",
+    "command": "journalctl -u nginx --since \"1 hour ago\"",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl -u nginx --since \"1 hour ago\"` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl -u nginx --since \"1 hour ago\"",
+    "example": "journalctl -u nginx --since \"1 hour ago\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-u-nginx-since-today",
+    "command": "journalctl -u nginx --since today",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl -u nginx --since today` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl -u nginx --since today",
+    "example": "journalctl -u nginx --since today",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-p-warning-n-100",
+    "command": "journalctl -p warning -n 100",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl -p warning -n 100` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl -p warning -n 100",
+    "example": "journalctl -p warning -n 100",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-disk-usage",
+    "command": "journalctl --disk-usage",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl --disk-usage` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl --disk-usage",
+    "example": "journalctl --disk-usage",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-vacuum-time-7d",
+    "command": "journalctl --vacuum-time=7d",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl --vacuum-time=7d` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl --vacuum-time=7d",
+    "example": "journalctl --vacuum-time=7d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-tail-f-var-log-kern-log",
+    "command": "tail -f /var/log/kern.log",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `tail -f /var/log/kern.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tail -f /var/log/kern.log",
+    "example": "tail -f /var/log/kern.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "tail",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-tail-f-var-log-mysql-error-log",
+    "command": "tail -f /var/log/mysql/error.log",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `tail -f /var/log/mysql/error.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tail -f /var/log/mysql/error.log",
+    "example": "tail -f /var/log/mysql/error.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "tail",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-tail-f-var-log-postgresql-postgresql-log",
+    "command": "tail -f /var/log/postgresql/postgresql.log",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `tail -f /var/log/postgresql/postgresql.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tail -f /var/log/postgresql/postgresql.log",
+    "example": "tail -f /var/log/postgresql/postgresql.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "tail",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-i-out-of-memory-var-log-syslog",
+    "command": "grep -i \"out of memory\" /var/log/syslog",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -i \"out of memory\" /var/log/syslog` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -i \"out of memory\" /var/log/syslog",
+    "example": "grep -i \"out of memory\" /var/log/syslog",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-i-permission-denied-app-log",
+    "command": "grep -i \"permission denied\" app.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -i \"permission denied\" app.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -i \"permission denied\" app.log",
+    "example": "grep -i \"permission denied\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-i-connection-refused-app-log",
+    "command": "grep -i \"connection refused\" app.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -i \"connection refused\" app.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -i \"connection refused\" app.log",
+    "example": "grep -i \"connection refused\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-i-timeout-app-log",
+    "command": "grep -i \"timeout\" app.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -i \"timeout\" app.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -i \"timeout\" app.log",
+    "example": "grep -i \"timeout\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-n-error-app-log",
+    "command": "grep -n \"ERROR\" app.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -n \"ERROR\" app.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -n \"ERROR\" app.log",
+    "example": "grep -n \"ERROR\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-c-5-exception-app-log",
+    "command": "grep -C 5 \"Exception\" app.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `grep -C 5 \"Exception\" app.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grep -C 5 \"Exception\" app.log",
+    "example": "grep -C 5 \"Exception\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-awk-print-9-access-log-sort-uniq-c",
+    "command": "awk '{print $9}' access.log | sort | uniq -c",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `awk '{print $9}' access.log | sort | uniq -c` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "awk '{print $9}' access.log | sort | uniq -c",
+    "example": "awk '{print $9}' access.log | sort | uniq -c",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-awk-print-1-access-log-sort-uniq-c-sort-nr-head",
+    "command": "awk '{print $1}' access.log | sort | uniq -c | sort -nr | head",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `awk '{print $1}' access.log | sort | uniq -c | sort -nr | head` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "awk '{print $1}' access.log | sort | uniq -c | sort -nr | head",
+    "example": "awk '{print $1}' access.log | sort | uniq -c | sort -nr | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-jq-file-json",
+    "command": "jq . file.json",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `jq . file.json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "jq . file.json",
+    "example": "jq . file.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "jq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-jq-message-logs-json",
+    "command": "jq .message logs.json",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `jq .message logs.json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "jq .message logs.json",
+    "example": "jq .message logs.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "jq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-python-m-json-tool-file-json",
+    "command": "python -m json.tool file.json",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `python -m json.tool file.json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "python -m json.tool file.json",
+    "example": "python -m json.tool file.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "python",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-s-http-localhost-health-jq",
+    "command": "curl -s http://localhost/health | jq",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl -s http://localhost/health | jq` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl -s http://localhost/health | jq",
+    "example": "curl -s http://localhost/health | jq",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-w-curl-format-txt-o-dev-null-s-https-example-com",
+    "command": "curl -w \"@curl-format.txt\" -o /dev/null -s https://example.com",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl -w \"@curl-format.txt\" -o /dev/null -s https://example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl -w \"@curl-format.txt\" -o /dev/null -s https://example.com",
+    "example": "curl -w \"@curl-format.txt\" -o /dev/null -s https://example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-nc-vz-host-443",
+    "command": "nc -vz host 443",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `nc -vz host 443` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "nc -vz host 443",
+    "example": "nc -vz host 443",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "nc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-telnet-host-25",
+    "command": "telnet host 25",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `telnet host 25` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "telnet host 25",
+    "example": "telnet host 25",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "telnet",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-dig-txt-example-com",
+    "command": "dig TXT example.com",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `dig TXT example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dig TXT example.com",
+    "example": "dig TXT example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-dig-mx-example-com",
+    "command": "dig MX example.com",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `dig MX example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dig MX example.com",
+    "example": "dig MX example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-dig-ns-example-com",
+    "command": "dig NS example.com",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `dig NS example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dig NS example.com",
+    "example": "dig NS example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-dig-a-example-com",
+    "command": "dig A example.com",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `dig A example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dig A example.com",
+    "example": "dig A example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-nslookup-type-mx-example-com",
+    "command": "nslookup -type=mx example.com",
+    "title": "Logs / Debugging apply/deploy/run karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `nslookup -type=mx example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "nslookup -type=mx example.com",
+    "example": "nslookup -type=mx example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "nslookup",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-openssl-s-client-showcerts-connect-example-com-443",
+    "command": "openssl s_client -showcerts -connect example.com:443",
+    "title": "Logs / Debugging details/status dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `openssl s_client -showcerts -connect example.com:443` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "openssl s_client -showcerts -connect example.com:443",
+    "example": "openssl s_client -showcerts -connect example.com:443",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-docker-compose-logs-tail-100-f",
+    "command": "docker compose logs --tail=100 -f",
+    "title": "Logs / Debugging logs/debugging dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `docker compose logs --tail=100 -f` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker compose logs --tail=100 -f",
+    "example": "docker compose logs --tail=100 -f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "logs",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-kubectl-describe-events-n-ns",
+    "command": "kubectl describe events -n ns",
+    "title": "Logs / Debugging details/status dekhna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `kubectl describe events -n ns` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl describe events -n ns",
+    "example": "kubectl describe events -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "logs",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/monitoring.commands.json b/public/data/monitoring.commands.json
index 4306af4..7ef0784 100644
--- a/public/data/monitoring.commands.json
+++ b/public/data/monitoring.commands.json
@@ -250,5 +250,424 @@
       "kubectl",
       "devops"
     ]
+  },
+  {
+    "id": "prometheus-grafana-promtool-tsdb-analyze-data",
+    "command": "promtool tsdb analyze data/",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `promtool tsdb analyze data/` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "promtool tsdb analyze data/",
+    "example": "promtool tsdb analyze data/",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "promtool",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-promtool-query-instant-http-localhost-9090-up",
+    "command": "promtool query instant http://localhost:9090 up",
+    "title": "Prometheus / Grafana apply/deploy/run karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `promtool query instant http://localhost:9090 up` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "promtool query instant http://localhost:9090 up",
+    "example": "promtool query instant http://localhost:9090 up",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "promtool",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-promtool-query-range-http-localhost-9090-up-start-2026-01-01t00-00-00z-end-2026-01-01t01-0",
+    "command": "promtool query range http://localhost:9090 up --start=2026-01-01T00:00:00Z --end=2026-01-01T01:00:00Z --step=1m",
+    "title": "Prometheus / Grafana apply/deploy/run karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `promtool query range http://localhost:9090 up --start=2026-01-01T00:00:00Z --end=2026-01-01T01:00:00Z --step=1m` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "promtool query range http://localhost:9090 up --start=2026-01-01T00:00:00Z --end=2026-01-01T01:00:00Z --step=1m",
+    "example": "promtool query range http://localhost:9090 up --start=2026-01-01T00:00:00Z --end=2026-01-01T01:00:00Z --step=1m",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "promtool",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-9090-api-v1-status-config",
+    "command": "curl http://localhost:9090/api/v1/status/config",
+    "title": "Prometheus / Grafana details/status dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:9090/api/v1/status/config` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:9090/api/v1/status/config",
+    "example": "curl http://localhost:9090/api/v1/status/config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-9090-api-v1-status-runtimeinfo",
+    "command": "curl http://localhost:9090/api/v1/status/runtimeinfo",
+    "title": "Prometheus / Grafana details/status dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:9090/api/v1/status/runtimeinfo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:9090/api/v1/status/runtimeinfo",
+    "example": "curl http://localhost:9090/api/v1/status/runtimeinfo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-9090-api-v1-alerts",
+    "command": "curl http://localhost:9090/api/v1/alerts",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:9090/api/v1/alerts` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:9090/api/v1/alerts",
+    "example": "curl http://localhost:9090/api/v1/alerts",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-9090-api-v1-rules",
+    "command": "curl http://localhost:9090/api/v1/rules",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:9090/api/v1/rules` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:9090/api/v1/rules",
+    "example": "curl http://localhost:9090/api/v1/rules",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-9090-api-v1-label-name-values",
+    "command": "curl http://localhost:9090/api/v1/label/__name__/values",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:9090/api/v1/label/__name__/values` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:9090/api/v1/label/__name__/values",
+    "example": "curl http://localhost:9090/api/v1/label/__name__/values",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-localhost-3000-api-search",
+    "command": "curl http://localhost:3000/api/search",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://localhost:3000/api/search` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://localhost:3000/api/search",
+    "example": "curl http://localhost:3000/api/search",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-admin-pass-localhost-3000-api-datasources",
+    "command": "curl http://admin:pass@localhost:3000/api/datasources",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://admin:pass@localhost:3000/api/datasources` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://admin:pass@localhost:3000/api/datasources",
+    "example": "curl http://admin:pass@localhost:3000/api/datasources",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-curl-http-admin-pass-localhost-3000-api-dashboards-home",
+    "command": "curl http://admin:pass@localhost:3000/api/dashboards/home",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `curl http://admin:pass@localhost:3000/api/dashboards/home` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl http://admin:pass@localhost:3000/api/dashboards/home",
+    "example": "curl http://admin:pass@localhost:3000/api/dashboards/home",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-grafana-cli-plugins-ls",
+    "command": "grafana-cli plugins ls",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `grafana-cli plugins ls` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grafana-cli plugins ls",
+    "example": "grafana-cli plugins ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "grafana-cli",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-grafana-cli-plugins-install-plugin-name",
+    "command": "grafana-cli plugins install plugin-name",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `grafana-cli plugins install plugin-name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grafana-cli plugins install plugin-name",
+    "example": "grafana-cli plugins install plugin-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "grafana-cli",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-grafana-cli-admin-reset-admin-password-newpass",
+    "command": "grafana-cli admin reset-admin-password newpass",
+    "title": "Prometheus / Grafana resource create karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `grafana-cli admin reset-admin-password newpass` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "grafana-cli admin reset-admin-password newpass",
+    "example": "grafana-cli admin reset-admin-password newpass",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "grafana-cli",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-sudo-systemctl-restart-prometheus",
+    "command": "sudo systemctl restart prometheus",
+    "title": "Prometheus / Grafana restart/reload karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `sudo systemctl restart prometheus` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl restart prometheus",
+    "example": "sudo systemctl restart prometheus",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-sudo-systemctl-restart-grafana-server",
+    "command": "sudo systemctl restart grafana-server",
+    "title": "Prometheus / Grafana restart/reload karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `sudo systemctl restart grafana-server` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl restart grafana-server",
+    "example": "sudo systemctl restart grafana-server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "prometheus",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-docker-exec-it-prometheus-promtool-check-config-etc-prometheus-prometheus-yml",
+    "command": "docker exec -it prometheus promtool check config /etc/prometheus/prometheus.yml",
+    "title": "Prometheus / Grafana command use karna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `docker exec -it prometheus promtool check config /etc/prometheus/prometheus.yml` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "docker exec -it prometheus promtool check config /etc/prometheus/prometheus.yml",
+    "example": "docker exec -it prometheus promtool check config /etc/prometheus/prometheus.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "prometheus",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-kubectl-logs-n-monitoring-deploy-prometheus-server",
+    "command": "kubectl logs -n monitoring deploy/prometheus-server",
+    "title": "Prometheus / Grafana logs/debugging dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `kubectl logs -n monitoring deploy/prometheus-server` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl logs -n monitoring deploy/prometheus-server",
+    "example": "kubectl logs -n monitoring deploy/prometheus-server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "prometheus",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-kubectl-logs-n-monitoring-deploy-grafana",
+    "command": "kubectl logs -n monitoring deploy/grafana",
+    "title": "Prometheus / Grafana logs/debugging dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `kubectl logs -n monitoring deploy/grafana` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl logs -n monitoring deploy/grafana",
+    "example": "kubectl logs -n monitoring deploy/grafana",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "prometheus",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-kubectl-get-servicemonitor-a",
+    "command": "kubectl get servicemonitor -A",
+    "title": "Prometheus / Grafana details/status dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `kubectl get servicemonitor -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get servicemonitor -A",
+    "example": "kubectl get servicemonitor -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "prometheus",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "prometheus-grafana-kubectl-get-prometheusrule-a",
+    "command": "kubectl get prometheusrule -A",
+    "title": "Prometheus / Grafana details/status dekhna",
+    "category": "Prometheus / Grafana",
+    "difficulty": "Beginner",
+    "use_case": "Prometheus / Grafana me `kubectl get prometheusrule -A` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "kubectl get prometheusrule -A",
+    "example": "kubectl get prometheusrule -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe",
+      "kubectl logs"
+    ],
+    "tags": [
+      "prometheus",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/nginx.commands.json b/public/data/nginx.commands.json
index e046038..cef71c5 100644
--- a/public/data/nginx.commands.json
+++ b/public/data/nginx.commands.json
@@ -268,5 +268,461 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "nginx-sudo-systemctl-stop-nginx",
+    "command": "sudo systemctl stop nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo systemctl stop nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl stop nginx",
+    "example": "sudo systemctl stop nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-systemctl-start-nginx",
+    "command": "sudo systemctl start nginx",
+    "title": "Nginx apply/deploy/run karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo systemctl start nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl start nginx",
+    "example": "sudo systemctl start nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-systemctl-disable-nginx",
+    "command": "sudo systemctl disable nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo systemctl disable nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl disable nginx",
+    "example": "sudo systemctl disable nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-nginx-v",
+    "command": "sudo nginx -V",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo nginx -V` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nginx -V",
+    "example": "sudo nginx -V",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-tail-n-100-var-log-nginx-access-log",
+    "command": "sudo tail -n 100 /var/log/nginx/access.log",
+    "title": "Nginx logs/debugging dekhna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo tail -n 100 /var/log/nginx/access.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo tail -n 100 /var/log/nginx/access.log",
+    "example": "sudo tail -n 100 /var/log/nginx/access.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-tail-n-100-var-log-nginx-error-log",
+    "command": "sudo tail -n 100 /var/log/nginx/error.log",
+    "title": "Nginx logs/debugging dekhna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo tail -n 100 /var/log/nginx/error.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo tail -n 100 /var/log/nginx/error.log",
+    "example": "sudo tail -n 100 /var/log/nginx/error.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-grep-r-listen-80-etc-nginx",
+    "command": "sudo grep -R \"listen 80\" /etc/nginx",
+    "title": "Nginx details/status dekhna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo grep -R \"listen 80\" /etc/nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep -R \"listen 80\" /etc/nginx",
+    "example": "sudo grep -R \"listen 80\" /etc/nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-grep-r-server-name-etc-nginx-sites-enabled",
+    "command": "sudo grep -R \"server_name\" /etc/nginx/sites-enabled",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo grep -R \"server_name\" /etc/nginx/sites-enabled` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep -R \"server_name\" /etc/nginx/sites-enabled",
+    "example": "sudo grep -R \"server_name\" /etc/nginx/sites-enabled",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-nano-etc-nginx-nginx-conf",
+    "command": "sudo nano /etc/nginx/nginx.conf",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo nano /etc/nginx/nginx.conf` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nano /etc/nginx/nginx.conf",
+    "example": "sudo nano /etc/nginx/nginx.conf",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-nano-etc-nginx-sites-available-default",
+    "command": "sudo nano /etc/nginx/sites-available/default",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo nano /etc/nginx/sites-available/default` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nano /etc/nginx/sites-available/default",
+    "example": "sudo nano /etc/nginx/sites-available/default",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-cp-etc-nginx-sites-available-default-etc-nginx-sites-available-example",
+    "command": "sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/example",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/example` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/example",
+    "example": "sudo cp /etc/nginx/sites-available/default /etc/nginx/sites-available/example",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-unlink-etc-nginx-sites-enabled-example",
+    "command": "sudo unlink /etc/nginx/sites-enabled/example",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo unlink /etc/nginx/sites-enabled/example` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo unlink /etc/nginx/sites-enabled/example",
+    "example": "sudo unlink /etc/nginx/sites-enabled/example",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-mkdir-p-var-www-example",
+    "command": "sudo mkdir -p /var/www/example",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo mkdir -p /var/www/example` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo mkdir -p /var/www/example",
+    "example": "sudo mkdir -p /var/www/example",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-chown-r-www-data-www-data-var-www-example",
+    "command": "sudo chown -R www-data:www-data /var/www/example",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo chown -R www-data:www-data /var/www/example` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chown -R www-data:www-data /var/www/example",
+    "example": "sudo chown -R www-data:www-data /var/www/example",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-chmod-r-755-var-www-example",
+    "command": "sudo chmod -R 755 /var/www/example",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo chmod -R 755 /var/www/example` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chmod -R 755 /var/www/example",
+    "example": "sudo chmod -R 755 /var/www/example",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-curl-h-host-example-com-http-127-0-0-1",
+    "command": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `curl -H \"Host: example.com\" http://127.0.0.1` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "example": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-curl-i-http-localhost",
+    "command": "curl -I http://localhost",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `curl -I http://localhost` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl -I http://localhost",
+    "example": "curl -I http://localhost",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-openssl-s-client-connect-example-com-443-servername-example-com",
+    "command": "openssl s_client -connect example.com:443 -servername example.com",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `openssl s_client -connect example.com:443 -servername example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "openssl s_client -connect example.com:443 -servername example.com",
+    "example": "openssl s_client -connect example.com:443 -servername example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-logrotate-d-etc-logrotate-d-nginx",
+    "command": "sudo logrotate -d /etc/logrotate.d/nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo logrotate -d /etc/logrotate.d/nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo logrotate -d /etc/logrotate.d/nginx",
+    "example": "sudo logrotate -d /etc/logrotate.d/nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-apt-install-nginx",
+    "command": "sudo apt install nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Beginner",
+    "use_case": "Nginx me `sudo apt install nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt install nginx",
+    "example": "sudo apt install nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-apt-remove-nginx",
+    "command": "sudo apt remove nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Intermediate",
+    "use_case": "Nginx me `sudo apt remove nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt remove nginx",
+    "example": "sudo apt remove nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-ss-tulpn-grep-nginx",
+    "command": "sudo ss -tulpn | grep nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Intermediate",
+    "use_case": "Nginx me `sudo ss -tulpn | grep nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ss -tulpn | grep nginx",
+    "example": "sudo ss -tulpn | grep nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-ps-aux-grep-nginx",
+    "command": "ps aux | grep nginx",
+    "title": "Nginx command use karna",
+    "category": "Nginx",
+    "difficulty": "Intermediate",
+    "use_case": "Nginx me `ps aux | grep nginx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ps aux | grep nginx",
+    "example": "ps aux | grep nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "ps",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "nginx-sudo-kill-hup-cat-run-nginx-pid",
+    "command": "sudo kill -HUP $(cat /run/nginx.pid)",
+    "title": "Nginx apply/deploy/run karna",
+    "category": "Nginx",
+    "difficulty": "Intermediate",
+    "use_case": "Nginx me `sudo kill -HUP $(cat /run/nginx.pid)` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo kill -HUP $(cat /run/nginx.pid)",
+    "example": "sudo kill -HUP $(cat /run/nginx.pid)",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "nginx",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/security.commands.json b/public/data/security.commands.json
index 1f3c613..21af8d5 100644
--- a/public/data/security.commands.json
+++ b/public/data/security.commands.json
@@ -322,5 +322,613 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "server-security-sudo-adduser-deploy",
+    "command": "sudo adduser deploy",
+    "title": "Server Security resource create karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo adduser deploy` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo adduser deploy",
+    "example": "sudo adduser deploy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-usermod-ag-sudo-deploy",
+    "command": "sudo usermod -aG sudo deploy",
+    "title": "Server Security apply/deploy/run karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo usermod -aG sudo deploy` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo usermod -aG sudo deploy",
+    "example": "sudo usermod -aG sudo deploy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-passwd-l-root",
+    "command": "sudo passwd -l root",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo passwd -l root` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo passwd -l root",
+    "example": "sudo passwd -l root",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-passwd-u-user",
+    "command": "sudo passwd -u user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo passwd -u user` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo passwd -u user",
+    "example": "sudo passwd -u user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-chage-l-user",
+    "command": "sudo chage -l user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo chage -l user` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chage -l user",
+    "example": "sudo chage -l user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-chage-m-90-user",
+    "command": "sudo chage -M 90 user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo chage -M 90 user` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chage -M 90 user",
+    "example": "sudo chage -M 90 user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-visudo",
+    "command": "sudo visudo",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo visudo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo visudo",
+    "example": "sudo visudo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-grep-sudo-var-log-auth-log",
+    "command": "sudo grep sudo /var/log/auth.log",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo grep sudo /var/log/auth.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep sudo /var/log/auth.log",
+    "example": "sudo grep sudo /var/log/auth.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-journalctl-u-ssh-n-100",
+    "command": "sudo journalctl -u ssh -n 100",
+    "title": "Server Security logs/debugging dekhna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo journalctl -u ssh -n 100` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo journalctl -u ssh -n 100",
+    "example": "sudo journalctl -u ssh -n 100",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-grep-failed-password-var-log-auth-log",
+    "command": "sudo grep \"Failed password\" /var/log/auth.log",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo grep \"Failed password\" /var/log/auth.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep \"Failed password\" /var/log/auth.log",
+    "example": "sudo grep \"Failed password\" /var/log/auth.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-grep-accepted-var-log-auth-log",
+    "command": "sudo grep \"Accepted\" /var/log/auth.log",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo grep \"Accepted\" /var/log/auth.log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo grep \"Accepted\" /var/log/auth.log",
+    "example": "sudo grep \"Accepted\" /var/log/auth.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ufw-allow-openssh",
+    "command": "sudo ufw allow OpenSSH",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo ufw allow OpenSSH` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw allow OpenSSH",
+    "example": "sudo ufw allow OpenSSH",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ufw-limit-openssh",
+    "command": "sudo ufw limit OpenSSH",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo ufw limit OpenSSH` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ufw limit OpenSSH",
+    "example": "sudo ufw limit OpenSSH",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-apt-install-fail2ban",
+    "command": "sudo apt install fail2ban",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo apt install fail2ban` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo apt install fail2ban",
+    "example": "sudo apt install fail2ban",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-systemctl-status-fail2ban",
+    "command": "sudo systemctl status fail2ban",
+    "title": "Server Security details/status dekhna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo systemctl status fail2ban` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl status fail2ban",
+    "example": "sudo systemctl status fail2ban",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-fail2ban-client-set-sshd-unbanip-ip",
+    "command": "sudo fail2ban-client set sshd unbanip IP",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo fail2ban-client set sshd unbanip IP` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo fail2ban-client set sshd unbanip IP",
+    "example": "sudo fail2ban-client set sshd unbanip IP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-lynis-audit-system",
+    "command": "sudo lynis audit system",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Beginner",
+    "use_case": "Server Security me `sudo lynis audit system` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo lynis audit system",
+    "example": "sudo lynis audit system",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-rkhunter-check",
+    "command": "sudo rkhunter --check",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo rkhunter --check` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo rkhunter --check",
+    "example": "sudo rkhunter --check",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-chkrootkit",
+    "command": "sudo chkrootkit",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo chkrootkit` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo chkrootkit",
+    "example": "sudo chkrootkit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-find-type-f-perm-002-ls-2-dev-null",
+    "command": "sudo find / -type f -perm -002 -ls 2>/dev/null",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo find / -type f -perm -002 -ls 2>/dev/null` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo find / -type f -perm -002 -ls 2>/dev/null",
+    "example": "sudo find / -type f -perm -002 -ls 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-find-nouser-o-nogroup",
+    "command": "sudo find / -nouser -o -nogroup",
+    "title": "Server Security apply/deploy/run karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo find / -nouser -o -nogroup` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo find / -nouser -o -nogroup",
+    "example": "sudo find / -nouser -o -nogroup",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-getfacl-file",
+    "command": "sudo getfacl file",
+    "title": "Server Security details/status dekhna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo getfacl file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo getfacl file",
+    "example": "sudo getfacl file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-setfacl-m-u-user-r-file",
+    "command": "sudo setfacl -m u:user:r file",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo setfacl -m u:user:r file` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo setfacl -m u:user:r file",
+    "example": "sudo setfacl -m u:user:r file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-auditctl-l",
+    "command": "sudo auditctl -l",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo auditctl -l` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo auditctl -l",
+    "example": "sudo auditctl -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ausearch-m-user-login",
+    "command": "sudo ausearch -m USER_LOGIN",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ausearch -m USER_LOGIN` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ausearch -m USER_LOGIN",
+    "example": "sudo ausearch -m USER_LOGIN",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-aa-status",
+    "command": "sudo aa-status",
+    "title": "Server Security details/status dekhna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo aa-status` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo aa-status",
+    "example": "sudo aa-status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-sestatus",
+    "command": "sudo sestatus",
+    "title": "Server Security details/status dekhna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo sestatus` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo sestatus",
+    "example": "sudo sestatus",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-iptables-l-input-n-v",
+    "command": "sudo iptables -L INPUT -n -v",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo iptables -L INPUT -n -v` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo iptables -L INPUT -n -v",
+    "example": "sudo iptables -L INPUT -n -v",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-nft-list-ruleset",
+    "command": "sudo nft list ruleset",
+    "title": "Server Security details/status dekhna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo nft list ruleset` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo nft list ruleset",
+    "example": "sudo nft list ruleset",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ss-lntup",
+    "command": "sudo ss -lntup",
+    "title": "Server Security apply/deploy/run karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ss -lntup` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ss -lntup",
+    "example": "sudo ss -lntup",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-lsof-i-p-n",
+    "command": "sudo lsof -i -P -n",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo lsof -i -P -n` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo lsof -i -P -n",
+    "example": "sudo lsof -i -P -n",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-fuser-v-80-tcp",
+    "command": "sudo fuser -v 80/tcp",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo fuser -v 80/tcp` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo fuser -v 80/tcp",
+    "example": "sudo fuser -v 80/tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/ssl-certbot.commands.json b/public/data/ssl-certbot.commands.json
index 696e4be..5bde972 100644
--- a/public/data/ssl-certbot.commands.json
+++ b/public/data/ssl-certbot.commands.json
@@ -214,5 +214,271 @@
       "cmd",
       "devops"
     ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certificates-cert-name-example-com",
+    "command": "sudo certbot certificates --cert-name example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certificates --cert-name example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo certbot certificates --cert-name example.com",
+    "example": "sudo certbot certificates --cert-name example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-renew-cert-name-example-com",
+    "command": "sudo certbot renew --cert-name example.com",
+    "title": "SSL / Certbot resource create karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot renew --cert-name example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo certbot renew --cert-name example.com",
+    "example": "sudo certbot renew --cert-name example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-revoke-cert-path-etc-letsencrypt-live-example-com-cert-pem",
+    "command": "sudo certbot revoke --cert-path /etc/letsencrypt/live/example.com/cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot revoke --cert-path /etc/letsencrypt/live/example.com/cert.pem` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo certbot revoke --cert-path /etc/letsencrypt/live/example.com/cert.pem",
+    "example": "sudo certbot revoke --cert-path /etc/letsencrypt/live/example.com/cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certonly-webroot-w-var-www-html-d-example-com",
+    "command": "sudo certbot certonly --webroot -w /var/www/html -d example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certonly --webroot -w /var/www/html -d example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo certbot certonly --webroot -w /var/www/html -d example.com",
+    "example": "sudo certbot certonly --webroot -w /var/www/html -d example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certonly-dns-cloudflare-d-example-com",
+    "command": "sudo certbot certonly --dns-cloudflare -d example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certonly --dns-cloudflare -d example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo certbot certonly --dns-cloudflare -d example.com",
+    "example": "sudo certbot certonly --dns-cloudflare -d example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-openssl-x509-enddate-noout-in-etc-letsencrypt-live-example-com-cert-pem",
+    "command": "sudo openssl x509 -enddate -noout -in /etc/letsencrypt/live/example.com/cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo openssl x509 -enddate -noout -in /etc/letsencrypt/live/example.com/cert.pem` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo openssl x509 -enddate -noout -in /etc/letsencrypt/live/example.com/cert.pem",
+    "example": "sudo openssl x509 -enddate -noout -in /etc/letsencrypt/live/example.com/cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-req-new-newkey-rsa-2048-nodes-keyout-key-pem-out-csr-pem",
+    "command": "openssl req -new -newkey rsa:2048 -nodes -keyout key.pem -out csr.pem",
+    "title": "SSL / Certbot resource create karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl req -new -newkey rsa:2048 -nodes -keyout key.pem -out csr.pem` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "openssl req -new -newkey rsa:2048 -nodes -keyout key.pem -out csr.pem",
+    "example": "openssl req -new -newkey rsa:2048 -nodes -keyout key.pem -out csr.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-rsa-in-key-pem-check",
+    "command": "openssl rsa -in key.pem -check",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl rsa -in key.pem -check` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "openssl rsa -in key.pem -check",
+    "example": "openssl rsa -in key.pem -check",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-dhparam-out-dhparam-pem-2048",
+    "command": "openssl dhparam -out dhparam.pem 2048",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl dhparam -out dhparam.pem 2048` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "openssl dhparam -out dhparam.pem 2048",
+    "example": "openssl dhparam -out dhparam.pem 2048",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-testssl-sh-https-example-com",
+    "command": "testssl.sh https://example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `testssl.sh https://example.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "testssl.sh https://example.com",
+    "example": "testssl.sh https://example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "testssl.sh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-curl-resolve-example-com-443-127-0-0-1-https-example-com-iv",
+    "command": "curl --resolve example.com:443:127.0.0.1 https://example.com -Iv",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `curl --resolve example.com:443:127.0.0.1 https://example.com -Iv` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "curl --resolve example.com:443:127.0.0.1 https://example.com -Iv",
+    "example": "curl --resolve example.com:443:127.0.0.1 https://example.com -Iv",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-ls-lah-etc-letsencrypt-live",
+    "command": "sudo ls -lah /etc/letsencrypt/live",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo ls -lah /etc/letsencrypt/live` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ls -lah /etc/letsencrypt/live",
+    "example": "sudo ls -lah /etc/letsencrypt/live",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-ls-lah-etc-letsencrypt-renewal",
+    "command": "sudo ls -lah /etc/letsencrypt/renewal",
+    "title": "SSL / Certbot resource create karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo ls -lah /etc/letsencrypt/renewal` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo ls -lah /etc/letsencrypt/renewal",
+    "example": "sudo ls -lah /etc/letsencrypt/renewal",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-systemctl-list-timers-grep-certbot",
+    "command": "sudo systemctl list-timers | grep certbot",
+    "title": "SSL / Certbot details/status dekhna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo systemctl list-timers | grep certbot` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sudo systemctl list-timers | grep certbot",
+    "example": "sudo systemctl list-timers | grep certbot",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/system-monitoring.commands.json b/public/data/system-monitoring.commands.json
index f1c4d88..6ed9d01 100644
--- a/public/data/system-monitoring.commands.json
+++ b/public/data/system-monitoring.commands.json
@@ -322,5 +322,613 @@
       "watch",
       "devops"
     ]
+  },
+  {
+    "id": "system-monitoring-mpstat-1-5",
+    "command": "mpstat 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `mpstat 1 5` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mpstat 1 5",
+    "example": "mpstat 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "mpstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pidstat-1-5",
+    "command": "pidstat 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `pidstat 1 5` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "pidstat 1 5",
+    "example": "pidstat 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pidstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-iotop",
+    "command": "iotop",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `iotop` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "iotop",
+    "example": "iotop",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "iotop",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-iftop",
+    "command": "iftop",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `iftop` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "iftop",
+    "example": "iftop",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "iftop",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-nload",
+    "command": "nload",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `nload` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "nload",
+    "example": "nload",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "nload",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ncdu",
+    "command": "ncdu /",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `ncdu /` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ncdu /",
+    "example": "ncdu /",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ncdu",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-dstat",
+    "command": "dstat",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `dstat` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dstat",
+    "example": "dstat",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "dstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-glances",
+    "command": "glances",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `glances` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "glances",
+    "example": "glances",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "glances",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-uptime-p",
+    "command": "uptime -p",
+    "title": "System Monitoring apply/deploy/run karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `uptime -p` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "uptime -p",
+    "example": "uptime -p",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "uptime",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-loadavg",
+    "command": "cat /proc/loadavg",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `cat /proc/loadavg` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cat /proc/loadavg",
+    "example": "cat /proc/loadavg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-meminfo",
+    "command": "cat /proc/meminfo",
+    "title": "System Monitoring details/status dekhna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `cat /proc/meminfo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cat /proc/meminfo",
+    "example": "cat /proc/meminfo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-cpuinfo",
+    "command": "cat /proc/cpuinfo",
+    "title": "System Monitoring details/status dekhna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `cat /proc/cpuinfo` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "cat /proc/cpuinfo",
+    "example": "cat /proc/cpuinfo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lscpu",
+    "command": "lscpu",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `lscpu` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "lscpu",
+    "example": "lscpu",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lscpu",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lsmem",
+    "command": "lsmem",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `lsmem` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "lsmem",
+    "example": "lsmem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lsmem",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lsusb",
+    "command": "lsusb",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `lsusb` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "lsusb",
+    "example": "lsusb",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lsusb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lspci",
+    "command": "lspci",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `lspci` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "lspci",
+    "example": "lspci",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lspci",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-sensors",
+    "command": "sensors",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Beginner",
+    "use_case": "System Monitoring me `sensors` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "sensors",
+    "example": "sensors",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "sensors",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-journalctl-disk-usage",
+    "command": "journalctl --disk-usage",
+    "title": "System Monitoring logs/debugging dekhna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `journalctl --disk-usage` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "journalctl --disk-usage",
+    "example": "journalctl --disk-usage",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-systemd-analyze",
+    "command": "systemd-analyze",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `systemd-analyze` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "systemd-analyze",
+    "example": "systemd-analyze",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "systemd-analyze",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-systemd-analyze-blame",
+    "command": "systemd-analyze blame",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `systemd-analyze blame` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "systemd-analyze blame",
+    "example": "systemd-analyze blame",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "systemd-analyze",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-systemd-analyze-critical-chain",
+    "command": "systemd-analyze critical-chain",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `systemd-analyze critical-chain` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "systemd-analyze critical-chain",
+    "example": "systemd-analyze critical-chain",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "systemd-analyze",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ls-lh-var-log",
+    "command": "ls -lh /var/log",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ls -lh /var/log` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ls -lh /var/log",
+    "example": "ls -lh /var/log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ls",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-find-var-log-type-f-size-100m",
+    "command": "find /var/log -type f -size +100M",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `find /var/log -type f -size +100M` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "find /var/log -type f -size +100M",
+    "example": "find /var/log -type f -size +100M",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "find",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ss-s",
+    "command": "ss -s",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ss -s` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ss -s",
+    "example": "ss -s",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ss",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ip-addr",
+    "command": "ip addr",
+    "title": "System Monitoring resource create karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ip addr` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ip addr",
+    "example": "ip addr",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ip-route",
+    "command": "ip route",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ip route` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ip route",
+    "example": "ip route",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ethtool-eth0",
+    "command": "ethtool eth0",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ethtool eth0` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ethtool eth0",
+    "example": "ethtool eth0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ethtool",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-dig-short-domain-com",
+    "command": "dig +short domain.com",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `dig +short domain.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "dig +short domain.com",
+    "example": "dig +short domain.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-mtr-domain-com",
+    "command": "mtr domain.com",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `mtr domain.com` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "mtr domain.com",
+    "example": "mtr domain.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "mtr",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-tcpdump-i-eth0-port-80",
+    "command": "tcpdump -i eth0 port 80",
+    "title": "System Monitoring backup/export lena",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `tcpdump -i eth0 port 80` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "tcpdump -i eth0 port 80",
+    "example": "tcpdump -i eth0 port 80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "tcpdump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-strace-p-pid",
+    "command": "strace -p <pid>",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `strace -p <pid>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "strace -p <pid>",
+    "example": "strace -p <pid>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "strace",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ltrace-p-pid",
+    "command": "ltrace -p <pid>",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ltrace -p <pid>` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "ltrace -p <pid>",
+    "example": "ltrace -p <pid>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ltrace",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/terraform.commands.json b/public/data/terraform.commands.json
index ad4cb17..473493b 100644
--- a/public/data/terraform.commands.json
+++ b/public/data/terraform.commands.json
@@ -340,5 +340,665 @@
       "terraform",
       "devops"
     ]
+  },
+  {
+    "id": "terraform-terraform-init-upgrade",
+    "command": "terraform init -upgrade",
+    "title": "Terraform resource create karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform init -upgrade` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform init -upgrade",
+    "example": "terraform init -upgrade",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-init-backend-config-backend-hcl",
+    "command": "terraform init -backend-config=backend.hcl",
+    "title": "Terraform resource create karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform init -backend-config=backend.hcl` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform init -backend-config=backend.hcl",
+    "example": "terraform init -backend-config=backend.hcl",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-fmt-recursive",
+    "command": "terraform fmt -recursive",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform fmt -recursive` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform fmt -recursive",
+    "example": "terraform fmt -recursive",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-validate-json",
+    "command": "terraform validate -json",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform validate -json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform validate -json",
+    "example": "terraform validate -json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-var-file-prod-tfvars",
+    "command": "terraform plan -var-file=prod.tfvars",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform plan -var-file=prod.tfvars` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform plan -var-file=prod.tfvars",
+    "example": "terraform plan -var-file=prod.tfvars",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-apply-auto-approve",
+    "command": "terraform apply -auto-approve",
+    "title": "Terraform apply/deploy/run karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform apply -auto-approve` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform apply -auto-approve",
+    "example": "terraform apply -auto-approve",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-destroy-target-resource-name",
+    "command": "terraform destroy -target resource.name",
+    "title": "Terraform delete/destroy action chalana",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform destroy -target resource.name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform destroy -target resource.name",
+    "example": "terraform destroy -target resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-output-json",
+    "command": "terraform output -json",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform output -json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform output -json",
+    "example": "terraform output -json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-pull",
+    "command": "terraform state pull",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform state pull` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform state pull",
+    "example": "terraform state pull",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-push-state-tfstate",
+    "command": "terraform state push state.tfstate",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform state push state.tfstate` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform state push state.tfstate",
+    "example": "terraform state push state.tfstate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-mv-old-new",
+    "command": "terraform state mv old new",
+    "title": "Terraform resource create karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform state mv old new` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform state mv old new",
+    "example": "terraform state mv old new",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-rm-resource-name",
+    "command": "terraform state rm resource.name",
+    "title": "Terraform delete/destroy action chalana",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform state rm resource.name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform state rm resource.name",
+    "example": "terraform state rm resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-replace-provider-old-new",
+    "command": "terraform state replace-provider old new",
+    "title": "Terraform resource create karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform state replace-provider old new` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform state replace-provider old new",
+    "example": "terraform state replace-provider old new",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-force-unlock-lock-id",
+    "command": "terraform force-unlock LOCK_ID",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform force-unlock LOCK_ID` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform force-unlock LOCK_ID",
+    "example": "terraform force-unlock LOCK_ID",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-workspace-show",
+    "command": "terraform workspace show",
+    "title": "Terraform details/status dekhna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform workspace show` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform workspace show",
+    "example": "terraform workspace show",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-workspace-delete-dev",
+    "command": "terraform workspace delete dev",
+    "title": "Terraform delete/destroy action chalana",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform workspace delete dev` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform workspace delete dev",
+    "example": "terraform workspace delete dev",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-providers-lock",
+    "command": "terraform providers lock",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform providers lock` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform providers lock",
+    "example": "terraform providers lock",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-providers-schema-json",
+    "command": "terraform providers schema -json",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform providers schema -json` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform providers schema -json",
+    "example": "terraform providers schema -json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-graph",
+    "command": "terraform graph",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform graph` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform graph",
+    "example": "terraform graph",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-console",
+    "command": "terraform console",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform console` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform console",
+    "example": "terraform console",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-taint-resource-name",
+    "command": "terraform taint resource.name",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform taint resource.name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform taint resource.name",
+    "example": "terraform taint resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-untaint-resource-name",
+    "command": "terraform untaint resource.name",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform untaint resource.name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform untaint resource.name",
+    "example": "terraform untaint resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-test",
+    "command": "terraform test",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform test` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform test",
+    "example": "terraform test",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-login",
+    "command": "terraform login",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform login` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform login",
+    "example": "terraform login",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-logout",
+    "command": "terraform logout",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform logout` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform logout",
+    "example": "terraform logout",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-destroy",
+    "command": "terraform plan -destroy",
+    "title": "Terraform delete/destroy action chalana",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -destroy` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform plan -destroy",
+    "example": "terraform plan -destroy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-apply-replace-resource-name",
+    "command": "terraform apply -replace=resource.name",
+    "title": "Terraform apply/deploy/run karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform apply -replace=resource.name` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform apply -replace=resource.name",
+    "example": "terraform apply -replace=resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-import-aws-instance-web-i-xxxx",
+    "command": "terraform import aws_instance.web i-xxxx",
+    "title": "Terraform restore/import karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform import aws_instance.web i-xxxx` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform import aws_instance.web i-xxxx",
+    "example": "terraform import aws_instance.web i-xxxx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-show-json-tfplan",
+    "command": "terraform show -json tfplan",
+    "title": "Terraform details/status dekhna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform show -json tfplan` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform show -json tfplan",
+    "example": "terraform show -json tfplan",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-metadata-functions",
+    "command": "terraform metadata functions",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform metadata functions` command real-world DevOps, troubleshooting, deployment ya operations work ke liye use hota hai.",
+    "syntax": "terraform metadata functions",
+    "example": "terraform metadata functions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, IDs, names, logs, resources ya result details verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
   }
 ]
