diff --git a/public/data/ansible.commands.json b/public/data/ansible.commands.json
index 025cfea..af02581 100644
--- a/public/data/ansible.commands.json
+++ b/public/data/ansible.commands.json
@@ -741,5 +741,585 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "ansible-ansible-all-list-hosts",
+    "command": "ansible all --list-hosts",
+    "title": "Ansible status/details verify karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all --list-hosts` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all --list-hosts",
+    "example": "ansible all --list-hosts",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-web-m-ping-i-inventory-ini",
+    "command": "ansible web -m ping -i inventory.ini",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible web -m ping -i inventory.ini` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible web -m ping -i inventory.ini",
+    "example": "ansible web -m ping -i inventory.ini",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-setup-a-filter-ansible-distribution",
+    "command": "ansible all -m setup -a \"filter=ansible_distribution*\"",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m setup -a \"filter=ansible_distribution*\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m setup -a \"filter=ansible_distribution*\"",
+    "example": "ansible all -m setup -a \"filter=ansible_distribution*\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-setup-a-filter-ansible-default-ipv4",
+    "command": "ansible all -m setup -a \"filter=ansible_default_ipv4\"",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m setup -a \"filter=ansible_default_ipv4\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m setup -a \"filter=ansible_default_ipv4\"",
+    "example": "ansible all -m setup -a \"filter=ansible_default_ipv4\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-command-a-hostname",
+    "command": "ansible all -m command -a \"hostname\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m command -a \"hostname\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m command -a \"hostname\"",
+    "example": "ansible all -m command -a \"hostname\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-shell-a-uptime-forks-20",
+    "command": "ansible all -m shell -a \"uptime\" --forks 20",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m shell -a \"uptime\" --forks 20` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m shell -a \"uptime\" --forks 20",
+    "example": "ansible all -m shell -a \"uptime\" --forks 20",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-raw-a-uptime",
+    "command": "ansible all -m raw -a \"uptime\"",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m raw -a \"uptime\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m raw -a \"uptime\"",
+    "example": "ansible all -m raw -a \"uptime\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-stat-a-path-etc-passwd",
+    "command": "ansible all -m stat -a \"path=/etc/passwd\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m stat -a \"path=/etc/passwd\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m stat -a \"path=/etc/passwd\"",
+    "example": "ansible all -m stat -a \"path=/etc/passwd\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-fetch-a-src-var-log-syslog-dest-logs-flat-yes",
+    "command": "ansible all -m fetch -a \"src=/var/log/syslog dest=logs/ flat=yes\"",
+    "title": "Ansible logs/debugging command",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m fetch -a \"src=/var/log/syslog dest=logs/ flat=yes\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m fetch -a \"src=/var/log/syslog dest=logs/ flat=yes\"",
+    "example": "ansible all -m fetch -a \"src=/var/log/syslog dest=logs/ flat=yes\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-synchronize-a-src-app-dest-var-www-app",
+    "command": "ansible all -m synchronize -a \"src=./app dest=/var/www/app\"",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m synchronize -a \"src=./app dest=/var/www/app\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m synchronize -a \"src=./app dest=/var/www/app\"",
+    "example": "ansible all -m synchronize -a \"src=./app dest=/var/www/app\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-lineinfile-a-path-etc-hosts-line-1-2-3-4-app-become",
+    "command": "ansible all -m lineinfile -a \"path=/etc/hosts line='1.2.3.4 app'\" --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m lineinfile -a \"path=/etc/hosts line='1.2.3.4 app'\" --become` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m lineinfile -a \"path=/etc/hosts line='1.2.3.4 app'\" --become",
+    "example": "ansible all -m lineinfile -a \"path=/etc/hosts line='1.2.3.4 app'\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-template-a-src-config-j2-dest-etc-app-conf-become",
+    "command": "ansible all -m template -a \"src=config.j2 dest=/etc/app.conf\" --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Beginner",
+    "use_case": "Ansible me `ansible all -m template -a \"src=config.j2 dest=/etc/app.conf\" --become` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m template -a \"src=config.j2 dest=/etc/app.conf\" --become",
+    "example": "ansible all -m template -a \"src=config.j2 dest=/etc/app.conf\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-cron-a-name-backup-minute-0-hour-2-job-backup-sh-become",
+    "command": "ansible all -m cron -a \"name=backup minute=0 hour=2 job=/backup.sh\" --become",
+    "title": "Ansible backup/export command",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible all -m cron -a \"name=backup minute=0 hour=2 job=/backup.sh\" --become` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m cron -a \"name=backup minute=0 hour=2 job=/backup.sh\" --become",
+    "example": "ansible all -m cron -a \"name=backup minute=0 hour=2 job=/backup.sh\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-package-a-name-curl-state-present-become",
+    "command": "ansible all -m package -a \"name=curl state=present\" --become",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible all -m package -a \"name=curl state=present\" --become` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m package -a \"name=curl state=present\" --become",
+    "example": "ansible all -m package -a \"name=curl state=present\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-all-m-systemd-a-name-nginx-state-restarted-enabled-yes-become",
+    "command": "ansible all -m systemd -a \"name=nginx state=restarted enabled=yes\" --become",
+    "title": "Ansible restart/reload/refresh karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible all -m systemd -a \"name=nginx state=restarted enabled=yes\" --become` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible all -m systemd -a \"name=nginx state=restarted enabled=yes\" --become",
+    "example": "ansible all -m systemd -a \"name=nginx state=restarted enabled=yes\" --become",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-list-tasks",
+    "command": "ansible-playbook playbook.yml --list-tasks",
+    "title": "Ansible status/details verify karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml --list-tasks` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml --list-tasks",
+    "example": "ansible-playbook playbook.yml --list-tasks",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-list-tags",
+    "command": "ansible-playbook playbook.yml --list-tags",
+    "title": "Ansible status/details verify karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml --list-tags` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml --list-tags",
+    "example": "ansible-playbook playbook.yml --list-tags",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-step",
+    "command": "ansible-playbook playbook.yml --step",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml --step` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml --step",
+    "example": "ansible-playbook playbook.yml --step",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-c-d",
+    "command": "ansible-playbook playbook.yml -C -D",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml -C -D` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml -C -D",
+    "example": "ansible-playbook playbook.yml -C -D",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-forks-20",
+    "command": "ansible-playbook playbook.yml --forks 20",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml --forks 20` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml --forks 20",
+    "example": "ansible-playbook playbook.yml --forks 20",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-playbook-playbook-yml-vault-password-file-vault-pass",
+    "command": "ansible-playbook playbook.yml --vault-password-file .vault-pass",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-playbook playbook.yml --vault-password-file .vault-pass` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook playbook.yml --vault-password-file .vault-pass",
+    "example": "ansible-playbook playbook.yml --vault-password-file .vault-pass",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ansible",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-vault-create-secrets-yml",
+    "command": "ansible-vault create secrets.yml",
+    "title": "Ansible resource create/generate karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-vault create secrets.yml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-vault create secrets.yml",
+    "example": "ansible-vault create secrets.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-vault",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-vault-encrypt-string-password-name-db-password",
+    "command": "ansible-vault encrypt_string \"password\" --name db_password",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-vault encrypt_string \"password\" --name db_password` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-vault encrypt_string \"password\" --name db_password",
+    "example": "ansible-vault encrypt_string \"password\" --name db_password",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-vault",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-galaxy-init-role-name",
+    "command": "ansible-galaxy init role_name",
+    "title": "Ansible resource create/generate karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-galaxy init role_name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-galaxy init role_name",
+    "example": "ansible-galaxy init role_name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-galaxy",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-galaxy-role-install-geerlingguy-nginx",
+    "command": "ansible-galaxy role install geerlingguy.nginx",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-galaxy role install geerlingguy.nginx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-galaxy role install geerlingguy.nginx",
+    "example": "ansible-galaxy role install geerlingguy.nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-galaxy",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-galaxy-collection-install-community-docker",
+    "command": "ansible-galaxy collection install community.docker",
+    "title": "Ansible deploy/run/install command",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-galaxy collection install community.docker` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-galaxy collection install community.docker",
+    "example": "ansible-galaxy collection install community.docker",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-galaxy",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-lint-playbook-yml",
+    "command": "ansible-lint playbook.yml",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-lint playbook.yml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-lint playbook.yml",
+    "example": "ansible-lint playbook.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-lint",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ansible-ansible-console-all",
+    "command": "ansible-console all",
+    "title": "Ansible command use karna",
+    "category": "Ansible",
+    "difficulty": "Intermediate",
+    "use_case": "Ansible me `ansible-console all` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-console all",
+    "example": "ansible-console all",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ansible",
+      "ansible-console",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/aws.commands.json b/public/data/aws.commands.json
index 40ad9f4..5ef47d6 100644
--- a/public/data/aws.commands.json
+++ b/public/data/aws.commands.json
@@ -1450,5 +1450,1139 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "aws-cli-aws-s3api-list-buckets",
+    "command": "aws s3api list-buckets",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3api list-buckets` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3api list-buckets",
+    "example": "aws s3api list-buckets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3api-get-bucket-location-bucket-bucket",
+    "command": "aws s3api get-bucket-location --bucket bucket",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3api get-bucket-location --bucket bucket` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3api get-bucket-location --bucket bucket",
+    "example": "aws s3api get-bucket-location --bucket bucket",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3api-get-bucket-policy-bucket-bucket",
+    "command": "aws s3api get-bucket-policy --bucket bucket",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3api get-bucket-policy --bucket bucket` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3api get-bucket-policy --bucket bucket",
+    "example": "aws s3api get-bucket-policy --bucket bucket",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3api-put-bucket-versioning-bucket-bucket-versioning-configuration-status-enabled",
+    "command": "aws s3api put-bucket-versioning --bucket bucket --versioning-configuration Status=Enabled",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3api put-bucket-versioning --bucket bucket --versioning-configuration Status=Enabled` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3api put-bucket-versioning --bucket bucket --versioning-configuration Status=Enabled",
+    "example": "aws s3api put-bucket-versioning --bucket bucket --versioning-configuration Status=Enabled",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3api-list-object-versions-bucket-bucket",
+    "command": "aws s3api list-object-versions --bucket bucket",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3api list-object-versions --bucket bucket` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3api list-object-versions --bucket bucket",
+    "example": "aws s3api list-object-versions --bucket bucket",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-s3-ls-s3-bucket-recursive-human-readable-summarize",
+    "command": "aws s3 ls s3://bucket --recursive --human-readable --summarize",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws s3 ls s3://bucket --recursive --human-readable --summarize` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws s3 ls s3://bucket --recursive --human-readable --summarize",
+    "example": "aws s3 ls s3://bucket --recursive --human-readable --summarize",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-instance-status",
+    "command": "aws ec2 describe-instance-status",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-instance-status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 describe-instance-status",
+    "example": "aws ec2 describe-instance-status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-reboot-instances-instance-ids-i-xxx",
+    "command": "aws ec2 reboot-instances --instance-ids i-xxx",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 reboot-instances --instance-ids i-xxx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 reboot-instances --instance-ids i-xxx",
+    "example": "aws ec2 reboot-instances --instance-ids i-xxx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-terminate-instances-instance-ids-i-xxx",
+    "command": "aws ec2 terminate-instances --instance-ids i-xxx",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 terminate-instances --instance-ids i-xxx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 terminate-instances --instance-ids i-xxx",
+    "example": "aws ec2 terminate-instances --instance-ids i-xxx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-create-tags-resources-i-xxx-tags-key-name-value-server",
+    "command": "aws ec2 create-tags --resources i-xxx --tags Key=Name,Value=server",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 create-tags --resources i-xxx --tags Key=Name,Value=server` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 create-tags --resources i-xxx --tags Key=Name,Value=server",
+    "example": "aws ec2 create-tags --resources i-xxx --tags Key=Name,Value=server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-network-interfaces",
+    "command": "aws ec2 describe-network-interfaces",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-network-interfaces` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 describe-network-interfaces",
+    "example": "aws ec2 describe-network-interfaces",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-nat-gateways",
+    "command": "aws ec2 describe-nat-gateways",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-nat-gateways` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 describe-nat-gateways",
+    "example": "aws ec2 describe-nat-gateways",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-internet-gateways",
+    "command": "aws ec2 describe-internet-gateways",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-internet-gateways` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 describe-internet-gateways",
+    "example": "aws ec2 describe-internet-gateways",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-describe-security-group-rules",
+    "command": "aws ec2 describe-security-group-rules",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 describe-security-group-rules` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 describe-security-group-rules",
+    "example": "aws ec2 describe-security-group-rules",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-authorize-security-group-ingress-group-id-sg-protocol-tcp-port-443-cidr-0-0-0-0-0",
+    "command": "aws ec2 authorize-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 authorize-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 authorize-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "example": "aws ec2 authorize-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ec2-revoke-security-group-ingress-group-id-sg-protocol-tcp-port-443-cidr-0-0-0-0-0",
+    "command": "aws ec2 revoke-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ec2 revoke-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ec2 revoke-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "example": "aws ec2 revoke-security-group-ingress --group-id sg --protocol tcp --port 443 --cidr 0.0.0.0/0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-describe-listeners-load-balancer-arn-arn",
+    "command": "aws elbv2 describe-listeners --load-balancer-arn arn",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 describe-listeners --load-balancer-arn arn` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws elbv2 describe-listeners --load-balancer-arn arn",
+    "example": "aws elbv2 describe-listeners --load-balancer-arn arn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-describe-rules-listener-arn-arn",
+    "command": "aws elbv2 describe-rules --listener-arn arn",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 describe-rules --listener-arn arn` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws elbv2 describe-rules --listener-arn arn",
+    "example": "aws elbv2 describe-rules --listener-arn arn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-elbv2-modify-target-group-target-group-arn-arn-health-check-path-health",
+    "command": "aws elbv2 modify-target-group --target-group-arn arn --health-check-path /health",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws elbv2 modify-target-group --target-group-arn arn --health-check-path /health` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws elbv2 modify-target-group --target-group-arn arn --health-check-path /health",
+    "example": "aws elbv2 modify-target-group --target-group-arn arn --health-check-path /health",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-autoscaling-set-desired-capacity-auto-scaling-group-name-asg-desired-capacity-2",
+    "command": "aws autoscaling set-desired-capacity --auto-scaling-group-name asg --desired-capacity 2",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws autoscaling set-desired-capacity --auto-scaling-group-name asg --desired-capacity 2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws autoscaling set-desired-capacity --auto-scaling-group-name asg --desired-capacity 2",
+    "example": "aws autoscaling set-desired-capacity --auto-scaling-group-name asg --desired-capacity 2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-describe-task-definition-task-definition-task",
+    "command": "aws ecs describe-task-definition --task-definition task",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs describe-task-definition --task-definition task` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecs describe-task-definition --task-definition task",
+    "example": "aws ecs describe-task-definition --task-definition task",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-register-task-definition-cli-input-json-file-task-json",
+    "command": "aws ecs register-task-definition --cli-input-json file://task.json",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs register-task-definition --cli-input-json file://task.json` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecs register-task-definition --cli-input-json file://task.json",
+    "example": "aws ecs register-task-definition --cli-input-json file://task.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-run-task-cluster-cluster-task-definition-task",
+    "command": "aws ecs run-task --cluster cluster --task-definition task",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs run-task --cluster cluster --task-definition task` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecs run-task --cluster cluster --task-definition task",
+    "example": "aws ecs run-task --cluster cluster --task-definition task",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecs-execute-command-cluster-cluster-task-task-container-app-interactive-command-sh",
+    "command": "aws ecs execute-command --cluster cluster --task task --container app --interactive --command sh",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecs execute-command --cluster cluster --task task --container app --interactive --command sh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecs execute-command --cluster cluster --task task --container app --interactive --command sh",
+    "example": "aws ecs execute-command --cluster cluster --task task --container app --interactive --command sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecr-create-repository-repository-name-repo",
+    "command": "aws ecr create-repository --repository-name repo",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecr create-repository --repository-name repo` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecr create-repository --repository-name repo",
+    "example": "aws ecr create-repository --repository-name repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ecr-delete-image-repository-name-repo-image-ids-imagetag-old",
+    "command": "aws ecr delete-image --repository-name repo --image-ids imageTag=old",
+    "title": "AWS CLI risky cleanup/delete command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws ecr delete-image --repository-name repo --image-ids imageTag=old` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ecr delete-image --repository-name repo --image-ids imageTag=old",
+    "example": "aws ecr delete-image --repository-name repo --image-ids imageTag=old",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-logs-describe-log-streams-log-group-name-group",
+    "command": "aws logs describe-log-streams --log-group-name group",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws logs describe-log-streams --log-group-name group` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws logs describe-log-streams --log-group-name group",
+    "example": "aws logs describe-log-streams --log-group-name group",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-logs-get-log-events-log-group-name-group-log-stream-name-stream",
+    "command": "aws logs get-log-events --log-group-name group --log-stream-name stream",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws logs get-log-events --log-group-name group --log-stream-name stream` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws logs get-log-events --log-group-name group --log-stream-name stream",
+    "example": "aws logs get-log-events --log-group-name group --log-stream-name stream",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudwatch-put-metric-alarm-alarm-name-name",
+    "command": "aws cloudwatch put-metric-alarm --alarm-name name",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws cloudwatch put-metric-alarm --alarm-name name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws cloudwatch put-metric-alarm --alarm-name name",
+    "example": "aws cloudwatch put-metric-alarm --alarm-name name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-start-db-instance-db-instance-identifier-db",
+    "command": "aws rds start-db-instance --db-instance-identifier db",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds start-db-instance --db-instance-identifier db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws rds start-db-instance --db-instance-identifier db",
+    "example": "aws rds start-db-instance --db-instance-identifier db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-stop-db-instance-db-instance-identifier-db",
+    "command": "aws rds stop-db-instance --db-instance-identifier db",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds stop-db-instance --db-instance-identifier db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws rds stop-db-instance --db-instance-identifier db",
+    "example": "aws rds stop-db-instance --db-instance-identifier db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-reboot-db-instance-db-instance-identifier-db",
+    "command": "aws rds reboot-db-instance --db-instance-identifier db",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds reboot-db-instance --db-instance-identifier db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws rds reboot-db-instance --db-instance-identifier db",
+    "example": "aws rds reboot-db-instance --db-instance-identifier db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-describe-db-log-files-db-instance-identifier-db",
+    "command": "aws rds describe-db-log-files --db-instance-identifier db",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds describe-db-log-files --db-instance-identifier db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws rds describe-db-log-files --db-instance-identifier db",
+    "example": "aws rds describe-db-log-files --db-instance-identifier db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-rds-download-db-log-file-portion-db-instance-identifier-db-log-file-name-error-postgresql-log",
+    "command": "aws rds download-db-log-file-portion --db-instance-identifier db --log-file-name error/postgresql.log",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws rds download-db-log-file-portion --db-instance-identifier db --log-file-name error/postgresql.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws rds download-db-log-file-portion --db-instance-identifier db --log-file-name error/postgresql.log",
+    "example": "aws rds download-db-log-file-portion --db-instance-identifier db --log-file-name error/postgresql.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-lambda-get-function-function-name-fn",
+    "command": "aws lambda get-function --function-name fn",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws lambda get-function --function-name fn` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws lambda get-function --function-name fn",
+    "example": "aws lambda get-function --function-name fn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-lambda-update-function-code-function-name-fn-zip-file-fileb-function-zip",
+    "command": "aws lambda update-function-code --function-name fn --zip-file fileb://function.zip",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws lambda update-function-code --function-name fn --zip-file fileb://function.zip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws lambda update-function-code --function-name fn --zip-file fileb://function.zip",
+    "example": "aws lambda update-function-code --function-name fn --zip-file fileb://function.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-lambda-update-function-configuration-function-name-fn-environment-variables-key-value",
+    "command": "aws lambda update-function-configuration --function-name fn --environment Variables={KEY=value}",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws lambda update-function-configuration --function-name fn --environment Variables={KEY=value}` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws lambda update-function-configuration --function-name fn --environment Variables={KEY=value}",
+    "example": "aws lambda update-function-configuration --function-name fn --environment Variables={KEY=value}",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-list-attached-user-policies-user-name-user",
+    "command": "aws iam list-attached-user-policies --user-name user",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Intermediate",
+    "use_case": "AWS CLI me `aws iam list-attached-user-policies --user-name user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws iam list-attached-user-policies --user-name user",
+    "example": "aws iam list-attached-user-policies --user-name user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-list-access-keys-user-name-user",
+    "command": "aws iam list-access-keys --user-name user",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws iam list-access-keys --user-name user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws iam list-access-keys --user-name user",
+    "example": "aws iam list-access-keys --user-name user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-create-access-key-user-name-user",
+    "command": "aws iam create-access-key --user-name user",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws iam create-access-key --user-name user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws iam create-access-key --user-name user",
+    "example": "aws iam create-access-key --user-name user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-iam-delete-access-key-user-name-user-access-key-id-key",
+    "command": "aws iam delete-access-key --user-name user --access-key-id key",
+    "title": "AWS CLI risky cleanup/delete command",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws iam delete-access-key --user-name user --access-key-id key` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws iam delete-access-key --user-name user --access-key-id key",
+    "example": "aws iam delete-access-key --user-name user --access-key-id key",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-secretsmanager-create-secret-name-name-secret-string-value",
+    "command": "aws secretsmanager create-secret --name name --secret-string value",
+    "title": "AWS CLI resource create/generate karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws secretsmanager create-secret --name name --secret-string value` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws secretsmanager create-secret --name name --secret-string value",
+    "example": "aws secretsmanager create-secret --name name --secret-string value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-secretsmanager-put-secret-value-secret-id-name-secret-string-value",
+    "command": "aws secretsmanager put-secret-value --secret-id name --secret-string value",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws secretsmanager put-secret-value --secret-id name --secret-string value` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws secretsmanager put-secret-value --secret-id name --secret-string value",
+    "example": "aws secretsmanager put-secret-value --secret-id name --secret-string value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-ssm-put-parameter-name-name-value-value-type-securestring-overwrite",
+    "command": "aws ssm put-parameter --name name --value value --type SecureString --overwrite",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws ssm put-parameter --name name --value value --type SecureString --overwrite` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws ssm put-parameter --name name --value value --type SecureString --overwrite",
+    "example": "aws ssm put-parameter --name name --value value --type SecureString --overwrite",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudformation-validate-template-template-body-file-template-yml",
+    "command": "aws cloudformation validate-template --template-body file://template.yml",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws cloudformation validate-template --template-body file://template.yml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws cloudformation validate-template --template-body file://template.yml",
+    "example": "aws cloudformation validate-template --template-body file://template.yml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudformation-deploy-template-file-template-yml-stack-name-stack",
+    "command": "aws cloudformation deploy --template-file template.yml --stack-name stack",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws cloudformation deploy --template-file template.yml --stack-name stack` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws cloudformation deploy --template-file template.yml --stack-name stack",
+    "example": "aws cloudformation deploy --template-file template.yml --stack-name stack",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-cloudformation-delete-stack-stack-name-stack",
+    "command": "aws cloudformation delete-stack --stack-name stack",
+    "title": "AWS CLI risky cleanup/delete command",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws cloudformation delete-stack --stack-name stack` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws cloudformation delete-stack --stack-name stack",
+    "example": "aws cloudformation delete-stack --stack-name stack",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-route53-change-resource-record-sets-hosted-zone-id-z-change-batch-file-change-json",
+    "command": "aws route53 change-resource-record-sets --hosted-zone-id Z --change-batch file://change.json",
+    "title": "AWS CLI command use karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws route53 change-resource-record-sets --hosted-zone-id Z --change-batch file://change.json` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws route53 change-resource-record-sets --hosted-zone-id Z --change-batch file://change.json",
+    "example": "aws route53 change-resource-record-sets --hosted-zone-id Z --change-batch file://change.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-acm-describe-certificate-certificate-arn-arn",
+    "command": "aws acm describe-certificate --certificate-arn arn",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws acm describe-certificate --certificate-arn arn` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws acm describe-certificate --certificate-arn arn",
+    "example": "aws acm describe-certificate --certificate-arn arn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-describe-cluster-name-cluster",
+    "command": "aws eks describe-cluster --name cluster",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws eks describe-cluster --name cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws eks describe-cluster --name cluster",
+    "example": "aws eks describe-cluster --name cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-list-nodegroups-cluster-name-cluster",
+    "command": "aws eks list-nodegroups --cluster-name cluster",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws eks list-nodegroups --cluster-name cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws eks list-nodegroups --cluster-name cluster",
+    "example": "aws eks list-nodegroups --cluster-name cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-describe-nodegroup-cluster-name-cluster-nodegroup-name-ng",
+    "command": "aws eks describe-nodegroup --cluster-name cluster --nodegroup-name ng",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws eks describe-nodegroup --cluster-name cluster --nodegroup-name ng` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws eks describe-nodegroup --cluster-name cluster --nodegroup-name ng",
+    "example": "aws eks describe-nodegroup --cluster-name cluster --nodegroup-name ng",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-update-kubeconfig-name-cluster",
+    "command": "aws eks update-kubeconfig --name cluster",
+    "title": "AWS CLI deploy/run/install command",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws eks update-kubeconfig --name cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws eks update-kubeconfig --name cluster",
+    "example": "aws eks update-kubeconfig --name cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "aws-cli-aws-eks-get-token-cluster-name-cluster",
+    "command": "aws eks get-token --cluster-name cluster",
+    "title": "AWS CLI status/details verify karna",
+    "category": "AWS CLI",
+    "difficulty": "Advanced",
+    "use_case": "AWS CLI me `aws eks get-token --cluster-name cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aws eks get-token --cluster-name cluster",
+    "example": "aws eks get-token --cluster-name cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "aws configure list",
+      "aws sts get-caller-identity"
+    ],
+    "tags": [
+      "aws",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/azure.commands.json b/public/data/azure.commands.json
index d613f97..2449b34 100644
--- a/public/data/azure.commands.json
+++ b/public/data/azure.commands.json
@@ -990,5 +990,819 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "azure-cli-az-cloud-list",
+    "command": "az cloud list",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az cloud list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az cloud list",
+    "example": "az cloud list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-configure-defaults-group-rg-location-eastus",
+    "command": "az configure --defaults group=rg location=eastus",
+    "title": "Azure CLI deploy/run/install command",
+    "category": "Azure CLI",
+    "difficulty": "Beginner",
+    "use_case": "Azure CLI me `az configure --defaults group=rg location=eastus` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az configure --defaults group=rg location=eastus",
+    "example": "az configure --defaults group=rg location=eastus",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-account-get-access-token",
+    "command": "az account get-access-token",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az account get-access-token` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az account get-access-token",
+    "example": "az account get-access-token",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-group-exists-n-rg",
+    "command": "az group exists -n rg",
+    "title": "Azure CLI deploy/run/install command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az group exists -n rg` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az group exists -n rg",
+    "example": "az group exists -n rg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-deallocate-g-rg-n-vm",
+    "command": "az vm deallocate -g rg -n vm",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az vm deallocate -g rg -n vm` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az vm deallocate -g rg -n vm",
+    "example": "az vm deallocate -g rg -n vm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-restart-g-rg-n-vm",
+    "command": "az vm restart -g rg -n vm",
+    "title": "Azure CLI restart/reload/refresh karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az vm restart -g rg -n vm` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az vm restart -g rg -n vm",
+    "example": "az vm restart -g rg -n vm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-delete-g-rg-n-vm",
+    "command": "az vm delete -g rg -n vm",
+    "title": "Azure CLI risky cleanup/delete command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az vm delete -g rg -n vm` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az vm delete -g rg -n vm",
+    "example": "az vm delete -g rg -n vm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-create-g-rg-n-vm-image-ubuntu2204-admin-username-azureuser",
+    "command": "az vm create -g rg -n vm --image Ubuntu2204 --admin-username azureuser",
+    "title": "Azure CLI resource create/generate karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az vm create -g rg -n vm --image Ubuntu2204 --admin-username azureuser` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az vm create -g rg -n vm --image Ubuntu2204 --admin-username azureuser",
+    "example": "az vm create -g rg -n vm --image Ubuntu2204 --admin-username azureuser",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-vm-extension-list-g-rg-vm-name-vm",
+    "command": "az vm extension list -g rg --vm-name vm",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az vm extension list -g rg --vm-name vm` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az vm extension list -g rg --vm-name vm",
+    "example": "az vm extension list -g rg --vm-name vm",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-public-ip-show-g-rg-n-ip",
+    "command": "az network public-ip show -g rg -n ip",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network public-ip show -g rg -n ip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network public-ip show -g rg -n ip",
+    "example": "az network public-ip show -g rg -n ip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-nsg-rule-list-g-rg-nsg-name-nsg",
+    "command": "az network nsg rule list -g rg --nsg-name nsg",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network nsg rule list -g rg --nsg-name nsg` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network nsg rule list -g rg --nsg-name nsg",
+    "example": "az network nsg rule list -g rg --nsg-name nsg",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-nsg-rule-create-g-rg-nsg-name-nsg-n-allow-http-priority-100-destination-port-ranges-80-ac",
+    "command": "az network nsg rule create -g rg --nsg-name nsg -n allow-http --priority 100 --destination-port-ranges 80 --access Allow --protocol Tcp",
+    "title": "Azure CLI resource create/generate karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network nsg rule create -g rg --nsg-name nsg -n allow-http --priority 100 --destination-port-ranges 80 --access Allow --protocol Tcp` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network nsg rule create -g rg --nsg-name nsg -n allow-http --priority 100 --destination-port-ranges 80 --access Allow --protocol Tcp",
+    "example": "az network nsg rule create -g rg --nsg-name nsg -n allow-http --priority 100 --destination-port-ranges 80 --access Allow --protocol Tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-nsg-rule-delete-g-rg-nsg-name-nsg-n-rule",
+    "command": "az network nsg rule delete -g rg --nsg-name nsg -n rule",
+    "title": "Azure CLI risky cleanup/delete command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network nsg rule delete -g rg --nsg-name nsg -n rule` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network nsg rule delete -g rg --nsg-name nsg -n rule",
+    "example": "az network nsg rule delete -g rg --nsg-name nsg -n rule",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-lb-list",
+    "command": "az network lb list",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network lb list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network lb list",
+    "example": "az network lb list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-network-application-gateway-list",
+    "command": "az network application-gateway list",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az network application-gateway list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az network application-gateway list",
+    "example": "az network application-gateway list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-deployment-list-publishing-profiles-g-rg-n-app",
+    "command": "az webapp deployment list-publishing-profiles -g rg -n app",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az webapp deployment list-publishing-profiles -g rg -n app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az webapp deployment list-publishing-profiles -g rg -n app",
+    "example": "az webapp deployment list-publishing-profiles -g rg -n app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-deployment-source-config-zip-g-rg-n-app-src-app-zip",
+    "command": "az webapp deployment source config-zip -g rg -n app --src app.zip",
+    "title": "Azure CLI deploy/run/install command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az webapp deployment source config-zip -g rg -n app --src app.zip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az webapp deployment source config-zip -g rg -n app --src app.zip",
+    "example": "az webapp deployment source config-zip -g rg -n app --src app.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-ssh-g-rg-n-app",
+    "command": "az webapp ssh -g rg -n app",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az webapp ssh -g rg -n app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az webapp ssh -g rg -n app",
+    "example": "az webapp ssh -g rg -n app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-webapp-log-config-g-rg-n-app-application-logging-filesystem",
+    "command": "az webapp log config -g rg -n app --application-logging filesystem",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az webapp log config -g rg -n app --application-logging filesystem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az webapp log config -g rg -n app --application-logging filesystem",
+    "example": "az webapp log config -g rg -n app --application-logging filesystem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-acr-build-r-registry-t-app-tag",
+    "command": "az acr build -r registry -t app:tag .",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az acr build -r registry -t app:tag .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az acr build -r registry -t app:tag .",
+    "example": "az acr build -r registry -t app:tag .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-acr-repository-delete-n-registry-repository-repo",
+    "command": "az acr repository delete -n registry --repository repo",
+    "title": "Azure CLI risky cleanup/delete command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az acr repository delete -n registry --repository repo` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az acr repository delete -n registry --repository repo",
+    "example": "az acr repository delete -n registry --repository repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-get-upgrades-g-rg-n-cluster",
+    "command": "az aks get-upgrades -g rg -n cluster",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks get-upgrades -g rg -n cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az aks get-upgrades -g rg -n cluster",
+    "example": "az aks get-upgrades -g rg -n cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-upgrade-g-rg-n-cluster-kubernetes-version-version",
+    "command": "az aks upgrade -g rg -n cluster --kubernetes-version version",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks upgrade -g rg -n cluster --kubernetes-version version` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az aks upgrade -g rg -n cluster --kubernetes-version version",
+    "example": "az aks upgrade -g rg -n cluster --kubernetes-version version",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-scale-g-rg-n-cluster-node-count-3",
+    "command": "az aks scale -g rg -n cluster --node-count 3",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks scale -g rg -n cluster --node-count 3` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az aks scale -g rg -n cluster --node-count 3",
+    "example": "az aks scale -g rg -n cluster --node-count 3",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-aks-nodepool-list-g-rg-cluster-name-cluster",
+    "command": "az aks nodepool list -g rg --cluster-name cluster",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az aks nodepool list -g rg --cluster-name cluster` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az aks nodepool list -g rg --cluster-name cluster",
+    "example": "az aks nodepool list -g rg --cluster-name cluster",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-monitor-log-analytics-workspace-list",
+    "command": "az monitor log-analytics workspace list",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az monitor log-analytics workspace list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az monitor log-analytics workspace list",
+    "example": "az monitor log-analytics workspace list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-monitor-metrics-list-resource-resourceid-metric-percentage-cpu",
+    "command": "az monitor metrics list --resource resourceId --metric \"Percentage CPU\"",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az monitor metrics list --resource resourceId --metric \"Percentage CPU\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az monitor metrics list --resource resourceId --metric \"Percentage CPU\"",
+    "example": "az monitor metrics list --resource resourceId --metric \"Percentage CPU\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-keyvault-secret-set-vault-name-vault-name-secret-value-value",
+    "command": "az keyvault secret set --vault-name vault --name secret --value value",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az keyvault secret set --vault-name vault --name secret --value value` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az keyvault secret set --vault-name vault --name secret --value value",
+    "example": "az keyvault secret set --vault-name vault --name secret --value value",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-keyvault-secret-delete-vault-name-vault-name-secret",
+    "command": "az keyvault secret delete --vault-name vault --name secret",
+    "title": "Azure CLI risky cleanup/delete command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az keyvault secret delete --vault-name vault --name secret` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az keyvault secret delete --vault-name vault --name secret",
+    "example": "az keyvault secret delete --vault-name vault --name secret",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-account-keys-list-g-rg-n-account",
+    "command": "az storage account keys list -g rg -n account",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage account keys list -g rg -n account` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az storage account keys list -g rg -n account",
+    "example": "az storage account keys list -g rg -n account",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-container-create-account-name-account-n-container",
+    "command": "az storage container create --account-name account -n container",
+    "title": "Azure CLI resource create/generate karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage container create --account-name account -n container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az storage container create --account-name account -n container",
+    "example": "az storage container create --account-name account -n container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-blob-upload-account-name-account-c-container-f-file-n-file",
+    "command": "az storage blob upload --account-name account -c container -f file -n file",
+    "title": "Azure CLI deploy/run/install command",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage blob upload --account-name account -c container -f file -n file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az storage blob upload --account-name account -c container -f file -n file",
+    "example": "az storage blob upload --account-name account -c container -f file -n file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-storage-blob-download-account-name-account-c-container-n-file-f-file",
+    "command": "az storage blob download --account-name account -c container -n file -f file",
+    "title": "Azure CLI command use karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az storage blob download --account-name account -c container -n file -f file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az storage blob download --account-name account -c container -n file -f file",
+    "example": "az storage blob download --account-name account -c container -n file -f file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-role-definition-list-name-reader",
+    "command": "az role definition list --name Reader",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az role definition list --name Reader` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az role definition list --name Reader",
+    "example": "az role definition list --name Reader",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-role-assignment-create-assignee-user-role-reader-scope-scope",
+    "command": "az role assignment create --assignee user --role Reader --scope scope",
+    "title": "Azure CLI resource create/generate karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az role assignment create --assignee user --role Reader --scope scope` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az role assignment create --assignee user --role Reader --scope scope",
+    "example": "az role assignment create --assignee user --role Reader --scope scope",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-postgres-flexible-server-show-g-rg-n-server",
+    "command": "az postgres flexible-server show -g rg -n server",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az postgres flexible-server show -g rg -n server` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az postgres flexible-server show -g rg -n server",
+    "example": "az postgres flexible-server show -g rg -n server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "azure-cli-az-mysql-flexible-server-show-g-rg-n-server",
+    "command": "az mysql flexible-server show -g rg -n server",
+    "title": "Azure CLI status/details verify karna",
+    "category": "Azure CLI",
+    "difficulty": "Intermediate",
+    "use_case": "Azure CLI me `az mysql flexible-server show -g rg -n server` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "az mysql flexible-server show -g rg -n server",
+    "example": "az mysql flexible-server show -g rg -n server",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "az account show",
+      "az account list"
+    ],
+    "tags": [
+      "azure",
+      "az",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/cicd.commands.json b/public/data/cicd.commands.json
index 1f51b18..d218b5c 100644
--- a/public/data/cicd.commands.json
+++ b/public/data/cicd.commands.json
@@ -986,5 +986,788 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "ci-cd-npm-run-typecheck",
+    "command": "npm run typecheck",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npm run typecheck` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npm run typecheck",
+    "example": "npm run typecheck",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-run-test-unit",
+    "command": "npm run test:unit",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npm run test:unit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npm run test:unit",
+    "example": "npm run test:unit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-run-test-e2e",
+    "command": "npm run test:e2e",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npm run test:e2e` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npm run test:e2e",
+    "example": "npm run test:e2e",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npm-run-coverage",
+    "command": "npm run coverage",
+    "title": "CI/CD deploy/run/install command",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npm run coverage` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npm run coverage",
+    "example": "npm run coverage",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npx-vite-build",
+    "command": "npx vite build",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npx vite build` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npx vite build",
+    "example": "npx vite build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npx",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npx-vite-preview",
+    "command": "npx vite preview",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npx vite preview` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npx vite preview",
+    "example": "npx vite preview",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npx",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npx-eslint",
+    "command": "npx eslint .",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npx eslint .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npx eslint .",
+    "example": "npx eslint .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npx",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npx-prettier-check",
+    "command": "npx prettier --check .",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npx prettier --check .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npx prettier --check .",
+    "example": "npx prettier --check .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npx",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-npx-prettier-write",
+    "command": "npx prettier --write .",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `npx prettier --write .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "npx prettier --write .",
+    "example": "npx prettier --write .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "npx",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-composer-test",
+    "command": "composer test",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `composer test` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "composer test",
+    "example": "composer test",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "composer",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-composer-audit",
+    "command": "composer audit",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `composer audit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "composer audit",
+    "example": "composer audit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "composer",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-vendor-bin-phpunit",
+    "command": "vendor/bin/phpunit",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `vendor/bin/phpunit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vendor/bin/phpunit",
+    "example": "vendor/bin/phpunit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "vendor/bin/phpunit",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-vendor-bin-pint",
+    "command": "vendor/bin/pint",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `vendor/bin/pint` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vendor/bin/pint",
+    "example": "vendor/bin/pint",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "vendor/bin/pint",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-phpstan-analyse",
+    "command": "phpstan analyse",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `phpstan analyse` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "phpstan analyse",
+    "example": "phpstan analyse",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "phpstan",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-config-clear",
+    "command": "php artisan config:clear",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan config:clear` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan config:clear",
+    "example": "php artisan config:clear",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-route-cache",
+    "command": "php artisan route:cache",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan route:cache` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan route:cache",
+    "example": "php artisan route:cache",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-view-cache",
+    "command": "php artisan view:cache",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan view:cache` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan view:cache",
+    "example": "php artisan view:cache",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-event-cache",
+    "command": "php artisan event:cache",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan event:cache` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan event:cache",
+    "example": "php artisan event:cache",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-storage-link",
+    "command": "php artisan storage:link",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan storage:link` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan storage:link",
+    "example": "php artisan storage:link",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-queue-work-tries-3",
+    "command": "php artisan queue:work --tries=3",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan queue:work --tries=3` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan queue:work --tries=3",
+    "example": "php artisan queue:work --tries=3",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-queue-failed",
+    "command": "php artisan queue:failed",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan queue:failed` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan queue:failed",
+    "example": "php artisan queue:failed",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-php-artisan-queue-retry-all",
+    "command": "php artisan queue:retry all",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `php artisan queue:retry all` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "php artisan queue:retry all",
+    "example": "php artisan queue:retry all",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "php",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-python-m-pip-install-upgrade-pip",
+    "command": "python -m pip install --upgrade pip",
+    "title": "CI/CD deploy/run/install command",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `python -m pip install --upgrade pip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "python -m pip install --upgrade pip",
+    "example": "python -m pip install --upgrade pip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "python",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-python-m-venv-venv",
+    "command": "python -m venv .venv",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `python -m venv .venv` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "python -m venv .venv",
+    "example": "python -m venv .venv",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "python",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-source-venv-bin-activate",
+    "command": "source .venv/bin/activate",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `source .venv/bin/activate` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "source .venv/bin/activate",
+    "example": "source .venv/bin/activate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "source",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-pytest-q",
+    "command": "pytest -q",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `pytest -q` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pytest -q",
+    "example": "pytest -q",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "pytest",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-ruff-check",
+    "command": "ruff check .",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `ruff check .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ruff check .",
+    "example": "ruff check .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "ruff",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-black-check",
+    "command": "black --check .",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `black --check .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "black --check .",
+    "example": "black --check .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "black",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-docker-build-target-production-t-app",
+    "command": "docker build --target production -t app .",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `docker build --target production -t app .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker build --target production -t app .",
+    "example": "docker build --target production -t app .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "ci",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-docker-compose-f-compose-yml-f-compose-prod-yml-config",
+    "command": "docker compose -f compose.yml -f compose.prod.yml config",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `docker compose -f compose.yml -f compose.prod.yml config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose -f compose.yml -f compose.prod.yml config",
+    "example": "docker compose -f compose.yml -f compose.prod.yml config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "ci",
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-kubectl-set-image-deployment-app-app-image-tag",
+    "command": "kubectl set image deployment/app app=image:tag",
+    "title": "CI/CD deploy/run/install command",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `kubectl set image deployment/app app=image:tag` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl set image deployment/app app=image:tag",
+    "example": "kubectl set image deployment/app app=image:tag",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "ci",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-kubectl-rollout-undo-deployment-app",
+    "command": "kubectl rollout undo deployment/app",
+    "title": "CI/CD restart/reload/refresh karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `kubectl rollout undo deployment/app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl rollout undo deployment/app",
+    "example": "kubectl rollout undo deployment/app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "ci",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-helm-upgrade-install-app-chart-values-values-prod-yaml",
+    "command": "helm upgrade --install app ./chart --values values-prod.yaml",
+    "title": "CI/CD deploy/run/install command",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `helm upgrade --install app ./chart --values values-prod.yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm upgrade --install app ./chart --values values-prod.yaml",
+    "example": "helm upgrade --install app ./chart --values values-prod.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "ci",
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-ansible-playbook-deploy-yml-limit-prod",
+    "command": "ansible-playbook deploy.yml --limit prod",
+    "title": "CI/CD deploy/run/install command",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `ansible-playbook deploy.yml --limit prod` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ansible-playbook deploy.yml --limit prod",
+    "example": "ansible-playbook deploy.yml --limit prod",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "ansible all -m ping",
+      "ansible-playbook --check"
+    ],
+    "tags": [
+      "ci",
+      "ansible-playbook",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-make-build",
+    "command": "make build",
+    "title": "CI/CD resource create/generate karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `make build` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "make build",
+    "example": "make build",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "make",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-make-test",
+    "command": "make test",
+    "title": "CI/CD status/details verify karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `make test` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "make test",
+    "example": "make test",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "make",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-make-deploy",
+    "command": "make deploy",
+    "title": "CI/CD resource create/generate karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `make deploy` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "make deploy",
+    "example": "make deploy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "make",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-make-rollback",
+    "command": "make rollback",
+    "title": "CI/CD resource create/generate karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `make rollback` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "make rollback",
+    "example": "make rollback",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "make",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-rsync-az-exclude-node-modules-user-host-var-www-app",
+    "command": "rsync -az --exclude node_modules ./ user@host:/var/www/app",
+    "title": "CI/CD command use karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `rsync -az --exclude node_modules ./ user@host:/var/www/app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "rsync -az --exclude node_modules ./ user@host:/var/www/app",
+    "example": "rsync -az --exclude node_modules ./ user@host:/var/www/app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "rsync",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ci-cd-ssh-user-host-sudo-supervisorctl-restart-all",
+    "command": "ssh user@host \"sudo supervisorctl restart all\"",
+    "title": "CI/CD restart/reload/refresh karna",
+    "category": "CI/CD",
+    "difficulty": "Intermediate",
+    "use_case": "CI/CD me `ssh user@host \"sudo supervisorctl restart all\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh user@host \"sudo supervisorctl restart all\"",
+    "example": "ssh user@host \"sudo supervisorctl restart all\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ci",
+      "ssh",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/database-backup.commands.json b/public/data/database-backup.commands.json
index 9f26473..8b73e4b 100644
--- a/public/data/database-backup.commands.json
+++ b/public/data/database-backup.commands.json
@@ -875,5 +875,670 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-schema-only-db-schema-sql",
+    "command": "pg_dump --schema-only db > schema.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump --schema-only db > schema.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_dump --schema-only db > schema.sql",
+    "example": "pg_dump --schema-only db > schema.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-data-only-db-data-sql",
+    "command": "pg_dump --data-only db > data.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump --data-only db > data.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_dump --data-only db > data.sql",
+    "example": "pg_dump --data-only db > data.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-t-table-name-db-table-sql",
+    "command": "pg_dump -t table_name db > table.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Beginner",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump -t table_name db > table.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_dump -t table_name db > table.sql",
+    "example": "pg_dump -t table_name db > table.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-n-schema-name-db-schema-backup-sql",
+    "command": "pg_dump -n schema_name db > schema_backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump -n schema_name db > schema_backup.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_dump -n schema_name db > schema_backup.sql",
+    "example": "pg_dump -n schema_name db > schema_backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-dump-exclude-table-logs-db-backup-sql",
+    "command": "pg_dump --exclude-table=logs db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup logs/debugging command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_dump --exclude-table=logs db > backup.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_dump --exclude-table=logs db > backup.sql",
+    "example": "pg_dump --exclude-table=logs db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_dump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-clean-if-exists-d-db-backup-dump",
+    "command": "pg_restore --clean --if-exists -d db backup.dump",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore --clean --if-exists -d db backup.dump` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_restore --clean --if-exists -d db backup.dump",
+    "example": "pg_restore --clean --if-exists -d db backup.dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-schema-only-d-db-backup-dump",
+    "command": "pg_restore --schema-only -d db backup.dump",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore --schema-only -d db backup.dump` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_restore --schema-only -d db backup.dump",
+    "example": "pg_restore --schema-only -d db backup.dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-restore-data-only-d-db-backup-dump",
+    "command": "pg_restore --data-only -d db backup.dump",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_restore --data-only -d db backup.dump` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_restore --data-only -d db backup.dump",
+    "example": "pg_restore --data-only -d db backup.dump",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_restore",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-f-backup-sql-db",
+    "command": "psql -f backup.sql db",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -f backup.sql db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -f backup.sql db",
+    "example": "psql -f backup.sql db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-v-on-error-stop-1-f-migration-sql-db",
+    "command": "psql -v ON_ERROR_STOP=1 -f migration.sql db",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -v ON_ERROR_STOP=1 -f migration.sql db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -v ON_ERROR_STOP=1 -f migration.sql db",
+    "example": "psql -v ON_ERROR_STOP=1 -f migration.sql db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-count-from-table-db",
+    "command": "psql -c \"SELECT count(*) FROM table;\" db",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT count(*) FROM table;\" db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -c \"SELECT count(*) FROM table;\" db",
+    "example": "psql -c \"SELECT count(*) FROM table;\" db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-pg-size-pretty-pg-database-size-db",
+    "command": "psql -c \"SELECT pg_size_pretty(pg_database_size('db'));\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT pg_size_pretty(pg_database_size('db'));\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -c \"SELECT pg_size_pretty(pg_database_size('db'));\"",
+    "example": "psql -c \"SELECT pg_size_pretty(pg_database_size('db'));\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-from-pg-locks",
+    "command": "psql -c \"SELECT * FROM pg_locks;\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT * FROM pg_locks;\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -c \"SELECT * FROM pg_locks;\"",
+    "example": "psql -c \"SELECT * FROM pg_locks;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-pid-query-from-pg-stat-activity",
+    "command": "psql -c \"SELECT pid, query FROM pg_stat_activity;\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT pid, query FROM pg_stat_activity;\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -c \"SELECT pid, query FROM pg_stat_activity;\"",
+    "example": "psql -c \"SELECT pid, query FROM pg_stat_activity;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-psql-c-select-pg-terminate-backend-pid-from-pg-stat-activity-where-datname-db",
+    "command": "psql -c \"SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='db';\"",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `psql -c \"SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='db';\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "psql -c \"SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='db';\"",
+    "example": "psql -c \"SELECT pg_terminate_backend(pid) FROM pg_stat_activity WHERE datname='db';\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "psql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-createdb-t-template0-newdb",
+    "command": "createdb -T template0 newdb",
+    "title": "PostgreSQL / MySQL Backup resource create/generate karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `createdb -T template0 newdb` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "createdb -T template0 newdb",
+    "example": "createdb -T template0 newdb",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "createdb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-dropdb-if-exists-olddb",
+    "command": "dropdb --if-exists olddb",
+    "title": "PostgreSQL / MySQL Backup risky cleanup/delete command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `dropdb --if-exists olddb` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dropdb --if-exists olddb",
+    "example": "dropdb --if-exists olddb",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "dropdb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-createuser-user",
+    "command": "createuser user",
+    "title": "PostgreSQL / MySQL Backup resource create/generate karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `createuser user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "createuser user",
+    "example": "createuser user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "createuser",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-dropuser-user",
+    "command": "dropuser user",
+    "title": "PostgreSQL / MySQL Backup risky cleanup/delete command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `dropuser user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dropuser user",
+    "example": "dropuser user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "dropuser",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-isready-h-localhost-p-5432",
+    "command": "pg_isready -h localhost -p 5432",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_isready -h localhost -p 5432` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_isready -h localhost -p 5432",
+    "example": "pg_isready -h localhost -p 5432",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_isready",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-pg-basebackup-d-backup-dir-ft-z-p",
+    "command": "pg_basebackup -D backup_dir -Ft -z -P",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `pg_basebackup -D backup_dir -Ft -z -P` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pg_basebackup -D backup_dir -Ft -z -P",
+    "example": "pg_basebackup -D backup_dir -Ft -z -P",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "pg_basebackup",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-show-tables-db",
+    "command": "mysql -e \"SHOW TABLES;\" db",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SHOW TABLES;\" db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysql -e \"SHOW TABLES;\" db",
+    "example": "mysql -e \"SHOW TABLES;\" db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-describe-table-db",
+    "command": "mysql -e \"DESCRIBE table;\" db",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"DESCRIBE table;\" db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysql -e \"DESCRIBE table;\" db",
+    "example": "mysql -e \"DESCRIBE table;\" db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-select-count-from-table-db",
+    "command": "mysql -e \"SELECT COUNT(*) FROM table;\" db",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SELECT COUNT(*) FROM table;\" db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysql -e \"SELECT COUNT(*) FROM table;\" db",
+    "example": "mysql -e \"SELECT COUNT(*) FROM table;\" db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-e-show-grants-for-user",
+    "command": "mysql -e \"SHOW GRANTS FOR user;\"",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql -e \"SHOW GRANTS FOR user;\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysql -e \"SHOW GRANTS FOR user;\"",
+    "example": "mysql -e \"SHOW GRANTS FOR user;\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-no-data-db-schema-sql",
+    "command": "mysqldump --no-data db > schema.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --no-data db > schema.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqldump --no-data db > schema.sql",
+    "example": "mysqldump --no-data db > schema.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-no-create-info-db-data-sql",
+    "command": "mysqldump --no-create-info db > data.sql",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --no-create-info db > data.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqldump --no-create-info db > data.sql",
+    "example": "mysqldump --no-create-info db > data.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-db-table-table-sql",
+    "command": "mysqldump db table > table.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump db table > table.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqldump db table > table.sql",
+    "example": "mysqldump db table > table.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqldump-single-transaction-quick-lock-tables-false-db-backup-sql",
+    "command": "mysqldump --single-transaction --quick --lock-tables=false db > backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqldump --single-transaction --quick --lock-tables=false db > backup.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqldump --single-transaction --quick --lock-tables=false db > backup.sql",
+    "example": "mysqldump --single-transaction --quick --lock-tables=false db > backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqldump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysql-force-db-backup-sql",
+    "command": "mysql --force db < backup.sql",
+    "title": "PostgreSQL / MySQL Backup backup/export command",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysql --force db < backup.sql` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysql --force db < backup.sql",
+    "example": "mysql --force db < backup.sql",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysql",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqladmin-ping",
+    "command": "mysqladmin ping",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqladmin ping` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqladmin ping",
+    "example": "mysqladmin ping",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqladmin",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqladmin-extended-status",
+    "command": "mysqladmin extended-status",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqladmin extended-status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqladmin extended-status",
+    "example": "mysqladmin extended-status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqladmin",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqlcheck-repair-db-table",
+    "command": "mysqlcheck --repair db table",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqlcheck --repair db table` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqlcheck --repair db table",
+    "example": "mysqlcheck --repair db table",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqlcheck",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-mysqlcheck-optimize-db",
+    "command": "mysqlcheck --optimize db",
+    "title": "PostgreSQL / MySQL Backup status/details verify karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `mysqlcheck --optimize db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mysqlcheck --optimize db",
+    "example": "mysqlcheck --optimize db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "mysqlcheck",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "postgresql-mysql-backup-perror-1062",
+    "command": "perror 1062",
+    "title": "PostgreSQL / MySQL Backup command use karna",
+    "category": "PostgreSQL / MySQL Backup",
+    "difficulty": "Intermediate",
+    "use_case": "PostgreSQL / MySQL Backup me `perror 1062` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "perror 1062",
+    "example": "perror 1062",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "postgresql",
+      "perror",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/docker.commands.json b/public/data/docker.commands.json
index b609704..bc5f296 100644
--- a/public/data/docker.commands.json
+++ b/public/data/docker.commands.json
@@ -1549,5 +1549,1545 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "docker-docker-run-rm-image-command",
+    "command": "docker run --rm image command",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --rm image command` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --rm image command",
+    "example": "docker run --rm image command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-rm-it-ubuntu-bash",
+    "command": "docker run --rm -it ubuntu bash",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --rm -it ubuntu bash` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --rm -it ubuntu bash",
+    "example": "docker run --rm -it ubuntu bash",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-e-key-value-image",
+    "command": "docker run -e KEY=value image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run -e KEY=value image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run -e KEY=value image",
+    "example": "docker run -e KEY=value image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-env-file-env-image",
+    "command": "docker run --env-file .env image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --env-file .env image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --env-file .env image",
+    "example": "docker run --env-file .env image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-v-pwd-app-image",
+    "command": "docker run -v $(pwd):/app image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run -v $(pwd):/app image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run -v $(pwd):/app image",
+    "example": "docker run -v $(pwd):/app image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-network-host-image",
+    "command": "docker run --network host image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --network host image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --network host image",
+    "example": "docker run --network host image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-memory-512m-image",
+    "command": "docker run --memory=512m image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --memory=512m image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --memory=512m image",
+    "example": "docker run --memory=512m image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-cpus-1-5-image",
+    "command": "docker run --cpus=1.5 image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --cpus=1.5 image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --cpus=1.5 image",
+    "example": "docker run --cpus=1.5 image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-restart-unless-stopped-image",
+    "command": "docker run --restart unless-stopped image",
+    "title": "Docker restart/reload/refresh karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --restart unless-stopped image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --restart unless-stopped image",
+    "example": "docker run --restart unless-stopped image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-health-cmd-curl-f-http-localhost-exit-1-image",
+    "command": "docker run --health-cmd=\"curl -f http://localhost || exit 1\" image",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker run --health-cmd=\"curl -f http://localhost || exit 1\" image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --health-cmd=\"curl -f http://localhost || exit 1\" image",
+    "example": "docker run --health-cmd=\"curl -f http://localhost || exit 1\" image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-inspect-format-state-status-container",
+    "command": "docker inspect --format=\"{{.State.Status}}\" container",
+    "title": "Docker status/details verify karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker inspect --format=\"{{.State.Status}}\" container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker inspect --format=\"{{.State.Status}}\" container",
+    "example": "docker inspect --format=\"{{.State.Status}}\" container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-inspect-format-range-networksettings-networks-ipaddress-end-container",
+    "command": "docker inspect --format=\"{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}\" container",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker inspect --format=\"{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}\" container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker inspect --format=\"{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}\" container",
+    "example": "docker inspect --format=\"{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}\" container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-logs-tail-100-container",
+    "command": "docker logs --tail=100 container",
+    "title": "Docker logs/debugging command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker logs --tail=100 container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker logs --tail=100 container",
+    "example": "docker logs --tail=100 container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-logs-since-1h-container",
+    "command": "docker logs --since=1h container",
+    "title": "Docker logs/debugging command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker logs --since=1h container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker logs --since=1h container",
+    "example": "docker logs --since=1h container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-logs-timestamps-container",
+    "command": "docker logs --timestamps container",
+    "title": "Docker logs/debugging command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker logs --timestamps container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker logs --timestamps container",
+    "example": "docker logs --timestamps container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-exec-u-root-it-container-sh",
+    "command": "docker exec -u root -it container sh",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker exec -u root -it container sh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker exec -u root -it container sh",
+    "example": "docker exec -u root -it container sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-exec-container-env",
+    "command": "docker exec container env",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker exec container env` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker exec container env",
+    "example": "docker exec container env",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-exec-container-ps-aux",
+    "command": "docker exec container ps aux",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker exec container ps aux` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker exec container ps aux",
+    "example": "docker exec container ps aux",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-exec-container-cat-etc-os-release",
+    "command": "docker exec container cat /etc/os-release",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker exec container cat /etc/os-release` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker exec container cat /etc/os-release",
+    "example": "docker exec container cat /etc/os-release",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-diff-container",
+    "command": "docker diff container",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker diff container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker diff container",
+    "example": "docker diff container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-container-ls-format-table-names-status-ports",
+    "command": "docker container ls --format \"table {{.Names}}\t{{.Status}}\t{{.Ports}}\"",
+    "title": "Docker status/details verify karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker container ls --format \"table {{.Names}}\t{{.Status}}\t{{.Ports}}\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker container ls --format \"table {{.Names}}\t{{.Status}}\t{{.Ports}}\"",
+    "example": "docker container ls --format \"table {{.Names}}\t{{.Status}}\t{{.Ports}}\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-image-ls-digests",
+    "command": "docker image ls --digests",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker image ls --digests` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker image ls --digests",
+    "example": "docker image ls --digests",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-image-inspect-image",
+    "command": "docker image inspect image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker image inspect image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker image inspect image",
+    "example": "docker image inspect image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-image-history-image",
+    "command": "docker image history image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker image history image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker image history image",
+    "example": "docker image history image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-build-no-cache-t-app",
+    "command": "docker build --no-cache -t app .",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker build --no-cache -t app .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker build --no-cache -t app .",
+    "example": "docker build --no-cache -t app .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-build-pull-t-app",
+    "command": "docker build --pull -t app .",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker build --pull -t app .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker build --pull -t app .",
+    "example": "docker build --pull -t app .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-build-build-arg-key-value-t-app",
+    "command": "docker build --build-arg KEY=value -t app .",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker build --build-arg KEY=value -t app .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker build --build-arg KEY=value -t app .",
+    "example": "docker build --build-arg KEY=value -t app .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-buildx-create-use",
+    "command": "docker buildx create --use",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker buildx create --use` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker buildx create --use",
+    "example": "docker buildx create --use",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-buildx-inspect-bootstrap",
+    "command": "docker buildx inspect --bootstrap",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker buildx inspect --bootstrap` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker buildx inspect --bootstrap",
+    "example": "docker buildx inspect --bootstrap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-buildx-build-platform-linux-amd64-linux-arm64-t-repo-app-tag-push",
+    "command": "docker buildx build --platform linux/amd64,linux/arm64 -t repo/app:tag --push .",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker buildx build --platform linux/amd64,linux/arm64 -t repo/app:tag --push .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker buildx build --platform linux/amd64,linux/arm64 -t repo/app:tag --push .",
+    "example": "docker buildx build --platform linux/amd64,linux/arm64 -t repo/app:tag --push .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-up-build-d",
+    "command": "docker compose up --build -d",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose up --build -d` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose up --build -d",
+    "example": "docker compose up --build -d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-watch",
+    "command": "docker compose watch",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose watch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose watch",
+    "example": "docker compose watch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-top",
+    "command": "docker compose top",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose top` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose top",
+    "example": "docker compose top",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-events",
+    "command": "docker compose events",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose events` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose events",
+    "example": "docker compose events",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-cp-app-path-file-file",
+    "command": "docker compose cp app:/path/file ./file",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose cp app:/path/file ./file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose cp app:/path/file ./file",
+    "example": "docker compose cp app:/path/file ./file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-port-service-80",
+    "command": "docker compose port service 80",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose port service 80` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose port service 80",
+    "example": "docker compose port service 80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-pull-service",
+    "command": "docker compose pull service",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose pull service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose pull service",
+    "example": "docker compose pull service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-build-no-cache-service",
+    "command": "docker compose build --no-cache service",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Intermediate",
+    "use_case": "Docker me `docker compose build --no-cache service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose build --no-cache service",
+    "example": "docker compose build --no-cache service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-config-services",
+    "command": "docker compose config --services",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose config --services` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose config --services",
+    "example": "docker compose config --services",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-config-volumes",
+    "command": "docker compose config --volumes",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose config --volumes` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose config --volumes",
+    "example": "docker compose config --volumes",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-ls",
+    "command": "docker compose ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose ls",
+    "example": "docker compose ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-pause",
+    "command": "docker compose pause",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose pause` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose pause",
+    "example": "docker compose pause",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-unpause",
+    "command": "docker compose unpause",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose unpause` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose unpause",
+    "example": "docker compose unpause",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-kill-service",
+    "command": "docker compose kill service",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose kill service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose kill service",
+    "example": "docker compose kill service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-compose-rm-f-service",
+    "command": "docker compose rm -f service",
+    "title": "Docker risky cleanup/delete command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker compose rm -f service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker compose rm -f service",
+    "example": "docker compose rm -f service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-inspect-bridge",
+    "command": "docker network inspect bridge",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker network inspect bridge` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker network inspect bridge",
+    "example": "docker network inspect bridge",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-create-driver-bridge-appnet",
+    "command": "docker network create --driver bridge appnet",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker network create --driver bridge appnet` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker network create --driver bridge appnet",
+    "example": "docker network create --driver bridge appnet",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-network-create-subnet-172-20-0-0-16-appnet",
+    "command": "docker network create --subnet 172.20.0.0/16 appnet",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker network create --subnet 172.20.0.0/16 appnet` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker network create --subnet 172.20.0.0/16 appnet",
+    "example": "docker network create --subnet 172.20.0.0/16 appnet",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-volume-create-driver-local-volume",
+    "command": "docker volume create --driver local volume",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker volume create --driver local volume` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker volume create --driver local volume",
+    "example": "docker volume create --driver local volume",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-volume-ls-q",
+    "command": "docker volume ls -q",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker volume ls -q` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker volume ls -q",
+    "example": "docker volume ls -q",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-run-rm-v-volume-data-alpine-ls-data",
+    "command": "docker run --rm -v volume:/data alpine ls /data",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker run --rm -v volume:/data alpine ls /data` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker run --rm -v volume:/data alpine ls /data",
+    "example": "docker run --rm -v volume:/data alpine ls /data",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-plugin-ls",
+    "command": "docker plugin ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker plugin ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker plugin ls",
+    "example": "docker plugin ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-secret-ls",
+    "command": "docker secret ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker secret ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker secret ls",
+    "example": "docker secret ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-service-ls",
+    "command": "docker service ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker service ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker service ls",
+    "example": "docker service ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-stack-ls",
+    "command": "docker stack ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker stack ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker stack ls",
+    "example": "docker stack ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-stack-deploy-c-docker-compose-yml-app",
+    "command": "docker stack deploy -c docker-compose.yml app",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker stack deploy -c docker-compose.yml app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker stack deploy -c docker-compose.yml app",
+    "example": "docker stack deploy -c docker-compose.yml app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-stack-rm-app",
+    "command": "docker stack rm app",
+    "title": "Docker risky cleanup/delete command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker stack rm app` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker stack rm app",
+    "example": "docker stack rm app",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-swarm-init",
+    "command": "docker swarm init",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker swarm init` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker swarm init",
+    "example": "docker swarm init",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-swarm-join-token-worker",
+    "command": "docker swarm join-token worker",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker swarm join-token worker` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker swarm join-token worker",
+    "example": "docker swarm join-token worker",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-node-ls",
+    "command": "docker node ls",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker node ls` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker node ls",
+    "example": "docker node ls",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-service-ps-service",
+    "command": "docker service ps service",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker service ps service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker service ps service",
+    "example": "docker service ps service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-service-logs-service",
+    "command": "docker service logs service",
+    "title": "Docker logs/debugging command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker service logs service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker service logs service",
+    "example": "docker service logs service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-service-scale-web-3",
+    "command": "docker service scale web=3",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker service scale web=3` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker service scale web=3",
+    "example": "docker service scale web=3",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-service-update-image-image-tag-service",
+    "command": "docker service update --image image:tag service",
+    "title": "Docker deploy/run/install command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker service update --image image:tag service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker service update --image image:tag service",
+    "example": "docker service update --image image:tag service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-checkpoint-ls-container",
+    "command": "docker checkpoint ls container",
+    "title": "Docker status/details verify karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker checkpoint ls container` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker checkpoint ls container",
+    "example": "docker checkpoint ls container",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-context-create-remote-docker-host-ssh-user-host",
+    "command": "docker context create remote --docker host=ssh://user@host",
+    "title": "Docker resource create/generate karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker context create remote --docker host=ssh://user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker context create remote --docker host=ssh://user@host",
+    "example": "docker context create remote --docker host=ssh://user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-context-rm-remote",
+    "command": "docker context rm remote",
+    "title": "Docker risky cleanup/delete command",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker context rm remote` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker context rm remote",
+    "example": "docker context rm remote",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-manifest-inspect-image",
+    "command": "docker manifest inspect image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker manifest inspect image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker manifest inspect image",
+    "example": "docker manifest inspect image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-trust-inspect-image",
+    "command": "docker trust inspect image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker trust inspect image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker trust inspect image",
+    "example": "docker trust inspect image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "docker-docker-scan-image",
+    "command": "docker scan image",
+    "title": "Docker command use karna",
+    "category": "Docker",
+    "difficulty": "Advanced",
+    "use_case": "Docker me `docker scan image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "docker scan image",
+    "example": "docker scan image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "docker ps",
+      "docker logs",
+      "docker inspect"
+    ],
+    "tags": [
+      "docker",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/firewall.commands.json b/public/data/firewall.commands.json
index fa5feb6..169b215 100644
--- a/public/data/firewall.commands.json
+++ b/public/data/firewall.commands.json
@@ -667,5 +667,621 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "firewall-sudo-ufw-allow-from-10-0-0-0-8",
+    "command": "sudo ufw allow from 10.0.0.0/8",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw allow from 10.0.0.0/8` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw allow from 10.0.0.0/8",
+    "example": "sudo ufw allow from 10.0.0.0/8",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-allow-from-192-168-1-0-24-to-any-port-5432",
+    "command": "sudo ufw allow from 192.168.1.0/24 to any port 5432",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw allow from 192.168.1.0/24 to any port 5432` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw allow from 192.168.1.0/24 to any port 5432",
+    "example": "sudo ufw allow from 192.168.1.0/24 to any port 5432",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-deny-out-25-tcp",
+    "command": "sudo ufw deny out 25/tcp",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw deny out 25/tcp` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw deny out 25/tcp",
+    "example": "sudo ufw deny out 25/tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-route-allow-in-on-eth0-out-on-eth1",
+    "command": "sudo ufw route allow in on eth0 out on eth1",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw route allow in on eth0 out on eth1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw route allow in on eth0 out on eth1",
+    "example": "sudo ufw route allow in on eth0 out on eth1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-insert-1-allow-from-1-2-3-4-to-any-port-22",
+    "command": "sudo ufw insert 1 allow from 1.2.3.4 to any port 22",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw insert 1 allow from 1.2.3.4 to any port 22` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw insert 1 allow from 1.2.3.4 to any port 22",
+    "example": "sudo ufw insert 1 allow from 1.2.3.4 to any port 22",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-reset",
+    "command": "sudo ufw reset",
+    "title": "Firewall risky cleanup/delete command",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw reset` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw reset",
+    "example": "sudo ufw reset",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-show-raw",
+    "command": "sudo ufw show raw",
+    "title": "Firewall status/details verify karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw show raw` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw show raw",
+    "example": "sudo ufw show raw",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ufw-show-added",
+    "command": "sudo ufw show added",
+    "title": "Firewall status/details verify karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo ufw show added` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw show added",
+    "example": "sudo ufw show added",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-p-input-drop",
+    "command": "sudo iptables -P INPUT DROP",
+    "title": "Firewall risky cleanup/delete command",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -P INPUT DROP` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -P INPUT DROP",
+    "example": "sudo iptables -P INPUT DROP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-p-forward-drop",
+    "command": "sudo iptables -P FORWARD DROP",
+    "title": "Firewall risky cleanup/delete command",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -P FORWARD DROP` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -P FORWARD DROP",
+    "example": "sudo iptables -P FORWARD DROP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-p-output-accept",
+    "command": "sudo iptables -P OUTPUT ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -P OUTPUT ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -P OUTPUT ACCEPT",
+    "example": "sudo iptables -P OUTPUT ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-m-conntrack-ctstate-established-related-j-accept",
+    "command": "sudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT",
+    "example": "sudo iptables -A INPUT -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-i-lo-j-accept",
+    "command": "sudo iptables -A INPUT -i lo -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -i lo -j ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -A INPUT -i lo -j ACCEPT",
+    "example": "sudo iptables -A INPUT -i lo -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-p-icmp-j-accept",
+    "command": "sudo iptables -A INPUT -p icmp -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Beginner",
+    "use_case": "Firewall me `sudo iptables -A INPUT -p icmp -j ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -A INPUT -p icmp -j ACCEPT",
+    "example": "sudo iptables -A INPUT -p icmp -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-a-input-p-tcp-dport-443-m-conntrack-ctstate-new-j-accept",
+    "command": "sudo iptables -A INPUT -p tcp --dport 443 -m conntrack --ctstate NEW -j ACCEPT",
+    "title": "Firewall resource create/generate karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo iptables -A INPUT -p tcp --dport 443 -m conntrack --ctstate NEW -j ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -A INPUT -p tcp --dport 443 -m conntrack --ctstate NEW -j ACCEPT",
+    "example": "sudo iptables -A INPUT -p tcp --dport 443 -m conntrack --ctstate NEW -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-i-input-1-s-1-2-3-4-j-accept",
+    "command": "sudo iptables -I INPUT 1 -s 1.2.3.4 -j ACCEPT",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo iptables -I INPUT 1 -s 1.2.3.4 -j ACCEPT` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -I INPUT 1 -s 1.2.3.4 -j ACCEPT",
+    "example": "sudo iptables -I INPUT 1 -s 1.2.3.4 -j ACCEPT",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-f",
+    "command": "sudo iptables -F",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo iptables -F` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -F",
+    "example": "sudo iptables -F",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-iptables-x",
+    "command": "sudo iptables -X",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo iptables -X` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables -X",
+    "example": "sudo iptables -X",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-ip6tables-l-n-v",
+    "command": "sudo ip6tables -L -n -v",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo ip6tables -L -n -v` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ip6tables -L -n -v",
+    "example": "sudo ip6tables -L -n -v",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-add-table-inet-filter",
+    "command": "sudo nft add table inet filter",
+    "title": "Firewall resource create/generate karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo nft add table inet filter` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo nft add table inet filter",
+    "example": "sudo nft add table inet filter",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-add-chain-inet-filter-input-type-filter-hook-input-priority-0",
+    "command": "sudo nft add chain inet filter input { type filter hook input priority 0 \\; }",
+    "title": "Firewall resource create/generate karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo nft add chain inet filter input { type filter hook input priority 0 \\; }` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo nft add chain inet filter input { type filter hook input priority 0 \\; }",
+    "example": "sudo nft add chain inet filter input { type filter hook input priority 0 \\; }",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-add-rule-inet-filter-input-tcp-dport-22-accept",
+    "command": "sudo nft add rule inet filter input tcp dport 22 accept",
+    "title": "Firewall resource create/generate karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo nft add rule inet filter input tcp dport 22 accept` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo nft add rule inet filter input tcp dport 22 accept",
+    "example": "sudo nft add rule inet filter input tcp dport 22 accept",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-nft-flush-ruleset",
+    "command": "sudo nft flush ruleset",
+    "title": "Firewall risky cleanup/delete command",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo nft flush ruleset` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo nft flush ruleset",
+    "example": "sudo nft flush ruleset",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-state",
+    "command": "sudo firewall-cmd --state",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --state` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo firewall-cmd --state",
+    "example": "sudo firewall-cmd --state",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-get-default-zone",
+    "command": "sudo firewall-cmd --get-default-zone",
+    "title": "Firewall status/details verify karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --get-default-zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo firewall-cmd --get-default-zone",
+    "example": "sudo firewall-cmd --get-default-zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-set-default-zone-public",
+    "command": "sudo firewall-cmd --set-default-zone=public",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --set-default-zone=public` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo firewall-cmd --set-default-zone=public",
+    "example": "sudo firewall-cmd --set-default-zone=public",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-add-rich-rule-rule-family-ipv4-source-address-1-2-3-4-accept-permanent",
+    "command": "sudo firewall-cmd --add-rich-rule='rule family=\"ipv4\" source address=\"1.2.3.4\" accept' --permanent",
+    "title": "Firewall resource create/generate karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --add-rich-rule='rule family=\"ipv4\" source address=\"1.2.3.4\" accept' --permanent` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo firewall-cmd --add-rich-rule='rule family=\"ipv4\" source address=\"1.2.3.4\" accept' --permanent",
+    "example": "sudo firewall-cmd --add-rich-rule='rule family=\"ipv4\" source address=\"1.2.3.4\" accept' --permanent",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "firewall-sudo-firewall-cmd-remove-service-http-permanent",
+    "command": "sudo firewall-cmd --remove-service=http --permanent",
+    "title": "Firewall command use karna",
+    "category": "Firewall",
+    "difficulty": "Intermediate",
+    "use_case": "Firewall me `sudo firewall-cmd --remove-service=http --permanent` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo firewall-cmd --remove-service=http --permanent",
+    "example": "sudo firewall-cmd --remove-service=http --permanent",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "firewall",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/gcp.commands.json b/public/data/gcp.commands.json
index aa038a5..b0af8fa 100644
--- a/public/data/gcp.commands.json
+++ b/public/data/gcp.commands.json
@@ -1012,5 +1012,1061 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "gcp-cli-gcloud-auth-application-default-login",
+    "command": "gcloud auth application-default login",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Beginner",
+    "use_case": "GCP CLI me `gcloud auth application-default login` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud auth application-default login",
+    "example": "gcloud auth application-default login",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-auth-application-default-print-access-token",
+    "command": "gcloud auth application-default print-access-token",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud auth application-default print-access-token` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud auth application-default print-access-token",
+    "example": "gcloud auth application-default print-access-token",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-config-configurations-list",
+    "command": "gcloud config configurations list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud config configurations list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud config configurations list",
+    "example": "gcloud config configurations list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-config-configurations-create-dev",
+    "command": "gcloud config configurations create dev",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud config configurations create dev` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud config configurations create dev",
+    "example": "gcloud config configurations create dev",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-config-configurations-activate-dev",
+    "command": "gcloud config configurations activate dev",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud config configurations activate dev` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud config configurations activate dev",
+    "example": "gcloud config configurations activate dev",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-machine-types-list",
+    "command": "gcloud compute machine-types list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute machine-types list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute machine-types list",
+    "example": "gcloud compute machine-types list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-zones-list",
+    "command": "gcloud compute zones list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute zones list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute zones list",
+    "example": "gcloud compute zones list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-regions-list",
+    "command": "gcloud compute regions list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute regions list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute regions list",
+    "example": "gcloud compute regions list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-create-vm-zone-zone-machine-type-e2-medium",
+    "command": "gcloud compute instances create vm --zone zone --machine-type e2-medium",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute instances create vm --zone zone --machine-type e2-medium` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute instances create vm --zone zone --machine-type e2-medium",
+    "example": "gcloud compute instances create vm --zone zone --machine-type e2-medium",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-delete-vm-zone-zone",
+    "command": "gcloud compute instances delete vm --zone zone",
+    "title": "GCP CLI risky cleanup/delete command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute instances delete vm --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute instances delete vm --zone zone",
+    "example": "gcloud compute instances delete vm --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-instances-add-tags-vm-tags-http-server-zone-zone",
+    "command": "gcloud compute instances add-tags vm --tags http-server --zone zone",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute instances add-tags vm --tags http-server --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute instances add-tags vm --tags http-server --zone zone",
+    "example": "gcloud compute instances add-tags vm --tags http-server --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-ssh-vm-zone-zone-tunnel-through-iap",
+    "command": "gcloud compute ssh vm --zone zone --tunnel-through-iap",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute ssh vm --zone zone --tunnel-through-iap` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute ssh vm --zone zone --tunnel-through-iap",
+    "example": "gcloud compute ssh vm --zone zone --tunnel-through-iap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-scp-file-vm-tmp-zone-zone",
+    "command": "gcloud compute scp file vm:/tmp --zone zone",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute scp file vm:/tmp --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute scp file vm:/tmp --zone zone",
+    "example": "gcloud compute scp file vm:/tmp --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-disks-snapshot-disk-snapshot-names-snap-zone-zone",
+    "command": "gcloud compute disks snapshot disk --snapshot-names snap --zone zone",
+    "title": "GCP CLI backup/export command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute disks snapshot disk --snapshot-names snap --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute disks snapshot disk --snapshot-names snap --zone zone",
+    "example": "gcloud compute disks snapshot disk --snapshot-names snap --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-snapshots-delete-snap",
+    "command": "gcloud compute snapshots delete snap",
+    "title": "GCP CLI risky cleanup/delete command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute snapshots delete snap` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute snapshots delete snap",
+    "example": "gcloud compute snapshots delete snap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-networks-list",
+    "command": "gcloud compute networks list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute networks list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute networks list",
+    "example": "gcloud compute networks list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-subnetworks-list",
+    "command": "gcloud compute subnetworks list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute subnetworks list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute subnetworks list",
+    "example": "gcloud compute subnetworks list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-compute-firewall-rules-describe-rule",
+    "command": "gcloud compute firewall-rules describe rule",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud compute firewall-rules describe rule` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud compute firewall-rules describe rule",
+    "example": "gcloud compute firewall-rules describe rule",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-container-clusters-describe-cluster-zone-zone",
+    "command": "gcloud container clusters describe cluster --zone zone",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud container clusters describe cluster --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud container clusters describe cluster --zone zone",
+    "example": "gcloud container clusters describe cluster --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-container-clusters-resize-cluster-num-nodes-3-zone-zone",
+    "command": "gcloud container clusters resize cluster --num-nodes 3 --zone zone",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud container clusters resize cluster --num-nodes 3 --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud container clusters resize cluster --num-nodes 3 --zone zone",
+    "example": "gcloud container clusters resize cluster --num-nodes 3 --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-container-node-pools-list-cluster-cluster-zone-zone",
+    "command": "gcloud container node-pools list --cluster cluster --zone zone",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud container node-pools list --cluster cluster --zone zone` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud container node-pools list --cluster cluster --zone zone",
+    "example": "gcloud container node-pools list --cluster cluster --zone zone",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-services-update-traffic-service-to-latest-region-region",
+    "command": "gcloud run services update-traffic service --to-latest --region region",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run services update-traffic service --to-latest --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run services update-traffic service --to-latest --region region",
+    "example": "gcloud run services update-traffic service --to-latest --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-services-proxy-service-region-region",
+    "command": "gcloud run services proxy service --region region",
+    "title": "GCP CLI deploy/run/install command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run services proxy service --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run services proxy service --region region",
+    "example": "gcloud run services proxy service --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-revisions-list-service-service-region-region",
+    "command": "gcloud run revisions list --service service --region region",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run revisions list --service service --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run revisions list --service service --region region",
+    "example": "gcloud run revisions list --service service --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-revisions-describe-revision-region-region",
+    "command": "gcloud run revisions describe revision --region region",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run revisions describe revision --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run revisions describe revision --region region",
+    "example": "gcloud run revisions describe revision --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-jobs-create-job-image-image-region-region",
+    "command": "gcloud run jobs create job --image image --region region",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run jobs create job --image image --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run jobs create job --image image --region region",
+    "example": "gcloud run jobs create job --image image --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-run-jobs-delete-job-region-region",
+    "command": "gcloud run jobs delete job --region region",
+    "title": "GCP CLI risky cleanup/delete command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud run jobs delete job --region region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud run jobs delete job --region region",
+    "example": "gcloud run jobs delete job --region region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-builds-triggers-list",
+    "command": "gcloud builds triggers list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud builds triggers list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud builds triggers list",
+    "example": "gcloud builds triggers list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-builds-triggers-run-trigger-branch-main",
+    "command": "gcloud builds triggers run trigger --branch main",
+    "title": "GCP CLI deploy/run/install command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud builds triggers run trigger --branch main` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud builds triggers run trigger --branch main",
+    "example": "gcloud builds triggers run trigger --branch main",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-artifacts-repositories-create-repo-repository-format-docker-location-region",
+    "command": "gcloud artifacts repositories create repo --repository-format=docker --location=region",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud artifacts repositories create repo --repository-format=docker --location=region` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud artifacts repositories create repo --repository-format=docker --location=region",
+    "example": "gcloud artifacts repositories create repo --repository-format=docker --location=region",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-artifacts-docker-images-delete-image",
+    "command": "gcloud artifacts docker images delete image",
+    "title": "GCP CLI risky cleanup/delete command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud artifacts docker images delete image` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud artifacts docker images delete image",
+    "example": "gcloud artifacts docker images delete image",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-instances-describe-instance",
+    "command": "gcloud sql instances describe instance",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql instances describe instance` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud sql instances describe instance",
+    "example": "gcloud sql instances describe instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-backups-list-instance-instance",
+    "command": "gcloud sql backups list --instance instance",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql backups list --instance instance` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud sql backups list --instance instance",
+    "example": "gcloud sql backups list --instance instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-backups-create-instance-instance",
+    "command": "gcloud sql backups create --instance instance",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql backups create --instance instance` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud sql backups create --instance instance",
+    "example": "gcloud sql backups create --instance instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-import-sql-instance-gs-bucket-file-sql-database-db",
+    "command": "gcloud sql import sql instance gs://bucket/file.sql --database db",
+    "title": "GCP CLI restore/import command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql import sql instance gs://bucket/file.sql --database db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud sql import sql instance gs://bucket/file.sql --database db",
+    "example": "gcloud sql import sql instance gs://bucket/file.sql --database db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-sql-export-sql-instance-gs-bucket-file-sql-database-db",
+    "command": "gcloud sql export sql instance gs://bucket/file.sql --database db",
+    "title": "GCP CLI backup/export command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud sql export sql instance gs://bucket/file.sql --database db` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud sql export sql instance gs://bucket/file.sql --database db",
+    "example": "gcloud sql export sql instance gs://bucket/file.sql --database db",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-buckets-create-gs-bucket",
+    "command": "gcloud storage buckets create gs://bucket",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage buckets create gs://bucket` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud storage buckets create gs://bucket",
+    "example": "gcloud storage buckets create gs://bucket",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-rm-gs-bucket-path",
+    "command": "gcloud storage rm gs://bucket/path",
+    "title": "GCP CLI risky cleanup/delete command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage rm gs://bucket/path` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud storage rm gs://bucket/path",
+    "example": "gcloud storage rm gs://bucket/path",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-storage-du-gs-bucket-summarize",
+    "command": "gcloud storage du gs://bucket --summarize",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud storage du gs://bucket --summarize` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud storage du gs://bucket --summarize",
+    "example": "gcloud storage du gs://bucket --summarize",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-iam-roles-list",
+    "command": "gcloud iam roles list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud iam roles list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud iam roles list",
+    "example": "gcloud iam roles list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-iam-service-accounts-create-sa",
+    "command": "gcloud iam service-accounts create sa",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud iam service-accounts create sa` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud iam service-accounts create sa",
+    "example": "gcloud iam service-accounts create sa",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-projects-add-iam-policy-binding-project-member-serviceaccount-sa-role-roles-viewer",
+    "command": "gcloud projects add-iam-policy-binding PROJECT --member serviceAccount:sa --role roles/viewer",
+    "title": "GCP CLI resource create/generate karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud projects add-iam-policy-binding PROJECT --member serviceAccount:sa --role roles/viewer` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud projects add-iam-policy-binding PROJECT --member serviceAccount:sa --role roles/viewer",
+    "example": "gcloud projects add-iam-policy-binding PROJECT --member serviceAccount:sa --role roles/viewer",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-secrets-versions-list-secret",
+    "command": "gcloud secrets versions list secret",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud secrets versions list secret` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud secrets versions list secret",
+    "example": "gcloud secrets versions list secret",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-logging-read-severity-error-freshness-1h-limit-100",
+    "command": "gcloud logging read \"severity=ERROR\" --freshness=1h --limit=100",
+    "title": "GCP CLI command use karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud logging read \"severity=ERROR\" --freshness=1h --limit=100` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud logging read \"severity=ERROR\" --freshness=1h --limit=100",
+    "example": "gcloud logging read \"severity=ERROR\" --freshness=1h --limit=100",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-logging-tail-resource-type-cloud-run-revision",
+    "command": "gcloud logging tail \"resource.type=cloud_run_revision\"",
+    "title": "GCP CLI logs/debugging command",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud logging tail \"resource.type=cloud_run_revision\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud logging tail \"resource.type=cloud_run_revision\"",
+    "example": "gcloud logging tail \"resource.type=cloud_run_revision\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-scheduler-jobs-list",
+    "command": "gcloud scheduler jobs list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud scheduler jobs list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud scheduler jobs list",
+    "example": "gcloud scheduler jobs list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-pubsub-topics-list",
+    "command": "gcloud pubsub topics list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud pubsub topics list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud pubsub topics list",
+    "example": "gcloud pubsub topics list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "gcp-cli-gcloud-pubsub-subscriptions-list",
+    "command": "gcloud pubsub subscriptions list",
+    "title": "GCP CLI status/details verify karna",
+    "category": "GCP CLI",
+    "difficulty": "Intermediate",
+    "use_case": "GCP CLI me `gcloud pubsub subscriptions list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gcloud pubsub subscriptions list",
+    "example": "gcloud pubsub subscriptions list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "gcloud config list",
+      "gcloud auth list"
+    ],
+    "tags": [
+      "gcp",
+      "gcloud",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/git.commands.json b/public/data/git.commands.json
index 27d0495..4f51959 100644
--- a/public/data/git.commands.json
+++ b/public/data/git.commands.json
@@ -1372,5 +1372,1516 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "git-github-git-config-global-init-defaultbranch-main",
+    "command": "git config --global init.defaultBranch main",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git config --global init.defaultBranch main` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git config --global init.defaultBranch main",
+    "example": "git config --global init.defaultBranch main",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-global-core-editor-code-wait",
+    "command": "git config --global core.editor \"code --wait\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git config --global core.editor \"code --wait\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git config --global core.editor \"code --wait\"",
+    "example": "git config --global core.editor \"code --wait\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-global-pull-rebase-true",
+    "command": "git config --global pull.rebase true",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git config --global pull.rebase true` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git config --global pull.rebase true",
+    "example": "git config --global pull.rebase true",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-global-credential-helper-store",
+    "command": "git config --global credential.helper store",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git config --global credential.helper store` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git config --global credential.helper store",
+    "example": "git config --global credential.helper store",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-config-get-remote-origin-url",
+    "command": "git config --get remote.origin.url",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git config --get remote.origin.url` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git config --get remote.origin.url",
+    "example": "git config --get remote.origin.url",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-remote-add-origin-url",
+    "command": "git remote add origin <url>",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git remote add origin <url>` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git remote add origin <url>",
+    "example": "git remote add origin <url>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-remote-remove-origin",
+    "command": "git remote remove origin",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git remote remove origin` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git remote remove origin",
+    "example": "git remote remove origin",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-remote-rename-origin-upstream",
+    "command": "git remote rename origin upstream",
+    "title": "Git / GitHub deploy/run/install command",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git remote rename origin upstream` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git remote rename origin upstream",
+    "example": "git remote rename origin upstream",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-remote-set-url-origin-url",
+    "command": "git remote set-url origin <url>",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git remote set-url origin <url>` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git remote set-url origin <url>",
+    "example": "git remote set-url origin <url>",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-ls-remote-origin",
+    "command": "git ls-remote origin",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git ls-remote origin` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git ls-remote origin",
+    "example": "git ls-remote origin",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-branch-vv",
+    "command": "git branch -vv",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git branch -vv` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git branch -vv",
+    "example": "git branch -vv",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-branch-d-branch",
+    "command": "git branch -d branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git branch -d branch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git branch -d branch",
+    "example": "git branch -d branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-branch-d-branch",
+    "command": "git branch -D branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git branch -D branch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git branch -D branch",
+    "example": "git branch -D branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-checkout-file",
+    "command": "git checkout -- file",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git checkout -- file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git checkout -- file",
+    "example": "git checkout -- file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-checkout-commit-file",
+    "command": "git checkout commit -- file",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git checkout commit -- file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git checkout commit -- file",
+    "example": "git checkout commit -- file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-switch-c-branch",
+    "command": "git switch -c branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git switch -c branch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git switch -c branch",
+    "example": "git switch -c branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-switch",
+    "command": "git switch -",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git switch -` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git switch -",
+    "example": "git switch -",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-add-p",
+    "command": "git add -p",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git add -p` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git add -p",
+    "example": "git add -p",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-commit-amend",
+    "command": "git commit --amend",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git commit --amend` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git commit --amend",
+    "example": "git commit --amend",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-commit-amend-no-edit",
+    "command": "git commit --amend --no-edit",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git commit --amend --no-edit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git commit --amend --no-edit",
+    "example": "git commit --amend --no-edit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-commit-allow-empty-m-trigger-ci",
+    "command": "git commit --allow-empty -m \"trigger ci\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git commit --allow-empty -m \"trigger ci\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git commit --allow-empty -m \"trigger ci\"",
+    "example": "git commit --allow-empty -m \"trigger ci\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-log-stat",
+    "command": "git log --stat",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git log --stat` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git log --stat",
+    "example": "git log --stat",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-log-name-only",
+    "command": "git log --name-only",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git log --name-only` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git log --name-only",
+    "example": "git log --name-only",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-log-author-name",
+    "command": "git log --author=\"Name\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git log --author=\"Name\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git log --author=\"Name\"",
+    "example": "git log --author=\"Name\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-log-since-1-week-ago",
+    "command": "git log --since=\"1 week ago\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git log --since=\"1 week ago\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git log --since=\"1 week ago\"",
+    "example": "git log --since=\"1 week ago\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-log-grep-fix",
+    "command": "git log --grep=\"fix\"",
+    "title": "Git / GitHub logs/debugging command",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git log --grep=\"fix\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git log --grep=\"fix\"",
+    "example": "git log --grep=\"fix\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-diff-main-feature",
+    "command": "git diff main..feature",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git diff main..feature` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git diff main..feature",
+    "example": "git diff main..feature",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-diff-name-only-main-feature",
+    "command": "git diff --name-only main..feature",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git diff --name-only main..feature` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git diff --name-only main..feature",
+    "example": "git diff --name-only main..feature",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-diff-check",
+    "command": "git diff --check",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git diff --check` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git diff --check",
+    "example": "git diff --check",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-show-stat-commit",
+    "command": "git show --stat commit",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git show --stat commit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git show --stat commit",
+    "example": "git show --stat commit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-show-name-only-commit",
+    "command": "git show --name-only commit",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git show --name-only commit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git show --name-only commit",
+    "example": "git show --name-only commit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-push-u-m-include-untracked",
+    "command": "git stash push -u -m \"include untracked\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash push -u -m \"include untracked\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash push -u -m \"include untracked\"",
+    "example": "git stash push -u -m \"include untracked\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-list",
+    "command": "git stash list",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash list",
+    "example": "git stash list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-show-p-stash-0",
+    "command": "git stash show -p stash@{0}",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash show -p stash@{0}` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash show -p stash@{0}",
+    "example": "git stash show -p stash@{0}",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-apply-stash-0",
+    "command": "git stash apply stash@{0}",
+    "title": "Git / GitHub deploy/run/install command",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash apply stash@{0}` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash apply stash@{0}",
+    "example": "git stash apply stash@{0}",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-drop-stash-0",
+    "command": "git stash drop stash@{0}",
+    "title": "Git / GitHub risky cleanup/delete command",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash drop stash@{0}` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash drop stash@{0}",
+    "example": "git stash drop stash@{0}",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-stash-clear",
+    "command": "git stash clear",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git stash clear` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git stash clear",
+    "example": "git stash clear",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-worktree-add-app-branch-branch",
+    "command": "git worktree add ../app-branch branch",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git worktree add ../app-branch branch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git worktree add ../app-branch branch",
+    "example": "git worktree add ../app-branch branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-worktree-list",
+    "command": "git worktree list",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git worktree list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git worktree list",
+    "example": "git worktree list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-worktree-remove-app-branch",
+    "command": "git worktree remove ../app-branch",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git worktree remove ../app-branch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git worktree remove ../app-branch",
+    "example": "git worktree remove ../app-branch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-submodule-status",
+    "command": "git submodule status",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git submodule status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git submodule status",
+    "example": "git submodule status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-submodule-update-init-recursive",
+    "command": "git submodule update --init --recursive",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git submodule update --init --recursive` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git submodule update --init --recursive",
+    "example": "git submodule update --init --recursive",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-submodule-foreach-git-pull-origin-main",
+    "command": "git submodule foreach git pull origin main",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git submodule foreach git pull origin main` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git submodule foreach git pull origin main",
+    "example": "git submodule foreach git pull origin main",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-notes-add-m-note-commit",
+    "command": "git notes add -m \"note\" commit",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Intermediate",
+    "use_case": "Git / GitHub me `git notes add -m \"note\" commit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git notes add -m \"note\" commit",
+    "example": "git notes add -m \"note\" commit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-grep-pattern",
+    "command": "git grep \"pattern\"",
+    "title": "Git / GitHub logs/debugging command",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git grep \"pattern\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git grep \"pattern\"",
+    "example": "git grep \"pattern\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-grep-n-todo",
+    "command": "git grep -n \"TODO\"",
+    "title": "Git / GitHub logs/debugging command",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git grep -n \"TODO\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git grep -n \"TODO\"",
+    "example": "git grep -n \"TODO\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-count-objects-vh",
+    "command": "git count-objects -vH",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git count-objects -vH` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git count-objects -vH",
+    "example": "git count-objects -vH",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-gc",
+    "command": "git gc",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git gc` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git gc",
+    "example": "git gc",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-fsck",
+    "command": "git fsck",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git fsck` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git fsck",
+    "example": "git fsck",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-update-index-assume-unchanged-file",
+    "command": "git update-index --assume-unchanged file",
+    "title": "Git / GitHub deploy/run/install command",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git update-index --assume-unchanged file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git update-index --assume-unchanged file",
+    "example": "git update-index --assume-unchanged file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-update-index-no-assume-unchanged-file",
+    "command": "git update-index --no-assume-unchanged file",
+    "title": "Git / GitHub deploy/run/install command",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git update-index --no-assume-unchanged file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git update-index --no-assume-unchanged file",
+    "example": "git update-index --no-assume-unchanged file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-lfs-install",
+    "command": "git lfs install",
+    "title": "Git / GitHub deploy/run/install command",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git lfs install` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git lfs install",
+    "example": "git lfs install",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-lfs-track-zip",
+    "command": "git lfs track \"*.zip\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git lfs track \"*.zip\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git lfs track \"*.zip\"",
+    "example": "git lfs track \"*.zip\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-git-lfs-ls-files",
+    "command": "git lfs ls-files",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `git lfs ls-files` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "git lfs ls-files",
+    "example": "git lfs ls-files",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "git status",
+      "git diff",
+      "git log --oneline"
+    ],
+    "tags": [
+      "git",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-fork",
+    "command": "gh repo fork",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh repo fork` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh repo fork",
+    "example": "gh repo fork",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-create",
+    "command": "gh repo create",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh repo create` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh repo create",
+    "example": "gh repo create",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-sync",
+    "command": "gh repo sync",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh repo sync` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh repo sync",
+    "example": "gh repo sync",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-repo-set-default",
+    "command": "gh repo set-default",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh repo set-default` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh repo set-default",
+    "example": "gh repo set-default",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-issue-create",
+    "command": "gh issue create",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh issue create` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh issue create",
+    "example": "gh issue create",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-issue-close-1",
+    "command": "gh issue close 1",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh issue close 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh issue close 1",
+    "example": "gh issue close 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-issue-comment-1-body-message",
+    "command": "gh issue comment 1 --body \"message\"",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh issue comment 1 --body \"message\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh issue comment 1 --body \"message\"",
+    "example": "gh issue comment 1 --body \"message\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-checks",
+    "command": "gh pr checks",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh pr checks` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh pr checks",
+    "example": "gh pr checks",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-diff",
+    "command": "gh pr diff",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh pr diff` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh pr diff",
+    "example": "gh pr diff",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-review-approve",
+    "command": "gh pr review --approve",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh pr review --approve` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh pr review --approve",
+    "example": "gh pr review --approve",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-review-request-changes",
+    "command": "gh pr review --request-changes",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh pr review --request-changes` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh pr review --request-changes",
+    "example": "gh pr review --request-changes",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-pr-close-1",
+    "command": "gh pr close 1",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh pr close 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh pr close 1",
+    "example": "gh pr close 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-gist-create-file-txt",
+    "command": "gh gist create file.txt",
+    "title": "Git / GitHub resource create/generate karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh gist create file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh gist create file.txt",
+    "example": "gh gist create file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-auth-refresh",
+    "command": "gh auth refresh",
+    "title": "Git / GitHub restart/reload/refresh karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh auth refresh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh auth refresh",
+    "example": "gh auth refresh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-api-rate-limit",
+    "command": "gh api rate_limit",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh api rate_limit` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh api rate_limit",
+    "example": "gh api rate_limit",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-codespace-list",
+    "command": "gh codespace list",
+    "title": "Git / GitHub status/details verify karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh codespace list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh codespace list",
+    "example": "gh codespace list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "git-github-gh-codespace-ssh",
+    "command": "gh codespace ssh",
+    "title": "Git / GitHub command use karna",
+    "category": "Git / GitHub",
+    "difficulty": "Advanced",
+    "use_case": "Git / GitHub me `gh codespace ssh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gh codespace ssh",
+    "example": "gh codespace ssh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "git",
+      "gh",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/helm.commands.json b/public/data/helm.commands.json
index 77eed24..db92467 100644
--- a/public/data/helm.commands.json
+++ b/public/data/helm.commands.json
@@ -730,5 +730,577 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "helm-helm-list-n-ns",
+    "command": "helm list -n ns",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm list -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm list -n ns",
+    "example": "helm list -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-list-failed-a",
+    "command": "helm list --failed -A",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm list --failed -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm list --failed -A",
+    "example": "helm list --failed -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-list-deployed-a",
+    "command": "helm list --deployed -A",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm list --deployed -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm list --deployed -A",
+    "example": "helm list --deployed -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-install-release-chart-namespace-ns-create-namespace",
+    "command": "helm install release chart --namespace ns --create-namespace",
+    "title": "Helm resource create/generate karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm install release chart --namespace ns --create-namespace` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm install release chart --namespace ns --create-namespace",
+    "example": "helm install release chart --namespace ns --create-namespace",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-install-release-chart-dry-run-debug",
+    "command": "helm install release chart --dry-run --debug",
+    "title": "Helm logs/debugging command",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm install release chart --dry-run --debug` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm install release chart --dry-run --debug",
+    "example": "helm install release chart --dry-run --debug",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-upgrade-release-chart-reuse-values",
+    "command": "helm upgrade release chart --reuse-values",
+    "title": "Helm deploy/run/install command",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm upgrade release chart --reuse-values` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm upgrade release chart --reuse-values",
+    "example": "helm upgrade release chart --reuse-values",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-upgrade-release-chart-atomic",
+    "command": "helm upgrade release chart --atomic",
+    "title": "Helm deploy/run/install command",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm upgrade release chart --atomic` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm upgrade release chart --atomic",
+    "example": "helm upgrade release chart --atomic",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-upgrade-release-chart-timeout-10m",
+    "command": "helm upgrade release chart --timeout 10m",
+    "title": "Helm deploy/run/install command",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm upgrade release chart --timeout 10m` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm upgrade release chart --timeout 10m",
+    "example": "helm upgrade release chart --timeout 10m",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-rollback-release-1-wait",
+    "command": "helm rollback release 1 --wait",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm rollback release 1 --wait` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm rollback release 1 --wait",
+    "example": "helm rollback release 1 --wait",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-uninstall-release-keep-history",
+    "command": "helm uninstall release --keep-history",
+    "title": "Helm deploy/run/install command",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm uninstall release --keep-history` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm uninstall release --keep-history",
+    "example": "helm uninstall release --keep-history",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-history-release-max-10",
+    "command": "helm history release --max 10",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm history release --max 10` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm history release --max 10",
+    "example": "helm history release --max 10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-get-notes-release-n-ns",
+    "command": "helm get notes release -n ns",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm get notes release -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm get notes release -n ns",
+    "example": "helm get notes release -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-get-values-release-n-ns-all",
+    "command": "helm get values release -n ns --all",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm get values release -n ns --all` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm get values release -n ns --all",
+    "example": "helm get values release -n ns --all",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-template-release-chart-values-values-yaml",
+    "command": "helm template release chart --values values.yaml",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Beginner",
+    "use_case": "Helm me `helm template release chart --values values.yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm template release chart --values values.yaml",
+    "example": "helm template release chart --values values.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-template-release-chart-debug",
+    "command": "helm template release chart --debug",
+    "title": "Helm logs/debugging command",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm template release chart --debug` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm template release chart --debug",
+    "example": "helm template release chart --debug",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-lint-chart-strict",
+    "command": "helm lint ./chart --strict",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm lint ./chart --strict` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm lint ./chart --strict",
+    "example": "helm lint ./chart --strict",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-dependency-list-chart",
+    "command": "helm dependency list ./chart",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm dependency list ./chart` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm dependency list ./chart",
+    "example": "helm dependency list ./chart",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-package-chart-destination-dist",
+    "command": "helm package ./chart --destination ./dist",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm package ./chart --destination ./dist` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm package ./chart --destination ./dist",
+    "example": "helm package ./chart --destination ./dist",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-push-chart-tgz-oci-registry-repo",
+    "command": "helm push chart.tgz oci://registry/repo",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm push chart.tgz oci://registry/repo` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm push chart.tgz oci://registry/repo",
+    "example": "helm push chart.tgz oci://registry/repo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-pull-oci-registry-repo-chart-version-1-0-0",
+    "command": "helm pull oci://registry/repo/chart --version 1.0.0",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm pull oci://registry/repo/chart --version 1.0.0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm pull oci://registry/repo/chart --version 1.0.0",
+    "example": "helm pull oci://registry/repo/chart --version 1.0.0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-registry-login-registry",
+    "command": "helm registry login registry",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm registry login registry` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm registry login registry",
+    "example": "helm registry login registry",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-registry-logout-registry",
+    "command": "helm registry logout registry",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm registry logout registry` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm registry logout registry",
+    "example": "helm registry logout registry",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-verify-chart-tgz",
+    "command": "helm verify chart.tgz",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm verify chart.tgz` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm verify chart.tgz",
+    "example": "helm verify chart.tgz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-plugin-install-plugin-url",
+    "command": "helm plugin install plugin-url",
+    "title": "Helm deploy/run/install command",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm plugin install plugin-url` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm plugin install plugin-url",
+    "example": "helm plugin install plugin-url",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-repo-index",
+    "command": "helm repo index .",
+    "title": "Helm command use karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm repo index .` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm repo index .",
+    "example": "helm repo index .",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "helm-helm-search-repo-keyword-versions",
+    "command": "helm search repo keyword --versions",
+    "title": "Helm status/details verify karna",
+    "category": "Helm",
+    "difficulty": "Intermediate",
+    "use_case": "Helm me `helm search repo keyword --versions` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "helm search repo keyword --versions",
+    "example": "helm search repo keyword --versions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "helm list -A",
+      "helm status release",
+      "helm upgrade"
+    ],
+    "tags": [
+      "helm",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/kubernetes.commands.json b/public/data/kubernetes.commands.json
index 83e5156..28afcb6 100644
--- a/public/data/kubernetes.commands.json
+++ b/public/data/kubernetes.commands.json
@@ -1544,5 +1544,1293 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pods-o-wide-a",
+    "command": "kubectl get pods -o wide -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get pods -o wide -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get pods -o wide -A",
+    "example": "kubectl get pods -o wide -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pods-field-selector-status-phase-failed-a",
+    "command": "kubectl get pods --field-selector=status.phase=Failed -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get pods --field-selector=status.phase=Failed -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get pods --field-selector=status.phase=Failed -A",
+    "example": "kubectl get pods --field-selector=status.phase=Failed -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pods-sort-by-metadata-creationtimestamp-a",
+    "command": "kubectl get pods --sort-by=.metadata.creationTimestamp -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get pods --sort-by=.metadata.creationTimestamp -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get pods --sort-by=.metadata.creationTimestamp -A",
+    "example": "kubectl get pods --sort-by=.metadata.creationTimestamp -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-nodes-o-wide",
+    "command": "kubectl get nodes -o wide",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get nodes -o wide` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get nodes -o wide",
+    "example": "kubectl get nodes -o wide",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-nodes-show-labels",
+    "command": "kubectl get nodes --show-labels",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get nodes --show-labels` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get nodes --show-labels",
+    "example": "kubectl get nodes --show-labels",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-deploy-o-yaml-n-ns-name",
+    "command": "kubectl get deploy -o yaml -n ns name",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get deploy -o yaml -n ns name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get deploy -o yaml -n ns name",
+    "example": "kubectl get deploy -o yaml -n ns name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-pod-pod-n-ns-o-yaml",
+    "command": "kubectl get pod pod -n ns -o yaml",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get pod pod -n ns -o yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get pod pod -n ns -o yaml",
+    "example": "kubectl get pod pod -n ns -o yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-secret-secret-n-ns-o-jsonpath-data",
+    "command": "kubectl get secret secret -n ns -o jsonpath=\"{.data}\"",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get secret secret -n ns -o jsonpath=\"{.data}\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get secret secret -n ns -o jsonpath=\"{.data}\"",
+    "example": "kubectl get secret secret -n ns -o jsonpath=\"{.data}\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-endpointslices-a",
+    "command": "kubectl get endpointslices -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get endpointslices -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get endpointslices -A",
+    "example": "kubectl get endpointslices -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-storageclass",
+    "command": "kubectl get storageclass",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get storageclass` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get storageclass",
+    "example": "kubectl get storageclass",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-priorityclass",
+    "command": "kubectl get priorityclass",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get priorityclass` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get priorityclass",
+    "example": "kubectl get priorityclass",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-poddisruptionbudget-a",
+    "command": "kubectl get poddisruptionbudget -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get poddisruptionbudget -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get poddisruptionbudget -A",
+    "example": "kubectl get poddisruptionbudget -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-networkpolicy-a",
+    "command": "kubectl get networkpolicy -A",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get networkpolicy -A` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get networkpolicy -A",
+    "example": "kubectl get networkpolicy -A",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-validatingwebhookconfiguration",
+    "command": "kubectl get validatingwebhookconfiguration",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get validatingwebhookconfiguration` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get validatingwebhookconfiguration",
+    "example": "kubectl get validatingwebhookconfiguration",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-mutatingwebhookconfiguration",
+    "command": "kubectl get mutatingwebhookconfiguration",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl get mutatingwebhookconfiguration` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get mutatingwebhookconfiguration",
+    "example": "kubectl get mutatingwebhookconfiguration",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-pvc-pvc-n-ns",
+    "command": "kubectl describe pvc pvc -n ns",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe pvc pvc -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl describe pvc pvc -n ns",
+    "example": "kubectl describe pvc pvc -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-job-job-n-ns",
+    "command": "kubectl describe job job -n ns",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe job job -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl describe job job -n ns",
+    "example": "kubectl describe job job -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-cronjob-cron-n-ns",
+    "command": "kubectl describe cronjob cron -n ns",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe cronjob cron -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl describe cronjob cron -n ns",
+    "example": "kubectl describe cronjob cron -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-describe-hpa-hpa-n-ns",
+    "command": "kubectl describe hpa hpa -n ns",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl describe hpa hpa -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl describe hpa hpa -n ns",
+    "example": "kubectl describe hpa hpa -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-pod-c-container-n-ns",
+    "command": "kubectl logs pod -c container -n ns",
+    "title": "Kubernetes logs/debugging command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs pod -c container -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl logs pod -c container -n ns",
+    "example": "kubectl logs pod -c container -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-deploy-app-all-containers-true-n-ns",
+    "command": "kubectl logs deploy/app --all-containers=true -n ns",
+    "title": "Kubernetes logs/debugging command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs deploy/app --all-containers=true -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl logs deploy/app --all-containers=true -n ns",
+    "example": "kubectl logs deploy/app --all-containers=true -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-logs-job-job-name-n-ns",
+    "command": "kubectl logs job/job-name -n ns",
+    "title": "Kubernetes logs/debugging command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl logs job/job-name -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl logs job/job-name -n ns",
+    "example": "kubectl logs job/job-name -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-attach-it-pod-n-ns",
+    "command": "kubectl attach -it pod -n ns",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl attach -it pod -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl attach -it pod -n ns",
+    "example": "kubectl attach -it pod -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-debug-pod-pod-it-image-busybox-n-ns",
+    "command": "kubectl debug pod/pod -it --image=busybox -n ns",
+    "title": "Kubernetes logs/debugging command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl debug pod/pod -it --image=busybox -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl debug pod/pod -it --image=busybox -n ns",
+    "example": "kubectl debug pod/pod -it --image=busybox -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-debug-node-node-name-it-image-busybox",
+    "command": "kubectl debug node/node-name -it --image=busybox",
+    "title": "Kubernetes logs/debugging command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl debug node/node-name -it --image=busybox` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl debug node/node-name -it --image=busybox",
+    "example": "kubectl debug node/node-name -it --image=busybox",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-run-tmp-shell-rm-it-image-busybox-sh",
+    "command": "kubectl run tmp-shell --rm -it --image=busybox -- sh",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl run tmp-shell --rm -it --image=busybox -- sh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl run tmp-shell --rm -it --image=busybox -- sh",
+    "example": "kubectl run tmp-shell --rm -it --image=busybox -- sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-run-curl-rm-it-image-curlimages-curl-sh",
+    "command": "kubectl run curl --rm -it --image=curlimages/curl -- sh",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl run curl --rm -it --image=curlimages/curl -- sh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl run curl --rm -it --image=curlimages/curl -- sh",
+    "example": "kubectl run curl --rm -it --image=curlimages/curl -- sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-deployment-nginx-image-nginx",
+    "command": "kubectl create deployment nginx --image=nginx",
+    "title": "Kubernetes resource create/generate karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl create deployment nginx --image=nginx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl create deployment nginx --image=nginx",
+    "example": "kubectl create deployment nginx --image=nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-expose-deployment-nginx-port-80-type-clusterip",
+    "command": "kubectl expose deployment nginx --port=80 --type=ClusterIP",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl expose deployment nginx --port=80 --type=ClusterIP` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl expose deployment nginx --port=80 --type=ClusterIP",
+    "example": "kubectl expose deployment nginx --port=80 --type=ClusterIP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-job-test-image-busybox-echo-hello",
+    "command": "kubectl create job test --image=busybox -- echo hello",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl create job test --image=busybox -- echo hello` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl create job test --image=busybox -- echo hello",
+    "example": "kubectl create job test --image=busybox -- echo hello",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-cronjob-hello-image-busybox-schedule-1-echo-hello",
+    "command": "kubectl create cronjob hello --image=busybox --schedule=\"*/1 * * * *\" -- echo hello",
+    "title": "Kubernetes resource create/generate karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl create cronjob hello --image=busybox --schedule=\"*/1 * * * *\" -- echo hello` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl create cronjob hello --image=busybox --schedule=\"*/1 * * * *\" -- echo hello",
+    "example": "kubectl create cronjob hello --image=busybox --schedule=\"*/1 * * * *\" -- echo hello",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-patch-deploy-app-p-spec-replicas-3-n-ns",
+    "command": "kubectl patch deploy app -p '{\"spec\":{\"replicas\":3}}' -n ns",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl patch deploy app -p '{\"spec\":{\"replicas\":3}}' -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl patch deploy app -p '{\"spec\":{\"replicas\":3}}' -n ns",
+    "example": "kubectl patch deploy app -p '{\"spec\":{\"replicas\":3}}' -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-annotate-deploy-app-key-value-n-ns",
+    "command": "kubectl annotate deploy app key=value -n ns",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl annotate deploy app key=value -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl annotate deploy app key=value -n ns",
+    "example": "kubectl annotate deploy app key=value -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-label-pod-pod-key-value-n-ns-overwrite",
+    "command": "kubectl label pod pod key=value -n ns --overwrite",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl label pod pod key=value -n ns --overwrite` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl label pod pod key=value -n ns --overwrite",
+    "example": "kubectl label pod pod key=value -n ns --overwrite",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-delete-pod-pod-grace-period-0-force-n-ns",
+    "command": "kubectl delete pod pod --grace-period=0 --force -n ns",
+    "title": "Kubernetes risky cleanup/delete command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl delete pod pod --grace-period=0 --force -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl delete pod pod --grace-period=0 --force -n ns",
+    "example": "kubectl delete pod pod --grace-period=0 --force -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-delete-ev-all-n-ns",
+    "command": "kubectl delete ev --all -n ns",
+    "title": "Kubernetes risky cleanup/delete command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl delete ev --all -n ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl delete ev --all -n ns",
+    "example": "kubectl delete ev --all -n ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-wait-for-condition-ready-pod-l-app-myapp-n-ns-timeout-120s",
+    "command": "kubectl wait --for=condition=ready pod -l app=myapp -n ns --timeout=120s",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl wait --for=condition=ready pod -l app=myapp -n ns --timeout=120s` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl wait --for=condition=ready pod -l app=myapp -n ns --timeout=120s",
+    "example": "kubectl wait --for=condition=ready pod -l app=myapp -n ns --timeout=120s",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-wait-for-condition-available-deploy-app-n-ns-timeout-120s",
+    "command": "kubectl wait --for=condition=available deploy/app -n ns --timeout=120s",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Intermediate",
+    "use_case": "Kubernetes me `kubectl wait --for=condition=available deploy/app -n ns --timeout=120s` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl wait --for=condition=available deploy/app -n ns --timeout=120s",
+    "example": "kubectl wait --for=condition=available deploy/app -n ns --timeout=120s",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-config-current-context",
+    "command": "kubectl config current-context",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl config current-context` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl config current-context",
+    "example": "kubectl config current-context",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-config-view",
+    "command": "kubectl config view",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl config view` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl config view",
+    "example": "kubectl config view",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-config-set-context-current-namespace-ns",
+    "command": "kubectl config set-context --current --namespace=ns",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl config set-context --current --namespace=ns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl config set-context --current --namespace=ns",
+    "example": "kubectl config set-context --current --namespace=ns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-config-delete-context-context",
+    "command": "kubectl config delete-context context",
+    "title": "Kubernetes risky cleanup/delete command",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl config delete-context context` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl config delete-context context",
+    "example": "kubectl config delete-context context",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-certificate-approve-csr-name",
+    "command": "kubectl certificate approve csr-name",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl certificate approve csr-name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl certificate approve csr-name",
+    "example": "kubectl certificate approve csr-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-top-pod-pod-n-ns-containers",
+    "command": "kubectl top pod pod -n ns --containers",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl top pod pod -n ns --containers` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl top pod pod -n ns --containers",
+    "example": "kubectl top pod pod -n ns --containers",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-drain-node-delete-emptydir-data-ignore-daemonsets",
+    "command": "kubectl drain node --delete-emptydir-data --ignore-daemonsets",
+    "title": "Kubernetes risky cleanup/delete command",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl drain node --delete-emptydir-data --ignore-daemonsets` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl drain node --delete-emptydir-data --ignore-daemonsets",
+    "example": "kubectl drain node --delete-emptydir-data --ignore-daemonsets",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-taint-nodes-node-dedicated-gpu-noschedule",
+    "command": "kubectl taint nodes node dedicated=gpu:NoSchedule",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl taint nodes node dedicated=gpu:NoSchedule` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl taint nodes node dedicated=gpu:NoSchedule",
+    "example": "kubectl taint nodes node dedicated=gpu:NoSchedule",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-taint-nodes-node-dedicated-gpu-noschedule",
+    "command": "kubectl taint nodes node dedicated=gpu:NoSchedule-",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl taint nodes node dedicated=gpu:NoSchedule-` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl taint nodes node dedicated=gpu:NoSchedule-",
+    "example": "kubectl taint nodes node dedicated=gpu:NoSchedule-",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-api-resources-namespaced-true",
+    "command": "kubectl api-resources --namespaced=true",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl api-resources --namespaced=true` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl api-resources --namespaced=true",
+    "example": "kubectl api-resources --namespaced=true",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-api-resources-verbs-list",
+    "command": "kubectl api-resources --verbs=list",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl api-resources --verbs=list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl api-resources --verbs=list",
+    "example": "kubectl api-resources --verbs=list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-explain-deployment-spec-strategy",
+    "command": "kubectl explain deployment.spec.strategy",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl explain deployment.spec.strategy` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl explain deployment.spec.strategy",
+    "example": "kubectl explain deployment.spec.strategy",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-apply-server-side-f-file-yaml",
+    "command": "kubectl apply --server-side -f file.yaml",
+    "title": "Kubernetes deploy/run/install command",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl apply --server-side -f file.yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl apply --server-side -f file.yaml",
+    "example": "kubectl apply --server-side -f file.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-replace-f-file-yaml",
+    "command": "kubectl replace -f file.yaml",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl replace -f file.yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl replace -f file.yaml",
+    "example": "kubectl replace -f file.yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-create-f-file-yaml-dry-run-client-o-yaml",
+    "command": "kubectl create -f file.yaml --dry-run=client -o yaml",
+    "title": "Kubernetes resource create/generate karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl create -f file.yaml --dry-run=client -o yaml` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl create -f file.yaml --dry-run=client -o yaml",
+    "example": "kubectl create -f file.yaml --dry-run=client -o yaml",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-raw-healthz",
+    "command": "kubectl get --raw /healthz",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl get --raw /healthz` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get --raw /healthz",
+    "example": "kubectl get --raw /healthz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-get-raw-metrics",
+    "command": "kubectl get --raw /metrics",
+    "title": "Kubernetes status/details verify karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl get --raw /metrics` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl get --raw /metrics",
+    "example": "kubectl get --raw /metrics",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "kubernetes-kubectl-proxy-port-8001",
+    "command": "kubectl proxy --port=8001",
+    "title": "Kubernetes command use karna",
+    "category": "Kubernetes",
+    "difficulty": "Advanced",
+    "use_case": "Kubernetes me `kubectl proxy --port=8001` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "kubectl proxy --port=8001",
+    "example": "kubectl proxy --port=8001",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "kubectl get pods -A",
+      "kubectl describe pod",
+      "kubectl logs"
+    ],
+    "tags": [
+      "kubernetes",
+      "kubectl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/linux.commands.json b/public/data/linux.commands.json
index 4554a81..ab930de 100644
--- a/public/data/linux.commands.json
+++ b/public/data/linux.commands.json
@@ -2030,5 +2030,2433 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "linux-ls-lh",
+    "command": "ls -lh",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ls -lh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ls -lh",
+    "example": "ls -lh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ls",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ls-ltr",
+    "command": "ls -ltr",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ls -ltr` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ls -ltr",
+    "example": "ls -ltr",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ls",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ls-r",
+    "command": "ls -R",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `ls -R` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ls -R",
+    "example": "ls -R",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ls",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tree-l-2",
+    "command": "tree -L 2",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `tree -L 2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tree -L 2",
+    "example": "tree -L 2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tree",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-realpath-file-txt",
+    "command": "realpath file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `realpath file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "realpath file.txt",
+    "example": "realpath file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "realpath",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-readlink-linkname",
+    "command": "readlink linkname",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Intermediate",
+    "use_case": "Linux me `readlink linkname` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "readlink linkname",
+    "example": "readlink linkname",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "readlink",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-install-m-755-script-sh-usr-local-bin-script",
+    "command": "install -m 755 script.sh /usr/local/bin/script",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `install -m 755 script.sh /usr/local/bin/script` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "install -m 755 script.sh /usr/local/bin/script",
+    "example": "install -m 755 script.sh /usr/local/bin/script",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "install",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-mktemp",
+    "command": "mktemp",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `mktemp` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mktemp",
+    "example": "mktemp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "mktemp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-mktemp-d",
+    "command": "mktemp -d",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `mktemp -d` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "mktemp -d",
+    "example": "mktemp -d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "mktemp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-split-b-100m-largefile-part",
+    "command": "split -b 100M largefile part_",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `split -b 100M largefile part_` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "split -b 100M largefile part_",
+    "example": "split -b 100M largefile part_",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "split",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-csplit-file-txt-pattern",
+    "command": "csplit file.txt /pattern/",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `csplit file.txt /pattern/` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "csplit file.txt /pattern/",
+    "example": "csplit file.txt /pattern/",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "csplit",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-comm-file1-file2",
+    "command": "comm file1 file2",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `comm file1 file2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "comm file1 file2",
+    "example": "comm file1 file2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "comm",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-diff-u-old-new",
+    "command": "diff -u old new",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `diff -u old new` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "diff -u old new",
+    "example": "diff -u old new",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "diff",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-patch-p1-fix-patch",
+    "command": "patch -p1 < fix.patch",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `patch -p1 < fix.patch` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "patch -p1 < fix.patch",
+    "example": "patch -p1 < fix.patch",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "patch",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-cmp-file1-file2",
+    "command": "cmp file1 file2",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `cmp file1 file2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cmp file1 file2",
+    "example": "cmp file1 file2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cmp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sha256sum-file",
+    "command": "sha256sum file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sha256sum file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sha256sum file",
+    "example": "sha256sum file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "sha256sum",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-md5sum-file",
+    "command": "md5sum file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `md5sum file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "md5sum file",
+    "example": "md5sum file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "md5sum",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-basename-pwd",
+    "command": "basename $(pwd)",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `basename $(pwd)` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "basename $(pwd)",
+    "example": "basename $(pwd)",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "basename",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dirname-var-www-html-index-html",
+    "command": "dirname /var/www/html/index.html",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dirname /var/www/html/index.html` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dirname /var/www/html/index.html",
+    "example": "dirname /var/www/html/index.html",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dirname",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-yes-head",
+    "command": "yes | head",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `yes | head` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "yes | head",
+    "example": "yes | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "yes",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-seq-1-10",
+    "command": "seq 1 10",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `seq 1 10` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "seq 1 10",
+    "example": "seq 1 10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "seq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-shuf-file-txt",
+    "command": "shuf file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `shuf file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "shuf file.txt",
+    "example": "shuf file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "shuf",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-paste-file1-file2",
+    "command": "paste file1 file2",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `paste file1 file2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "paste file1 file2",
+    "example": "paste file1 file2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "paste",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-column-t-file-txt",
+    "command": "column -t file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `column -t file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "column -t file.txt",
+    "example": "column -t file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "column",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-expand-file-txt",
+    "command": "expand file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `expand file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "expand file.txt",
+    "example": "expand file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "expand",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-unexpand-file-txt",
+    "command": "unexpand file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `unexpand file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "unexpand file.txt",
+    "example": "unexpand file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "unexpand",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-iconv-f-utf-8-t-ascii-file-txt",
+    "command": "iconv -f utf-8 -t ascii file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `iconv -f utf-8 -t ascii file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "iconv -f utf-8 -t ascii file.txt",
+    "example": "iconv -f utf-8 -t ascii file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "iconv",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dos2unix-file-txt",
+    "command": "dos2unix file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dos2unix file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dos2unix file.txt",
+    "example": "dos2unix file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dos2unix",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-unix2dos-file-txt",
+    "command": "unix2dos file.txt",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `unix2dos file.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "unix2dos file.txt",
+    "example": "unix2dos file.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "unix2dos",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-stat-c-a-file",
+    "command": "stat -c %a file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `stat -c %a file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "stat -c %a file",
+    "example": "stat -c %a file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "stat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-chmod-x-script-sh",
+    "command": "chmod +x script.sh",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `chmod +x script.sh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "chmod +x script.sh",
+    "example": "chmod +x script.sh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "chmod",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-chmod-r-u-rwx-g-rx-o-rwx-folder",
+    "command": "chmod -R u+rwX,g+rX,o-rwx folder",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `chmod -R u+rwX,g+rX,o-rwx folder` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "chmod -R u+rwX,g+rX,o-rwx folder",
+    "example": "chmod -R u+rwX,g+rX,o-rwx folder",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "chmod",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-chgrp-www-data-file",
+    "command": "chgrp www-data file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `chgrp www-data file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "chgrp www-data file",
+    "example": "chgrp www-data file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "chgrp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-umask",
+    "command": "umask",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `umask` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "umask",
+    "example": "umask",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "umask",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-getfacl-file",
+    "command": "getfacl file",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `getfacl file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "getfacl file",
+    "example": "getfacl file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "getfacl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-setfacl-m-u-www-data-rwx-folder",
+    "command": "setfacl -m u:www-data:rwx folder",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `setfacl -m u:www-data:rwx folder` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "setfacl -m u:www-data:rwx folder",
+    "example": "setfacl -m u:www-data:rwx folder",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "setfacl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lsattr-file",
+    "command": "lsattr file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lsattr file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lsattr file",
+    "example": "lsattr file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lsattr",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-chattr-i-file",
+    "command": "chattr +i file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `chattr +i file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "chattr +i file",
+    "example": "chattr +i file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "chattr",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-chattr-i-file",
+    "command": "chattr -i file",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `chattr -i file` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "chattr -i file",
+    "example": "chattr -i file",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "chattr",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-journalctl-since-10-minutes-ago",
+    "command": "sudo journalctl --since \"10 minutes ago\"",
+    "title": "Linux logs/debugging command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo journalctl --since \"10 minutes ago\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo journalctl --since \"10 minutes ago\"",
+    "example": "sudo journalctl --since \"10 minutes ago\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-journalctl-k",
+    "command": "sudo journalctl -k",
+    "title": "Linux logs/debugging command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo journalctl -k` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo journalctl -k",
+    "example": "sudo journalctl -k",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-dmesg-w",
+    "command": "sudo dmesg -w",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo dmesg -w` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo dmesg -w",
+    "example": "sudo dmesg -w",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-dmesg-level-err-warn",
+    "command": "sudo dmesg --level=err,warn",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo dmesg --level=err,warn` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo dmesg --level=err,warn",
+    "example": "sudo dmesg --level=err,warn",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-sysctl-a",
+    "command": "sudo sysctl -a",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo sysctl -a` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo sysctl -a",
+    "example": "sudo sysctl -a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-sysctl-vm-swappiness",
+    "command": "sudo sysctl vm.swappiness",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo sysctl vm.swappiness` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo sysctl vm.swappiness",
+    "example": "sudo sysctl vm.swappiness",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-sysctl-w-vm-swappiness-10",
+    "command": "sudo sysctl -w vm.swappiness=10",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo sysctl -w vm.swappiness=10` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo sysctl -w vm.swappiness=10",
+    "example": "sudo sysctl -w vm.swappiness=10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ulimit-a",
+    "command": "ulimit -a",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ulimit -a` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ulimit -a",
+    "example": "ulimit -a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ulimit",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ulimit-n",
+    "command": "ulimit -n",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ulimit -n` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ulimit -n",
+    "example": "ulimit -n",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ulimit",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-prlimit-pid-1234",
+    "command": "prlimit --pid 1234",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `prlimit --pid 1234` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "prlimit --pid 1234",
+    "example": "prlimit --pid 1234",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "prlimit",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lsof",
+    "command": "lsof",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lsof` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lsof",
+    "example": "lsof",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lsof",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lsof-i",
+    "command": "lsof -i",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lsof -i` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lsof -i",
+    "example": "lsof -i",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lsof",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lsof-itcp-stcp-listen",
+    "command": "lsof -iTCP -sTCP:LISTEN",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lsof -iTCP -sTCP:LISTEN` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lsof -iTCP -sTCP:LISTEN",
+    "example": "lsof -iTCP -sTCP:LISTEN",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lsof",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-fuser-k-8080-tcp",
+    "command": "fuser -k 8080/tcp",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `fuser -k 8080/tcp` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "fuser -k 8080/tcp",
+    "example": "fuser -k 8080/tcp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "fuser",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ss-tunap",
+    "command": "ss -tunap",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ss -tunap` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ss -tunap",
+    "example": "ss -tunap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ss",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ss-ltnp",
+    "command": "ss -ltnp",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ss -ltnp` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ss -ltnp",
+    "example": "ss -ltnp",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ss",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-a",
+    "command": "ip a",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip a` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip a",
+    "example": "ip a",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-link",
+    "command": "ip link",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip link` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip link",
+    "example": "ip link",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-route-show",
+    "command": "ip route show",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip route show` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip route show",
+    "example": "ip route show",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-neigh",
+    "command": "ip neigh",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip neigh` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip neigh",
+    "example": "ip neigh",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-addr-add-192-168-1-10-24-dev-eth0",
+    "command": "ip addr add 192.168.1.10/24 dev eth0",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip addr add 192.168.1.10/24 dev eth0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip addr add 192.168.1.10/24 dev eth0",
+    "example": "ip addr add 192.168.1.10/24 dev eth0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ip-link-set-eth0-up",
+    "command": "ip link set eth0 up",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ip link set eth0 up` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip link set eth0 up",
+    "example": "ip link set eth0 up",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nmcli-device-status",
+    "command": "nmcli device status",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `nmcli device status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nmcli device status",
+    "example": "nmcli device status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nmcli",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nmcli-connection-show",
+    "command": "nmcli connection show",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `nmcli connection show` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nmcli connection show",
+    "example": "nmcli connection show",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nmcli",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-resolvectl-status",
+    "command": "resolvectl status",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `resolvectl status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "resolvectl status",
+    "example": "resolvectl status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "resolvectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-cat-etc-resolv-conf",
+    "command": "cat /etc/resolv.conf",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `cat /etc/resolv.conf` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cat /etc/resolv.conf",
+    "example": "cat /etc/resolv.conf",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-dig-trace-example-com",
+    "command": "dig +trace example.com",
+    "title": "Linux logs/debugging command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `dig +trace example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dig +trace example.com",
+    "example": "dig +trace example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "dig",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-host-example-com",
+    "command": "host example.com",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `host example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "host example.com",
+    "example": "host example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "host",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-whois-example-com",
+    "command": "whois example.com",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `whois example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "whois example.com",
+    "example": "whois example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "whois",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-curl-s-ifconfig-me",
+    "command": "curl -s ifconfig.me",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `curl -s ifconfig.me` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl -s ifconfig.me",
+    "example": "curl -s ifconfig.me",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-wget-https-example-com-file-zip",
+    "command": "wget https://example.com/file.zip",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `wget https://example.com/file.zip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "wget https://example.com/file.zip",
+    "example": "wget https://example.com/file.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "wget",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-wget-c-https-example-com-file-zip",
+    "command": "wget -c https://example.com/file.zip",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `wget -c https://example.com/file.zip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "wget -c https://example.com/file.zip",
+    "example": "wget -c https://example.com/file.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "wget",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-aria2c-https-example-com-file-zip",
+    "command": "aria2c https://example.com/file.zip",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `aria2c https://example.com/file.zip` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "aria2c https://example.com/file.zip",
+    "example": "aria2c https://example.com/file.zip",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "aria2c",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nc-l-8080",
+    "command": "nc -l 8080",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `nc -l 8080` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nc -l 8080",
+    "example": "nc -l 8080",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-nc-vz-host-443",
+    "command": "nc -vz host 443",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `nc -vz host 443` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nc -vz host 443",
+    "example": "nc -vz host 443",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "nc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-socat-tcp-host-80",
+    "command": "socat - TCP:host:80",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `socat - TCP:host:80` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "socat - TCP:host:80",
+    "example": "socat - TCP:host:80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "socat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tcpdump-nn-i-any",
+    "command": "tcpdump -nn -i any",
+    "title": "Linux backup/export command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `tcpdump -nn -i any` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tcpdump -nn -i any",
+    "example": "tcpdump -nn -i any",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tcpdump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tcpdump-i-eth0-host-1-2-3-4",
+    "command": "tcpdump -i eth0 host 1.2.3.4",
+    "title": "Linux backup/export command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `tcpdump -i eth0 host 1.2.3.4` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tcpdump -i eth0 host 1.2.3.4",
+    "example": "tcpdump -i eth0 host 1.2.3.4",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tcpdump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-tcpdump-i-eth0-port-443-w-capture-pcap",
+    "command": "tcpdump -i eth0 port 443 -w capture.pcap",
+    "title": "Linux backup/export command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `tcpdump -i eth0 port 443 -w capture.pcap` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tcpdump -i eth0 port 443 -w capture.pcap",
+    "example": "tcpdump -i eth0 port 443 -w capture.pcap",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "tcpdump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-scp-r-folder-user-host-path",
+    "command": "scp -r folder user@host:/path",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `scp -r folder user@host:/path` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "scp -r folder user@host:/path",
+    "example": "scp -r folder user@host:/path",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "scp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sftp-user-host",
+    "command": "sftp user@host",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sftp user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sftp user@host",
+    "example": "sftp user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "sftp",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-i-key-pem-user-host",
+    "command": "ssh -i key.pem user@host",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh -i key.pem user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh -i key.pem user@host",
+    "example": "ssh -i key.pem user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-l-8080-localhost-80-user-host",
+    "command": "ssh -L 8080:localhost:80 user@host",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh -L 8080:localhost:80 user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh -L 8080:localhost:80 user@host",
+    "example": "ssh -L 8080:localhost:80 user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-n-l-5432-localhost-5432-user-host",
+    "command": "ssh -N -L 5432:localhost:5432 user@host",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh -N -L 5432:localhost:5432 user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh -N -L 5432:localhost:5432 user@host",
+    "example": "ssh -N -L 5432:localhost:5432 user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-o-stricthostkeychecking-no-user-host",
+    "command": "ssh -o StrictHostKeyChecking=no user@host",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh -o StrictHostKeyChecking=no user@host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh -o StrictHostKeyChecking=no user@host",
+    "example": "ssh -o StrictHostKeyChecking=no user@host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-agent-bash",
+    "command": "ssh-agent bash",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh-agent bash` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh-agent bash",
+    "example": "ssh-agent bash",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh-agent",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-add-ssh-id-ed25519",
+    "command": "ssh-add ~/.ssh/id_ed25519",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh-add ~/.ssh/id_ed25519` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh-add ~/.ssh/id_ed25519",
+    "example": "ssh-add ~/.ssh/id_ed25519",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh-add",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-ssh-keyscan-github-com-ssh-known-hosts",
+    "command": "ssh-keyscan github.com >> ~/.ssh/known_hosts",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `ssh-keyscan github.com >> ~/.ssh/known_hosts` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ssh-keyscan github.com >> ~/.ssh/known_hosts",
+    "example": "ssh-keyscan github.com >> ~/.ssh/known_hosts",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "ssh-keyscan",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-lastlog",
+    "command": "lastlog",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `lastlog` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lastlog",
+    "example": "lastlog",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "lastlog",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-groups-user",
+    "command": "groups user",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `groups user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "groups user",
+    "example": "groups user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "groups",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-getent-passwd-user",
+    "command": "getent passwd user",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `getent passwd user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "getent passwd user",
+    "example": "getent passwd user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "getent",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-getent-group-sudo",
+    "command": "getent group sudo",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `getent group sudo` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "getent group sudo",
+    "example": "getent group sudo",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "getent",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-userdel-r-user",
+    "command": "sudo userdel -r user",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo userdel -r user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo userdel -r user",
+    "example": "sudo userdel -r user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-groupadd-devops",
+    "command": "sudo groupadd devops",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo groupadd devops` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo groupadd devops",
+    "example": "sudo groupadd devops",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-gpasswd-a-user-group",
+    "command": "sudo gpasswd -a user group",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo gpasswd -a user group` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo gpasswd -a user group",
+    "example": "sudo gpasswd -a user group",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-crontab-e-u-www-data",
+    "command": "sudo crontab -e -u www-data",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo crontab -e -u www-data` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo crontab -e -u www-data",
+    "example": "sudo crontab -e -u www-data",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-list-units-type-service",
+    "command": "systemctl list-units --type=service",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl list-units --type=service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl list-units --type=service",
+    "example": "systemctl list-units --type=service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-list-timers",
+    "command": "systemctl list-timers",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl list-timers` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl list-timers",
+    "example": "systemctl list-timers",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-cat-nginx",
+    "command": "systemctl cat nginx",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl cat nginx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl cat nginx",
+    "example": "systemctl cat nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-show-nginx",
+    "command": "systemctl show nginx",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl show nginx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl show nginx",
+    "example": "systemctl show nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-daemon-reload",
+    "command": "systemctl daemon-reload",
+    "title": "Linux restart/reload/refresh karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl daemon-reload` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl daemon-reload",
+    "example": "systemctl daemon-reload",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-mask-service",
+    "command": "systemctl mask service",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl mask service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl mask service",
+    "example": "systemctl mask service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-systemctl-unmask-service",
+    "command": "systemctl unmask service",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `systemctl unmask service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl unmask service",
+    "example": "systemctl unmask service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-loginctl-list-sessions",
+    "command": "loginctl list-sessions",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `loginctl list-sessions` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "loginctl list-sessions",
+    "example": "loginctl list-sessions",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "loginctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-loginctl-show-session-session-id",
+    "command": "loginctl show-session SESSION_ID",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `loginctl show-session SESSION_ID` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "loginctl show-session SESSION_ID",
+    "example": "loginctl show-session SESSION_ID",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "loginctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-timedatectl-set-timezone-asia-kolkata",
+    "command": "timedatectl set-timezone Asia/Kolkata",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `timedatectl set-timezone Asia/Kolkata` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "timedatectl set-timezone Asia/Kolkata",
+    "example": "timedatectl set-timezone Asia/Kolkata",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "timedatectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-localectl-status",
+    "command": "localectl status",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `localectl status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "localectl status",
+    "example": "localectl status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "localectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-hostnamectl-set-hostname-server1",
+    "command": "hostnamectl set-hostname server1",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `hostnamectl set-hostname server1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "hostnamectl set-hostname server1",
+    "example": "hostnamectl set-hostname server1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "hostnamectl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-logrotate-d-etc-logrotate-conf",
+    "command": "sudo logrotate -d /etc/logrotate.conf",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo logrotate -d /etc/logrotate.conf` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo logrotate -d /etc/logrotate.conf",
+    "example": "sudo logrotate -d /etc/logrotate.conf",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-logrotate-f-etc-logrotate-conf",
+    "command": "sudo logrotate -f /etc/logrotate.conf",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo logrotate -f /etc/logrotate.conf` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo logrotate -f /etc/logrotate.conf",
+    "example": "sudo logrotate -f /etc/logrotate.conf",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-journalctl-vacuum-size-1g",
+    "command": "journalctl --vacuum-size=1G",
+    "title": "Linux logs/debugging command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `journalctl --vacuum-size=1G` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl --vacuum-size=1G",
+    "example": "journalctl --vacuum-size=1G",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-journalctl-vacuum-time-3d",
+    "command": "journalctl --vacuum-time=3d",
+    "title": "Linux logs/debugging command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `journalctl --vacuum-time=3d` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl --vacuum-time=3d",
+    "example": "journalctl --vacuum-time=3d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-list-upgradable",
+    "command": "sudo apt list --upgradable",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt list --upgradable` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo apt list --upgradable",
+    "example": "sudo apt list --upgradable",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-mark-hold-package",
+    "command": "sudo apt-mark hold package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt-mark hold package` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo apt-mark hold package",
+    "example": "sudo apt-mark hold package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-apt-mark-unhold-package",
+    "command": "sudo apt-mark unhold package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo apt-mark unhold package` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo apt-mark unhold package",
+    "example": "sudo apt-mark unhold package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-apt-cache-search-package",
+    "command": "apt-cache search package",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `apt-cache search package` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "apt-cache search package",
+    "example": "apt-cache search package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "apt-cache",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-apt-file-search-filename",
+    "command": "apt-file search filename",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `apt-file search filename` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "apt-file search filename",
+    "example": "apt-file search filename",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "linux",
+      "apt-file",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-dpkg-reconfigure-tzdata",
+    "command": "sudo dpkg-reconfigure tzdata",
+    "title": "Linux command use karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo dpkg-reconfigure tzdata` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo dpkg-reconfigure tzdata",
+    "example": "sudo dpkg-reconfigure tzdata",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-update-alternatives-config-editor",
+    "command": "sudo update-alternatives --config editor",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo update-alternatives --config editor` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo update-alternatives --config editor",
+    "example": "sudo update-alternatives --config editor",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-add-apt-repository-ppa-repo-name",
+    "command": "sudo add-apt-repository ppa:repo/name",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo add-apt-repository ppa:repo/name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo add-apt-repository ppa:repo/name",
+    "example": "sudo add-apt-repository ppa:repo/name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-snap-list",
+    "command": "sudo snap list",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo snap list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo snap list",
+    "example": "sudo snap list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-snap-install-package",
+    "command": "sudo snap install package",
+    "title": "Linux deploy/run/install command",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo snap install package` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo snap install package",
+    "example": "sudo snap install package",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-systemctl-isolate-rescue-target",
+    "command": "sudo systemctl isolate rescue.target",
+    "title": "Linux status/details verify karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo systemctl isolate rescue.target` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo systemctl isolate rescue.target",
+    "example": "sudo systemctl isolate rescue.target",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-init-6",
+    "command": "sudo init 6",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo init 6` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo init 6",
+    "example": "sudo init 6",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "linux-sudo-init-0",
+    "command": "sudo init 0",
+    "title": "Linux resource create/generate karna",
+    "category": "Linux",
+    "difficulty": "Advanced",
+    "use_case": "Linux me `sudo init 0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo init 0",
+    "example": "sudo init 0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "linux",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/logs-debugging.commands.json b/public/data/logs-debugging.commands.json
index 47497d3..1fa08b2 100644
--- a/public/data/logs-debugging.commands.json
+++ b/public/data/logs-debugging.commands.json
@@ -919,5 +919,689 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "logs-debugging-journalctl-f",
+    "command": "journalctl -f",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Beginner",
+    "use_case": "Logs / Debugging me `journalctl -f` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl -f",
+    "example": "journalctl -f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-n-200",
+    "command": "journalctl -n 200",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl -n 200` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl -n 200",
+    "example": "journalctl -n 200",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-u-docker-f",
+    "command": "journalctl -u docker -f",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl -u docker -f` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl -u docker -f",
+    "example": "journalctl -u docker -f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-u-containerd-f",
+    "command": "journalctl -u containerd -f",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl -u containerd -f` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl -u containerd -f",
+    "example": "journalctl -u containerd -f",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-since-2026-06-19-10-00-until-2026-06-19-11-00",
+    "command": "journalctl --since \"2026-06-19 10:00\" --until \"2026-06-19 11:00\"",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl --since \"2026-06-19 10:00\" --until \"2026-06-19 11:00\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl --since \"2026-06-19 10:00\" --until \"2026-06-19 11:00\"",
+    "example": "journalctl --since \"2026-06-19 10:00\" --until \"2026-06-19 11:00\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-o-json-pretty-n-10",
+    "command": "journalctl -o json-pretty -n 10",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl -o json-pretty -n 10` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl -o json-pretty -n 10",
+    "example": "journalctl -o json-pretty -n 10",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-pid-1234",
+    "command": "journalctl _PID=1234",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl _PID=1234` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl _PID=1234",
+    "example": "journalctl _PID=1234",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-journalctl-systemd-unit-nginx-service",
+    "command": "journalctl _SYSTEMD_UNIT=nginx.service",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `journalctl _SYSTEMD_UNIT=nginx.service` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "journalctl _SYSTEMD_UNIT=nginx.service",
+    "example": "journalctl _SYSTEMD_UNIT=nginx.service",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "journalctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-tail-f-app-log",
+    "command": "tail -F app.log",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `tail -F app.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tail -F app.log",
+    "example": "tail -F app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "tail",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-multitail-var-log-syslog-var-log-auth-log",
+    "command": "multitail /var/log/syslog /var/log/auth.log",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `multitail /var/log/syslog /var/log/auth.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "multitail /var/log/syslog /var/log/auth.log",
+    "example": "multitail /var/log/syslog /var/log/auth.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "multitail",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-ril-error-var-log",
+    "command": "grep -Ril \"error\" /var/log",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `grep -Ril \"error\" /var/log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "grep -Ril \"error\" /var/log",
+    "example": "grep -Ril \"error\" /var/log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-grep-e-error-warn-critical-app-log",
+    "command": "grep -E \"ERROR|WARN|CRITICAL\" app.log",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `grep -E \"ERROR|WARN|CRITICAL\" app.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "grep -E \"ERROR|WARN|CRITICAL\" app.log",
+    "example": "grep -E \"ERROR|WARN|CRITICAL\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "grep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-egrep-i-timeout-refused-reset-broken-pipe-app-log",
+    "command": "egrep -i \"timeout|refused|reset|broken pipe\" app.log",
+    "title": "Logs / Debugging risky cleanup/delete command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `egrep -i \"timeout|refused|reset|broken pipe\" app.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "egrep -i \"timeout|refused|reset|broken pipe\" app.log",
+    "example": "egrep -i \"timeout|refused|reset|broken pipe\" app.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "egrep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-zgrep-i-error-app-log-gz",
+    "command": "zgrep -i error app.log.gz",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `zgrep -i error app.log.gz` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "zgrep -i error app.log.gz",
+    "example": "zgrep -i error app.log.gz",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "zgrep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-zcat-app-log-gz-grep-error",
+    "command": "zcat app.log.gz | grep ERROR",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `zcat app.log.gz | grep ERROR` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "zcat app.log.gz | grep ERROR",
+    "example": "zcat app.log.gz | grep ERROR",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "zcat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-awk-9-500-print-access-log",
+    "command": "awk '$9 >= 500 {print}' access.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `awk '$9 >= 500 {print}' access.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "awk '$9 >= 500 {print}' access.log",
+    "example": "awk '$9 >= 500 {print}' access.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-awk-print-7-access-log-sort-uniq-c-sort-nr-head",
+    "command": "awk '{print $7}' access.log | sort | uniq -c | sort -nr | head",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `awk '{print $7}' access.log | sort | uniq -c | sort -nr | head` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "awk '{print $7}' access.log | sort | uniq -c | sort -nr | head",
+    "example": "awk '{print $7}' access.log | sort | uniq -c | sort -nr | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-awk-sum-10-end-print-sum-access-log",
+    "command": "awk '{sum+=$10} END {print sum}' access.log",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `awk '{sum+=$10} END {print sum}' access.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "awk '{sum+=$10} END {print sum}' access.log",
+    "example": "awk '{sum+=$10} END {print sum}' access.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "awk",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-cut-d-f1-access-log-sort-uniq-c-sort-nr-head",
+    "command": "cut -d\" \" -f1 access.log | sort | uniq -c | sort -nr | head",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `cut -d\" \" -f1 access.log | sort | uniq -c | sort -nr | head` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cut -d\" \" -f1 access.log | sort | uniq -c | sort -nr | head",
+    "example": "cut -d\" \" -f1 access.log | sort | uniq -c | sort -nr | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "cut",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-sed-n-start-end-p-file-log",
+    "command": "sed -n '/START/,/END/p' file.log",
+    "title": "Logs / Debugging deploy/run/install command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `sed -n '/START/,/END/p' file.log` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sed -n '/START/,/END/p' file.log",
+    "example": "sed -n '/START/,/END/p' file.log",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "sed",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-jq-r-level-logs-json",
+    "command": "jq -r .level logs.json",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `jq -r .level logs.json` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "jq -r .level logs.json",
+    "example": "jq -r .level logs.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "jq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-jq-r-select-level-error-logs-json",
+    "command": "jq -r \"select(.level==\"error\")\" logs.json",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `jq -r \"select(.level==\"error\")\" logs.json` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "jq -r \"select(.level==\"error\")\" logs.json",
+    "example": "jq -r \"select(.level==\"error\")\" logs.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "jq",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-gron-file-json",
+    "command": "gron file.json",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `gron file.json` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gron file.json",
+    "example": "gron file.json",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "gron",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-python3-m-http-server-8080",
+    "command": "python3 -m http.server 8080",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `python3 -m http.server 8080` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "python3 -m http.server 8080",
+    "example": "python3 -m http.server 8080",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "python3",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-v-connect-timeout-5-http-host",
+    "command": "curl -v --connect-timeout 5 http://host",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl -v --connect-timeout 5 http://host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl -v --connect-timeout 5 http://host",
+    "example": "curl -v --connect-timeout 5 http://host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-max-time-10-http-host",
+    "command": "curl --max-time 10 http://host",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl --max-time 10 http://host` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl --max-time 10 http://host",
+    "example": "curl --max-time 10 http://host",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-h-host-example-com-http-127-0-0-1",
+    "command": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl -H \"Host: example.com\" http://127.0.0.1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "example": "curl -H \"Host: example.com\" http://127.0.0.1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-curl-x-post-h-content-type-application-json-d-http-localhost-api",
+    "command": "curl -X POST -H \"Content-Type: application/json\" -d '{}' http://localhost/api",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `curl -X POST -H \"Content-Type: application/json\" -d '{}' http://localhost/api` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl -X POST -H \"Content-Type: application/json\" -d '{}' http://localhost/api",
+    "example": "curl -X POST -H \"Content-Type: application/json\" -d '{}' http://localhost/api",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "curl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-http-get-http-localhost",
+    "command": "http GET http://localhost",
+    "title": "Logs / Debugging status/details verify karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `http GET http://localhost` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "http GET http://localhost",
+    "example": "http GET http://localhost",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "http",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-tcpdump-a-s-0-port-80",
+    "command": "tcpdump -A -s 0 port 80",
+    "title": "Logs / Debugging backup/export command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `tcpdump -A -s 0 port 80` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "tcpdump -A -s 0 port 80",
+    "example": "tcpdump -A -s 0 port 80",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "tcpdump",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-strace-f-o-trace-log-command",
+    "command": "strace -f -o trace.log command",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `strace -f -o trace.log command` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "strace -f -o trace.log command",
+    "example": "strace -f -o trace.log command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "strace",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-strace-e-trace-file-command",
+    "command": "strace -e trace=file command",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `strace -e trace=file command` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "strace -e trace=file command",
+    "example": "strace -e trace=file command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "strace",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-strace-e-trace-network-command",
+    "command": "strace -e trace=network command",
+    "title": "Logs / Debugging logs/debugging command",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `strace -e trace=network command` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "strace -e trace=network command",
+    "example": "strace -e trace=network command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "strace",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-gdb-p-pid",
+    "command": "gdb -p pid",
+    "title": "Logs / Debugging command use karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `gdb -p pid` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "gdb -p pid",
+    "example": "gdb -p pid",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "gdb",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-coredumpctl-list",
+    "command": "coredumpctl list",
+    "title": "Logs / Debugging status/details verify karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `coredumpctl list` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "coredumpctl list",
+    "example": "coredumpctl list",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "coredumpctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "logs-debugging-coredumpctl-info-pid",
+    "command": "coredumpctl info pid",
+    "title": "Logs / Debugging status/details verify karna",
+    "category": "Logs / Debugging",
+    "difficulty": "Intermediate",
+    "use_case": "Logs / Debugging me `coredumpctl info pid` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "coredumpctl info pid",
+    "example": "coredumpctl info pid",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "logs",
+      "coredumpctl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/security.commands.json b/public/data/security.commands.json
index 21af8d5..aa9e163 100644
--- a/public/data/security.commands.json
+++ b/public/data/security.commands.json
@@ -930,5 +930,797 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "server-security-sudo-lastb",
+    "command": "sudo lastb",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo lastb` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo lastb",
+    "example": "sudo lastb",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-faillock-user-user",
+    "command": "sudo faillock --user user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo faillock --user user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo faillock --user user",
+    "example": "sudo faillock --user user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-pam-tally2-user-user",
+    "command": "sudo pam_tally2 --user user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo pam_tally2 --user user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo pam_tally2 --user user",
+    "example": "sudo pam_tally2 --user user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-passwd-s-user",
+    "command": "sudo passwd -S user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo passwd -S user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo passwd -S user",
+    "example": "sudo passwd -S user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-usermod-l-user",
+    "command": "sudo usermod -L user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo usermod -L user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo usermod -L user",
+    "example": "sudo usermod -L user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-usermod-u-user",
+    "command": "sudo usermod -U user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo usermod -U user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo usermod -U user",
+    "example": "sudo usermod -U user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-usermod-s-usr-sbin-nologin-user",
+    "command": "sudo usermod -s /usr/sbin/nologin user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo usermod -s /usr/sbin/nologin user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo usermod -s /usr/sbin/nologin user",
+    "example": "sudo usermod -s /usr/sbin/nologin user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-usermod-ag-docker-user",
+    "command": "sudo usermod -aG docker user",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo usermod -aG docker user` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo usermod -aG docker user",
+    "example": "sudo usermod -aG docker user",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-chown-root-root-etc-ssh-sshd-config",
+    "command": "sudo chown root:root /etc/ssh/sshd_config",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo chown root:root /etc/ssh/sshd_config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo chown root:root /etc/ssh/sshd_config",
+    "example": "sudo chown root:root /etc/ssh/sshd_config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-chmod-600-etc-ssh-sshd-config",
+    "command": "sudo chmod 600 /etc/ssh/sshd_config",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo chmod 600 /etc/ssh/sshd_config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo chmod 600 /etc/ssh/sshd_config",
+    "example": "sudo chmod 600 /etc/ssh/sshd_config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-grep-e-permitrootlogin-passwordauthentication-pubkeyauthentication-etc-ssh-sshd-config",
+    "command": "sudo grep -E \"PermitRootLogin|PasswordAuthentication|PubkeyAuthentication\" /etc/ssh/sshd_config",
+    "title": "Server Security logs/debugging command",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo grep -E \"PermitRootLogin|PasswordAuthentication|PubkeyAuthentication\" /etc/ssh/sshd_config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo grep -E \"PermitRootLogin|PasswordAuthentication|PubkeyAuthentication\" /etc/ssh/sshd_config",
+    "example": "sudo grep -E \"PermitRootLogin|PasswordAuthentication|PubkeyAuthentication\" /etc/ssh/sshd_config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-sshd-t-grep-permitrootlogin",
+    "command": "sudo sshd -T | grep permitrootlogin",
+    "title": "Server Security logs/debugging command",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo sshd -T | grep permitrootlogin` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo sshd -T | grep permitrootlogin",
+    "example": "sudo sshd -T | grep permitrootlogin",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-sshd-t-grep-passwordauthentication",
+    "command": "sudo sshd -T | grep passwordauthentication",
+    "title": "Server Security logs/debugging command",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo sshd -T | grep passwordauthentication` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo sshd -T | grep passwordauthentication",
+    "example": "sudo sshd -T | grep passwordauthentication",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-fail2ban-client-banned",
+    "command": "sudo fail2ban-client banned",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo fail2ban-client banned` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo fail2ban-client banned",
+    "example": "sudo fail2ban-client banned",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-fail2ban-client-unban-ip",
+    "command": "sudo fail2ban-client unban IP",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo fail2ban-client unban IP` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo fail2ban-client unban IP",
+    "example": "sudo fail2ban-client unban IP",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-fail2ban-client-reload",
+    "command": "sudo fail2ban-client reload",
+    "title": "Server Security restart/reload/refresh karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo fail2ban-client reload` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo fail2ban-client reload",
+    "example": "sudo fail2ban-client reload",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ufw-logging-on",
+    "command": "sudo ufw logging on",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ufw logging on` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw logging on",
+    "example": "sudo ufw logging on",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ufw-logging-medium",
+    "command": "sudo ufw logging medium",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ufw logging medium` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ufw logging medium",
+    "example": "sudo ufw logging medium",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-auditctl-w-etc-passwd-p-wa-k-passwd-changes",
+    "command": "sudo auditctl -w /etc/passwd -p wa -k passwd_changes",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo auditctl -w /etc/passwd -p wa -k passwd_changes` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo auditctl -w /etc/passwd -p wa -k passwd_changes",
+    "example": "sudo auditctl -w /etc/passwd -p wa -k passwd_changes",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-auditctl-w-etc-ssh-sshd-config-p-wa-k-sshd-config",
+    "command": "sudo auditctl -w /etc/ssh/sshd_config -p wa -k sshd_config",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo auditctl -w /etc/ssh/sshd_config -p wa -k sshd_config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo auditctl -w /etc/ssh/sshd_config -p wa -k sshd_config",
+    "example": "sudo auditctl -w /etc/ssh/sshd_config -p wa -k sshd_config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-aureport-au",
+    "command": "sudo aureport -au",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo aureport -au` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo aureport -au",
+    "example": "sudo aureport -au",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ausearch-k-sshd-config",
+    "command": "sudo ausearch -k sshd_config",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ausearch -k sshd_config` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ausearch -k sshd_config",
+    "example": "sudo ausearch -k sshd_config",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-find-perm-o-w-type-d-2-dev-null",
+    "command": "sudo find / -perm -o+w -type d 2>/dev/null",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo find / -perm -o+w -type d 2>/dev/null` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo find / -perm -o+w -type d 2>/dev/null",
+    "example": "sudo find / -perm -o+w -type d 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-find-type-f-name-pem-ls-2-dev-null",
+    "command": "sudo find / -type f -name \"*.pem\" -ls 2>/dev/null",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo find / -type f -name \"*.pem\" -ls 2>/dev/null` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo find / -type f -name \"*.pem\" -ls 2>/dev/null",
+    "example": "sudo find / -type f -name \"*.pem\" -ls 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-find-type-f-name-key-ls-2-dev-null",
+    "command": "sudo find / -type f -name \"*.key\" -ls 2>/dev/null",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo find / -type f -name \"*.key\" -ls 2>/dev/null` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo find / -type f -name \"*.key\" -ls 2>/dev/null",
+    "example": "sudo find / -type f -name \"*.key\" -ls 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-grep-r-private-key-home-2-dev-null",
+    "command": "sudo grep -R \"PRIVATE KEY\" /home 2>/dev/null",
+    "title": "Server Security logs/debugging command",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo grep -R \"PRIVATE KEY\" /home 2>/dev/null` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo grep -R \"PRIVATE KEY\" /home 2>/dev/null",
+    "example": "sudo grep -R \"PRIVATE KEY\" /home 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-ss-tulpn-grep-listen",
+    "command": "sudo ss -tulpn | grep LISTEN",
+    "title": "Server Security status/details verify karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo ss -tulpn | grep LISTEN` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo ss -tulpn | grep LISTEN",
+    "example": "sudo ss -tulpn | grep LISTEN",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-iptables-save",
+    "command": "sudo iptables-save",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo iptables-save` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo iptables-save",
+    "example": "sudo iptables-save",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-apparmor-status",
+    "command": "sudo apparmor_status",
+    "title": "Server Security status/details verify karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo apparmor_status` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo apparmor_status",
+    "example": "sudo apparmor_status",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-aa-enforce-profile",
+    "command": "sudo aa-enforce profile",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo aa-enforce profile` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo aa-enforce profile",
+    "example": "sudo aa-enforce profile",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-aa-complain-profile",
+    "command": "sudo aa-complain profile",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo aa-complain profile` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo aa-complain profile",
+    "example": "sudo aa-complain profile",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-semanage-port-l",
+    "command": "sudo semanage port -l",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo semanage port -l` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo semanage port -l",
+    "example": "sudo semanage port -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-restorecon-rv-var-www",
+    "command": "sudo restorecon -Rv /var/www",
+    "title": "Server Security restore/import command",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo restorecon -Rv /var/www` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo restorecon -Rv /var/www",
+    "example": "sudo restorecon -Rv /var/www",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-getenforce",
+    "command": "sudo getenforce",
+    "title": "Server Security status/details verify karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo getenforce` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo getenforce",
+    "example": "sudo getenforce",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-setenforce-0",
+    "command": "sudo setenforce 0",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo setenforce 0` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo setenforce 0",
+    "example": "sudo setenforce 0",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "server-security-sudo-setenforce-1",
+    "command": "sudo setenforce 1",
+    "title": "Server Security command use karna",
+    "category": "Server Security",
+    "difficulty": "Intermediate",
+    "use_case": "Server Security me `sudo setenforce 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo setenforce 1",
+    "example": "sudo setenforce 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "server",
+      "cmd",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/ssl-certbot.commands.json b/public/data/ssl-certbot.commands.json
index 5bde972..c139e11 100644
--- a/public/data/ssl-certbot.commands.json
+++ b/public/data/ssl-certbot.commands.json
@@ -480,5 +480,508 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "ssl-certbot-openssl-version",
+    "command": "openssl version",
+    "title": "SSL / Certbot status/details verify karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl version` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl version",
+    "example": "openssl version",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-x509-noout-subject-in-cert-pem",
+    "command": "openssl x509 -noout -subject -in cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl x509 -noout -subject -in cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl x509 -noout -subject -in cert.pem",
+    "example": "openssl x509 -noout -subject -in cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-x509-noout-issuer-in-cert-pem",
+    "command": "openssl x509 -noout -issuer -in cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl x509 -noout -issuer -in cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl x509 -noout -issuer -in cert.pem",
+    "example": "openssl x509 -noout -issuer -in cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-x509-noout-dates-in-cert-pem",
+    "command": "openssl x509 -noout -dates -in cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl x509 -noout -dates -in cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl x509 -noout -dates -in cert.pem",
+    "example": "openssl x509 -noout -dates -in cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-x509-noout-fingerprint-sha256-in-cert-pem",
+    "command": "openssl x509 -noout -fingerprint -sha256 -in cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl x509 -noout -fingerprint -sha256 -in cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl x509 -noout -fingerprint -sha256 -in cert.pem",
+    "example": "openssl x509 -noout -fingerprint -sha256 -in cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-x509-pubkey-noout-in-cert-pem",
+    "command": "openssl x509 -pubkey -noout -in cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl x509 -pubkey -noout -in cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl x509 -pubkey -noout -in cert.pem",
+    "example": "openssl x509 -pubkey -noout -in cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-verify-cafile-chain-pem-cert-pem",
+    "command": "openssl verify -CAfile chain.pem cert.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl verify -CAfile chain.pem cert.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl verify -CAfile chain.pem cert.pem",
+    "example": "openssl verify -CAfile chain.pem cert.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-s-client-connect-example-com-443-servername-example-com-tls1-2",
+    "command": "openssl s_client -connect example.com:443 -servername example.com -tls1_2",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl s_client -connect example.com:443 -servername example.com -tls1_2` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl s_client -connect example.com:443 -servername example.com -tls1_2",
+    "example": "openssl s_client -connect example.com:443 -servername example.com -tls1_2",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-s-client-connect-example-com-443-servername-example-com-tls1-3",
+    "command": "openssl s_client -connect example.com:443 -servername example.com -tls1_3",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl s_client -connect example.com:443 -servername example.com -tls1_3` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl s_client -connect example.com:443 -servername example.com -tls1_3",
+    "example": "openssl s_client -connect example.com:443 -servername example.com -tls1_3",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-s-client-connect-example-com-443-servername-example-com-dev-null-2-dev-null-openssl-x509-noo",
+    "command": "openssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null | openssl x509 -noout -dates",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null | openssl x509 -noout -dates` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null | openssl x509 -noout -dates",
+    "example": "openssl s_client -connect example.com:443 -servername example.com </dev/null 2>/dev/null | openssl x509 -noout -dates",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-req-text-noout-verify-in-csr-pem",
+    "command": "openssl req -text -noout -verify -in csr.pem",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl req -text -noout -verify -in csr.pem` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl req -text -noout -verify -in csr.pem",
+    "example": "openssl req -text -noout -verify -in csr.pem",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-req-newkey-rsa-4096-nodes-keyout-domain-key-out-domain-csr",
+    "command": "openssl req -newkey rsa:4096 -nodes -keyout domain.key -out domain.csr",
+    "title": "SSL / Certbot resource create/generate karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl req -newkey rsa:4096 -nodes -keyout domain.key -out domain.csr` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl req -newkey rsa:4096 -nodes -keyout domain.key -out domain.csr",
+    "example": "openssl req -newkey rsa:4096 -nodes -keyout domain.key -out domain.csr",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-rsa-in-domain-key-pubout",
+    "command": "openssl rsa -in domain.key -pubout",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl rsa -in domain.key -pubout` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl rsa -in domain.key -pubout",
+    "example": "openssl rsa -in domain.key -pubout",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-openssl-pkey-in-domain-key-check",
+    "command": "openssl pkey -in domain.key -check",
+    "title": "SSL / Certbot status/details verify karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `openssl pkey -in domain.key -check` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "openssl pkey -in domain.key -check",
+    "example": "openssl pkey -in domain.key -check",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "openssl s_client -connect example.com:443",
+      "curl -Iv https://example.com"
+    ],
+    "tags": [
+      "ssl",
+      "openssl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-plugins",
+    "command": "sudo certbot plugins",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot plugins` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot plugins",
+    "example": "sudo certbot plugins",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-register-email-email-example-com",
+    "command": "sudo certbot register --email email@example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot register --email email@example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot register --email email@example.com",
+    "example": "sudo certbot register --email email@example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-update-account-email-new-example-com",
+    "command": "sudo certbot update_account --email new@example.com",
+    "title": "SSL / Certbot resource create/generate karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot update_account --email new@example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot update_account --email new@example.com",
+    "example": "sudo certbot update_account --email new@example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-renew-pre-hook-systemctl-stop-nginx-post-hook-systemctl-start-nginx",
+    "command": "sudo certbot renew --pre-hook \"systemctl stop nginx\" --post-hook \"systemctl start nginx\"",
+    "title": "SSL / Certbot resource create/generate karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot renew --pre-hook \"systemctl stop nginx\" --post-hook \"systemctl start nginx\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot renew --pre-hook \"systemctl stop nginx\" --post-hook \"systemctl start nginx\"",
+    "example": "sudo certbot renew --pre-hook \"systemctl stop nginx\" --post-hook \"systemctl start nginx\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certonly-manual-d-example-com",
+    "command": "sudo certbot certonly --manual -d example.com",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certonly --manual -d example.com` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot certonly --manual -d example.com",
+    "example": "sudo certbot certonly --manual -d example.com",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certonly-manual-preferred-challenges-dns-d-example-com",
+    "command": "sudo certbot certonly --manual --preferred-challenges dns -d \"*.example.com\"",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certonly --manual --preferred-challenges dns -d \"*.example.com\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot certonly --manual --preferred-challenges dns -d \"*.example.com\"",
+    "example": "sudo certbot certonly --manual --preferred-challenges dns -d \"*.example.com\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-certbot-certificates-grep-expiry-date",
+    "command": "sudo certbot certificates | grep \"Expiry Date\"",
+    "title": "SSL / Certbot logs/debugging command",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo certbot certificates | grep \"Expiry Date\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo certbot certificates | grep \"Expiry Date\"",
+    "example": "sudo certbot certificates | grep \"Expiry Date\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-sudo-openssl-dhparam-out-etc-ssl-certs-dhparam-pem-4096",
+    "command": "sudo openssl dhparam -out /etc/ssl/certs/dhparam.pem 4096",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `sudo openssl dhparam -out /etc/ssl/certs/dhparam.pem 4096` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sudo openssl dhparam -out /etc/ssl/certs/dhparam.pem 4096",
+    "example": "sudo openssl dhparam -out /etc/ssl/certs/dhparam.pem 4096",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "systemctl status service",
+      "journalctl -u service -f"
+    ],
+    "tags": [
+      "ssl",
+      "cmd",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "ssl-certbot-curl-https-ssl-config-mozilla-org-ffdhe2048-txt",
+    "command": "curl https://ssl-config.mozilla.org/ffdhe2048.txt",
+    "title": "SSL / Certbot command use karna",
+    "category": "SSL / Certbot",
+    "difficulty": "Beginner",
+    "use_case": "SSL / Certbot me `curl https://ssl-config.mozilla.org/ffdhe2048.txt` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "curl https://ssl-config.mozilla.org/ffdhe2048.txt",
+    "example": "curl https://ssl-config.mozilla.org/ffdhe2048.txt",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "ssl",
+      "curl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/system-monitoring.commands.json b/public/data/system-monitoring.commands.json
index 6ed9d01..7dd3fef 100644
--- a/public/data/system-monitoring.commands.json
+++ b/public/data/system-monitoring.commands.json
@@ -930,5 +930,765 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "system-monitoring-vmstat-s",
+    "command": "vmstat -s",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `vmstat -s` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vmstat -s",
+    "example": "vmstat -s",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "vmstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-vmstat-d",
+    "command": "vmstat -d",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `vmstat -d` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vmstat -d",
+    "example": "vmstat -d",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "vmstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-iostat-xz-1",
+    "command": "iostat -xz 1",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `iostat -xz 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "iostat -xz 1",
+    "example": "iostat -xz 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "iostat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pidstat-d-1",
+    "command": "pidstat -d 1",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `pidstat -d 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pidstat -d 1",
+    "example": "pidstat -d 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pidstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pidstat-r-1",
+    "command": "pidstat -r 1",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `pidstat -r 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pidstat -r 1",
+    "example": "pidstat -r 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pidstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pidstat-u-1",
+    "command": "pidstat -u 1",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `pidstat -u 1` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pidstat -u 1",
+    "example": "pidstat -u 1",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pidstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-sar-r-1-5",
+    "command": "sar -r 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `sar -r 1 5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sar -r 1 5",
+    "example": "sar -r 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "sar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-sar-n-dev-1-5",
+    "command": "sar -n DEV 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `sar -n DEV 1 5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sar -n DEV 1 5",
+    "example": "sar -n DEV 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "sar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-sar-n-tcp-etcp-1-5",
+    "command": "sar -n TCP,ETCP 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `sar -n TCP,ETCP 1 5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sar -n TCP,ETCP 1 5",
+    "example": "sar -n TCP,ETCP 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "sar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-sar-b-1-5",
+    "command": "sar -b 1 5",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `sar -b 1 5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "sar -b 1 5",
+    "example": "sar -b 1 5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "sar",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-slabtop",
+    "command": "slabtop",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `slabtop` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "slabtop",
+    "example": "slabtop",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "slabtop",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-free-m",
+    "command": "free -m",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `free -m` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "free -m",
+    "example": "free -m",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "free",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-swapon-show",
+    "command": "swapon --show",
+    "title": "System Monitoring status/details verify karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `swapon --show` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "swapon --show",
+    "example": "swapon --show",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "swapon",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-swaps",
+    "command": "cat /proc/swaps",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `cat /proc/swaps` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cat /proc/swaps",
+    "example": "cat /proc/swaps",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-pressure-cpu",
+    "command": "cat /proc/pressure/cpu",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `cat /proc/pressure/cpu` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cat /proc/pressure/cpu",
+    "example": "cat /proc/pressure/cpu",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-pressure-memory",
+    "command": "cat /proc/pressure/memory",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `cat /proc/pressure/memory` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cat /proc/pressure/memory",
+    "example": "cat /proc/pressure/memory",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-cat-proc-pressure-io",
+    "command": "cat /proc/pressure/io",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `cat /proc/pressure/io` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "cat /proc/pressure/io",
+    "example": "cat /proc/pressure/io",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "cat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ps-eo-pid-ppid-cmd-mem-cpu-sort-cpu-head",
+    "command": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head",
+    "example": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%cpu | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ps",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ps-eo-pid-ppid-cmd-mem-cpu-sort-mem-head",
+    "command": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head",
+    "example": "ps -eo pid,ppid,cmd,%mem,%cpu --sort=-%mem | head",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ps",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pstree-p",
+    "command": "pstree -p",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `pstree -p` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pstree -p",
+    "example": "pstree -p",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pstree",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-pgrep-a-nginx",
+    "command": "pgrep -a nginx",
+    "title": "System Monitoring logs/debugging command",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `pgrep -a nginx` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "pgrep -a nginx",
+    "example": "pgrep -a nginx",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "pgrep",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-nproc",
+    "command": "nproc",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `nproc` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nproc",
+    "example": "nproc",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "nproc",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-dmidecode-t-memory",
+    "command": "dmidecode -t memory",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `dmidecode -t memory` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "dmidecode -t memory",
+    "example": "dmidecode -t memory",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "dmidecode",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-smartctl-a-dev-sda",
+    "command": "smartctl -a /dev/sda",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `smartctl -a /dev/sda` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "smartctl -a /dev/sda",
+    "example": "smartctl -a /dev/sda",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "smartctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-smartctl-h-dev-sda",
+    "command": "smartctl -H /dev/sda",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `smartctl -H /dev/sda` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "smartctl -H /dev/sda",
+    "example": "smartctl -H /dev/sda",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "smartctl",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-df-ih",
+    "command": "df -ih",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `df -ih` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "df -ih",
+    "example": "df -ih",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "df",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-du-xhd1",
+    "command": "du -xhd1 /",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `du -xhd1 /` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "du -xhd1 /",
+    "example": "du -xhd1 /",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "du",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-find-xdev-type-f-size-500m-2-dev-null",
+    "command": "find / -xdev -type f -size +500M 2>/dev/null",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `find / -xdev -type f -size +500M 2>/dev/null` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "find / -xdev -type f -size +500M 2>/dev/null",
+    "example": "find / -xdev -type f -size +500M 2>/dev/null",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "find",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lslocks",
+    "command": "lslocks",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `lslocks` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lslocks",
+    "example": "lslocks",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lslocks",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-lsns",
+    "command": "lsns",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `lsns` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "lsns",
+    "example": "lsns",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "lsns",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-ip-s-link",
+    "command": "ip -s link",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `ip -s link` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "ip -s link",
+    "example": "ip -s link",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "ip",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-nethogs",
+    "command": "nethogs",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `nethogs` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "nethogs",
+    "example": "nethogs",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "nethogs",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-bmon",
+    "command": "bmon",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `bmon` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "bmon",
+    "example": "bmon",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "bmon",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-vnstat",
+    "command": "vnstat",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `vnstat` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vnstat",
+    "example": "vnstat",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "vnstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-vnstat-l",
+    "command": "vnstat -l",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `vnstat -l` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "vnstat -l",
+    "example": "vnstat -l",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "vnstat",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-conntrack-l",
+    "command": "conntrack -L",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `conntrack -L` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "conntrack -L",
+    "example": "conntrack -L",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "conntrack",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-conntrack-s",
+    "command": "conntrack -S",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `conntrack -S` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "conntrack -S",
+    "example": "conntrack -S",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "conntrack",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-perf-top",
+    "command": "perf top",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `perf top` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "perf top",
+    "example": "perf top",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "perf",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-perf-stat-command",
+    "command": "perf stat command",
+    "title": "System Monitoring command use karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `perf stat command` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "perf stat command",
+    "example": "perf stat command",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "perf",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "system-monitoring-systemctl-status-no-pager",
+    "command": "systemctl status --no-pager",
+    "title": "System Monitoring status/details verify karna",
+    "category": "System Monitoring",
+    "difficulty": "Intermediate",
+    "use_case": "System Monitoring me `systemctl status --no-pager` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "systemctl status --no-pager",
+    "example": "systemctl status --no-pager",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [],
+    "tags": [
+      "system",
+      "systemctl",
+      "devops",
+      "cli"
+    ]
   }
 ]
diff --git a/public/data/terraform.commands.json b/public/data/terraform.commands.json
index 473493b..37c7d35 100644
--- a/public/data/terraform.commands.json
+++ b/public/data/terraform.commands.json
@@ -1000,5 +1000,731 @@
       "devops",
       "cli"
     ]
+  },
+  {
+    "id": "terraform-terraform-chdir-infra-init",
+    "command": "terraform -chdir=infra init",
+    "title": "Terraform resource create/generate karna",
+    "category": "Terraform",
+    "difficulty": "Beginner",
+    "use_case": "Terraform me `terraform -chdir=infra init` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform -chdir=infra init",
+    "example": "terraform -chdir=infra init",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-chdir-infra-plan",
+    "command": "terraform -chdir=infra plan",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform -chdir=infra plan` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform -chdir=infra plan",
+    "example": "terraform -chdir=infra plan",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-chdir-infra-apply",
+    "command": "terraform -chdir=infra apply",
+    "title": "Terraform deploy/run/install command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform -chdir=infra apply` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform -chdir=infra apply",
+    "example": "terraform -chdir=infra apply",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-var-instance-type-t3-micro",
+    "command": "terraform plan -var=\"instance_type=t3.micro\"",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -var=\"instance_type=t3.micro\"` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform plan -var=\"instance_type=t3.micro\"",
+    "example": "terraform plan -var=\"instance_type=t3.micro\"",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-target-resource-name",
+    "command": "terraform plan -target=resource.name",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -target=resource.name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform plan -target=resource.name",
+    "example": "terraform plan -target=resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-apply-target-resource-name",
+    "command": "terraform apply -target=resource.name",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform apply -target=resource.name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform apply -target=resource.name",
+    "example": "terraform apply -target=resource.name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-destroy-auto-approve",
+    "command": "terraform destroy -auto-approve",
+    "title": "Terraform risky cleanup/delete command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform destroy -auto-approve` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform destroy -auto-approve",
+    "example": "terraform destroy -auto-approve",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-output-name",
+    "command": "terraform output name",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform output name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform output name",
+    "example": "terraform output name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-output-raw-password",
+    "command": "terraform output -raw password",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform output -raw password` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform output -raw password",
+    "example": "terraform output -raw password",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Output me secrets/credentials ho sakte hain. Screenshot ya logs share karte time mask karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-list-grep-aws-instance",
+    "command": "terraform state list | grep aws_instance",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state list | grep aws_instance` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state list | grep aws_instance",
+    "example": "terraform state list | grep aws_instance",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-show-module-app-aws-instance-web",
+    "command": "terraform state show module.app.aws_instance.web",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state show module.app.aws_instance.web` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state show module.app.aws_instance.web",
+    "example": "terraform state show module.app.aws_instance.web",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-rm-module-old-resource",
+    "command": "terraform state rm module.old.resource",
+    "title": "Terraform risky cleanup/delete command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state rm module.old.resource` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state rm module.old.resource",
+    "example": "terraform state rm module.old.resource",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Dangerous command ho sakta hai. Production par run karne se pehle backup, target aur impact 2 baar verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-mv-module-old-module-new",
+    "command": "terraform state mv module.old module.new",
+    "title": "Terraform resource create/generate karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state mv module.old module.new` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state mv module.old module.new",
+    "example": "terraform state mv module.old module.new",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-import-module-app-aws-s3-bucket-bucket-bucket-name",
+    "command": "terraform import module.app.aws_s3_bucket.bucket bucket-name",
+    "title": "Terraform restore/import command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform import module.app.aws_s3_bucket.bucket bucket-name` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform import module.app.aws_s3_bucket.bucket bucket-name",
+    "example": "terraform import module.app.aws_s3_bucket.bucket bucket-name",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-refresh-false",
+    "command": "terraform plan -refresh=false",
+    "title": "Terraform restart/reload/refresh karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -refresh=false` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform plan -refresh=false",
+    "example": "terraform plan -refresh=false",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-apply-refresh-false",
+    "command": "terraform apply -refresh=false",
+    "title": "Terraform restart/reload/refresh karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform apply -refresh=false` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform apply -refresh=false",
+    "example": "terraform apply -refresh=false",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-refresh-var-file-prod-tfvars",
+    "command": "terraform refresh -var-file=prod.tfvars",
+    "title": "Terraform restart/reload/refresh karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform refresh -var-file=prod.tfvars` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform refresh -var-file=prod.tfvars",
+    "example": "terraform refresh -var-file=prod.tfvars",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-workspace-new-staging",
+    "command": "terraform workspace new staging",
+    "title": "Terraform resource create/generate karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform workspace new staging` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform workspace new staging",
+    "example": "terraform workspace new staging",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-workspace-select-staging",
+    "command": "terraform workspace select staging",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform workspace select staging` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform workspace select staging",
+    "example": "terraform workspace select staging",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-providers-mirror-providers",
+    "command": "terraform providers mirror ./providers",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform providers mirror ./providers` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform providers mirror ./providers",
+    "example": "terraform providers mirror ./providers",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-providers-lock-platform-linux-amd64",
+    "command": "terraform providers lock -platform=linux_amd64",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform providers lock -platform=linux_amd64` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform providers lock -platform=linux_amd64",
+    "example": "terraform providers lock -platform=linux_amd64",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-login-app-terraform-io",
+    "command": "terraform login app.terraform.io",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform login app.terraform.io` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform login app.terraform.io",
+    "example": "terraform login app.terraform.io",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-test-filter-tests-basic-tftest-hcl",
+    "command": "terraform test -filter=tests/basic.tftest.hcl",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform test -filter=tests/basic.tftest.hcl` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform test -filter=tests/basic.tftest.hcl",
+    "example": "terraform test -filter=tests/basic.tftest.hcl",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-fmt-check-recursive",
+    "command": "terraform fmt -check -recursive",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform fmt -check -recursive` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform fmt -check -recursive",
+    "example": "terraform fmt -check -recursive",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-validate-no-color",
+    "command": "terraform validate -no-color",
+    "title": "Terraform status/details verify karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform validate -no-color` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform validate -no-color",
+    "example": "terraform validate -no-color",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-detailed-exitcode",
+    "command": "terraform plan -detailed-exitcode",
+    "title": "Terraform logs/debugging command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -detailed-exitcode` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform plan -detailed-exitcode",
+    "example": "terraform plan -detailed-exitcode",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-graph-dot-tpng-graph-png",
+    "command": "terraform graph | dot -Tpng > graph.png",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform graph | dot -Tpng > graph.png` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform graph | dot -Tpng > graph.png",
+    "example": "terraform graph | dot -Tpng > graph.png",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-console-var-file-dev-tfvars",
+    "command": "terraform console -var-file=dev.tfvars",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform console -var-file=dev.tfvars` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform console -var-file=dev.tfvars",
+    "example": "terraform console -var-file=dev.tfvars",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-force-unlock-force-lock-id",
+    "command": "terraform force-unlock -force LOCK_ID",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform force-unlock -force LOCK_ID` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform force-unlock -force LOCK_ID",
+    "example": "terraform force-unlock -force LOCK_ID",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-pull-terraform-tfstate-backup",
+    "command": "terraform state pull > terraform.tfstate.backup",
+    "title": "Terraform backup/export command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state pull > terraform.tfstate.backup` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state pull > terraform.tfstate.backup",
+    "example": "terraform state pull > terraform.tfstate.backup",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-state-push-terraform-tfstate",
+    "command": "terraform state push terraform.tfstate",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform state push terraform.tfstate` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform state push terraform.tfstate",
+    "example": "terraform state push terraform.tfstate",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-apply-parallelism-5",
+    "command": "terraform apply -parallelism=5",
+    "title": "Terraform deploy/run/install command",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform apply -parallelism=5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform apply -parallelism=5",
+    "example": "terraform apply -parallelism=5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Production me run karne se pehle maintenance impact aur rollback plan verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
+  },
+  {
+    "id": "terraform-terraform-plan-parallelism-5",
+    "command": "terraform plan -parallelism=5",
+    "title": "Terraform command use karna",
+    "category": "Terraform",
+    "difficulty": "Intermediate",
+    "use_case": "Terraform me `terraform plan -parallelism=5` command real-world DevOps, server administration, troubleshooting, deployment, security ya cloud operations ke liye useful hai.",
+    "syntax": "terraform plan -parallelism=5",
+    "example": "terraform plan -parallelism=5",
+    "output_explanation": "Output command/tool/version ke hisaab se change ho sakta hai. Status, paths, IDs, resource names, metrics, logs ya error messages carefully verify karo.",
+    "warning": "Read-only ya low-risk command hai, phir bhi environment aur target verify karo.",
+    "related_commands": [
+      "terraform plan",
+      "terraform apply",
+      "terraform state list"
+    ],
+    "tags": [
+      "terraform",
+      "devops",
+      "cli"
+    ]
   }
 ]
